# 更新说明：选择性重做与段落编排（重摇 / 单段二采 / 拖拽调序 / 段禁用）

> 2026-08-27。此前「重跑这段」会级联重做其后全部段落，「重跑这段」与「重摇此段」
> 功能重叠，「重置二采」语义不明，手动选择模式的勾选框切换后不出现。本次围绕
> 六个模块完成选择性重做（只重做标记段、其余段保留）与段落编排（调序/禁用）。

## 一、需求与模块对照

| 模块 | 需求 | 入口 |
|---|---|---|
| A 选择性重做 | 重做某段不级联重做后面全部；可间隔重做多段；四种锚定模式；按顺序单独跑某几段 | 段卡片「🎲 重摇」→ 模式弹窗 → 页脚「重摇已标记 N 段」 |
| B 单段重新二采 | 「重置二采」升级为立即重做本段二采（含插入视频段） | 段卡片「🔄 重新二采此段」 |
| C 手动选择刷新修复 | 勾选框切换不立即出现；链完成后无法执行二采 | （自动修复） |
| D 按钮简化 | 删「重跑这段」，保留「从这段继续」+「重摇」 | 段卡片 actions 区 |
| E 拖拽调序 | 段卡片拖拽换执行顺序 | 卡片标题 ⠿ 把手（hover 另有 ⬆/⬇ 兜底按钮） |
| F 段禁用（不上链） | 某段保留在链上但不执行、不进成片 | 段卡片「⏸ 不上链」toggle |

## 二、模块 A：选择性重做（重摇标记）

### 交互流

1. 已完成段点「🎲 重摇」→ 弹窗选**锚定模式**（默认双锚）→「标记」（不立即执行）；
2. 可连续标记多段（卡片出「🔁 待重摇·模式」徽章），页脚 CTA 变「🎲 重摇已标记 N 段」；
3. 点页脚提交：随机种子 + 临时切逐段审片 → 队列快照写入 `manifest.redo_queue`；
4. 审片模式**每运行推进一段**（报告提示剩余 N 段），满意再运行继续；已提交未执行的
   标记可在弹窗「取消重摇标记」撤销（后端 `redo_cancel` 接口幂等清队列条目）；
5. 全部重摇完成后**运行一次自动重拼全片**（或用合并导出即时取片）。

### 四种锚定模式（本次重做的临时策略，与段属性「独立镜头」无关）

| 模式 | 首锚（上段尾帧桥） | 尾锚（下段保留段首帧） | 适用 |
|---|---|---|---|
| 双锚（默认） | ✅ | ✅ | 无缝替换 |
| 仅锚上段 | ✅ | ❌ | 结尾想大改（之后重摇下段） |
| 仅锚下段 | ❌ | ✅ | 开头重新起手 |
| 无锚 | ❌ | ❌ | 完全自由发挥，两端硬切 |

- 用户显式身份锚（尾帧图/每段尾帧锚定）**优先**于接缝尾锚，不受模式影响；
- 下一段是插入段/独立镜头/禁用段/未完成/也在重摇列表 → 该侧锚自动缺省。

### 后端机制（nodes.py）

- `_parse_redo_segs`：ds 标记解析校验（越界/序章/插入段/非法模式/禁用段剔除）；
- 种子 bump（`_redo_seed`）：重摇段种子 = 控件种子+i，**与存档种子相同则 +1**——
  保证每次重摇种子必变 → `base_hash` 必变 → 二采记录自动失效重渲（高清跟着换）；
- redo 段锚定 eff 值**同时供采样与 `_up_hi`**：基础链与二采重采样锚一致，行为不漂移；
- 队列逐段消费原子落盘：审片中段 break 后剩余队列保留在 manifest，前端据此显示
  「继续重摇剩余 N 段」；
- **不触发 truncate**（只换种子不改提示词）→ 保留段二采记录不清除——这是选择性
  重做省时的来源；redo 与「重跑起始段」互斥（redo 优先，报告行说明）；
- manifest 写入合并旧尾（`_merged_list`）：审片中段 break 时循环内积累的记录列表
  短于 done，直接写会丢后续保留段记录——新积累前缀 + 旧记录补尾。

## 三、模块 B：单段重新二采

「↺ 重置二采」升级为「🔄 重新二采此段」：清本段二采记录（`/h3chain/upscale_reset`）
→ 临时切「手动选择 + 只勾本段」提交队列 → 立即还原设置。其他段因回放 `fresh=False`
直接沿用已有 mp4 不被覆盖。显示条件放宽到「已选二采模型」即可见（未做过二采的段
也能补做），**插入视频段同样适用**——其二采 latent 来自 `seg_NNN.pt` 存档（插入时
已一次性 VAE 编码），全程不碰原视频文件的编解码。

插入段二采说明：分辨率增益来自放大网络（与提示词无关）；空提示词低强度重采样是
设计内的「轻精修」（无条件视频先验补高频纹理、消放大伪影）。是 AI 放大重渲染非
逐像素无损；同参数重渲 = 同输出（确定性），效果不满意调全局二采参数而非重跑。

## 四、模块 C/D：刷新修复与按钮简化

- `cardsSignature` 并入 `upscale.mode/include`、`redo_segs/redo_queue`、
  `segments[i].disabled`——切模式/勾选/标记/禁用后卡片立即重建（此前要切页再回来）；
- 链完成后手动选择模式勾了段且选了模型 → 页脚「▶ 执行二采（已勾选 N 段）」可点
  （此前被"已完成"锁死）；
- 已完成段删「▶ 重跑这段」，保留「▶ 从这段继续」（其后全部级联重做）+「🎲 重摇」
  （只重做本段），title 说明两者区别。

## 五、模块 E：拖拽调序

- 卡片标题 ⠿ 把手拖动（HTML5 DnD），插入线吸附显示；hover 另有 **⬆/⬇ 兜底小按钮**
  （按提示词段名次换位，中间隔着插入视频一并跨过，走同一条提交路径）；
- **后端零改动**：重排后逐段哈希序列变化，`reroll_start` 自动从重排点级联重做
  （链语义正确：段顺序变了锚定依赖必然重算）；
- 插入段位置不直接拖（由 pos 决定，随前驱提示词段走）；拖动范围内的重摇标记与
  二采勾选自动清除（槽位整体错位，范围外不受影响）；
- `reorderChain`/`nudgeIns` 纯函数进 `_js_harness` 单测（22 例）。

## 六、模块 F：段禁用（不上链）

**设计原则：槽位稳定、记录保留、只跳过执行。** 禁用/启用不触发任何重做。

- 前端：段卡片「⏸ 不上链」toggle；禁用段半透明虚线框 +「⏸ 已禁用」徽章 + 进度轨
  斜纹；禁用段只提供「▶ 重新上链」入口（重摇/二采/合并勾选均无意义）；
- 后端主循环：禁用段不采样/不解码/不进成片，manifest 记录类列表沿用旧记录（无则
  空占位）**保槽位不错位**，done 照常推进；段文件缺失由 replay 存在性守卫兜底
  （重新上链时自动采样）；
- 引导桥跨禁用段：guide 不更新 → 下段锚定最近一个已执行段的尾帧（与成片顺序一致）；
- 事实首段判定（`_is_fact_first`）：禁用段 0 时段 1 成为事实首段（不裁头不锚定）；
- 成片剔除（两路同口径）：二采高清拼接 `try_final(skip_slots=...)`、基础内存帧
  本就不含禁用段；合并导出勾选框同样不提供；
- 已完成段的禁用/启用零成本（latent 存档原样，随时恢复）；redo 标记与禁用互斥
  （`_parse_redo_segs` 与前端 `redoSlotValid` 同口径拦截，队列合并处防御性复验）。

## 七、Bugfix（本次审查修复）

1. **禁用段必崩**：主循环禁用分支引用了未初始化的 `_skipped_n`（UnboundLocalError）
   ——删除该无用计数（统计已由运行前报告行覆盖）；
2. **try_final 不跳禁用段**：禁用段有旧高清记录会混进成片、无记录会永久阻塞全片
   高清——`try_final` 加 `skip_slots` 参数，校验与拼接均剔除；
3. **队列残留不复验禁用**：段标记重摇后再被禁用，队列条目永久滞留（每次运行都报
   却永不执行）——队列合并处增加禁用复验，滞留条目自动滤除；
4. **redo + 禁用段误跳自动成片**：半截成片判定 `len(seg_frames) < total` 在有禁用段
   时恒真——口径改为「实际执行段数」（total − 禁用数）；
5. **全禁用链崩溃**：`torch.cat(all_frames)` 空列表抛错——单帧黑场占位 + 静音波形
   保下游节点不崩，报告行说明；
6. **审片完成文案**：有禁用段时「本链已全部完成」永不显示——同口径修正；
7. **E2 记忆锚失效**：段 0 禁用时锚永不出（E2 静默失效）——来源段改为第一个启用
   的提示词段；
8. **合并导出可勾禁用段**：与成片口径不一致——已禁用段不给勾选框（灰显）。

## 八、测试

- `tests/test_redo.py`（19 例）：`_parse_redo_segs`（合法/越界/序章/插入段/非法
  mode/禁用剔除）、`_redo_seed` bump 规则（连续重摇交替不死循环）、`_merged_list`
  补尾/无操作/截断、`_seam_tail_kf` 切片、truncate 清空 redo_queue；
- `tests/test_upscale.py` 新增 `test_try_final_skips_disabled_slots`：禁用段无记录
  不阻塞全片高清、有记录不混进成片、全禁用返回 False；
- `tests/_js_harness.js` 新增 22 例：reorderChain 重排/边界/非法入参、nudgeIns
  名次换位/跨插入段/边界 null/与 reorderChain 联动、redoSlotValid/redoMap/
  redoPending/redoQueued 纯逻辑。

## 九、部署

同步到 ComfyUI（`同步到ComfyUI.bat` 或 `sync_to_comfyui.ps1`）→ 重启 ComfyUI →
浏览器 Ctrl+F5。旧项目存档无需迁移：`ds.redo_segs`/`segments[i].disabled` 均为
缺省即旧行为（普通续跑/全部启用）。
