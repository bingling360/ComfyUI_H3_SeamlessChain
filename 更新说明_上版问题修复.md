# 更新说明：上版（选择性重做与段落编排）问题修复

> 2026-08-27。上一版引入的选择性重做/段落编排功能存在五处问题：不上链点了没反应、
> 单独重摇某段报参数不一致、二采手动选择模式怪异且多余、对插入视频做二采没必要、
> 实验性功能面板要"先做点什么"才显示。本次全部修复，无新增功能。
>
> **追加（同日晚）：修复「重新二采此段提交后不生效」**——详见第九节。

## 一、问题与修复对照

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 「不上链」点了没反应，只在上边显示"已禁用" | 旧版是按钮→写状态→无刷新触发，卡片上的"⏸ 已禁用"徽章是唯一反馈 | 改为「分段处理」内的**勾选框**：勾选即状态，点选立即生效并当场反映（与「独立镜头」勾选框同款交互） |
| 2 | 单独重摇某段报「存档参数与当前不一致」 | 画布控件在成链后被改过（步数/CFG/画幅等），后端 `assert_match` 硬校验拦截 | 提交重摇前**自动套用存档参数**到画布（静默纠偏，LED 显示还原了哪些项）+ 报错文案分场景改进 |
| 3 | 二采手动选择模式怪异、没必要 | 上版的"勾选哪些段做二采"交互与段卡片「重新二采此段」完全重叠 | **界面移除**该模式（下拉/卡片勾选/页脚按钮全删），只留「关闭/跟随生成」；后端保留为内部通道 |
| 4 | 对插入视频二采没必要 | 上版把插入段也纳入二采（VAE 重编码+神经放大+重采样），对外部成品只引入二次损失 | 插入段/序章**不再二采**；成片拼接时这些段按基础分辨率直通、统一缩放对齐目标画幅 |
| 5 | 实验性功能面板要先做点什么才显示 | 实验区渲染签名（esig）未纳入**节点存在性**：节点未就绪时渲染过"没有节点"，节点就绪后签名不变则永不重建 | esig 加入 `!!data.node`；画布节点载入/新建钩子（`loadedGraphNode`/`nodeCreated`）主动补刷 |

## 二、修复 1：不上链改为勾选框

- 位置：段卡片展开的「分段处理」面板内，与「🔗 独立镜头」同款式样（红色系区分）；
- **勾选框即状态**：`checked = !!seg.disabled`，勾/取消当场写入 `segments[i].disabled`
  并 `scheduleRefresh(60)` 立即重建卡片——不再有"点了没反应"的中间态；
- 卡片上的「⏸ 已禁用」徽章与半透明虚线框保留（只读视觉提示）；
- 已禁用段卡片仍保留「▶ 重新上链」快捷入口（等价于取消勾选）；
- tooltip 完整说明语义：保留链上槽位与存档、不采样不解码不进成片、下段锚定跨接、
  取消勾选零成本恢复。

## 三、修复 2：重摇参数一致性

**为什么单段重摇也要参数一致**：重摇段与链上其余段共享同一批锚定/桥接与采样配置，
参数不同 = 新段与旧段画风不一致的链混搭；存档 latent 的回放与续接也假设同配置。
这不是多余的校验，而是链一致性的底线。

**修复分两层**：

1. **前端自动纠偏（`submitRedo` → `applyChainParams`）**：提交重摇前静默把存档的
   全部共享参数写回画布控件——画幅（AR×MP 命中组合或切自定义+宽高）、每段时长、
   引导帧数、步数、CFG、采样器、调度器、递减锚定、桥帧门控三件套（模式/阈值/上限）。
   值已一致则零改动零提示（幂等）；实际还原的项写进 LED：「已自动还原存档参数：步数 24、CFG 6…」。
2. **后端报错文案分场景（`checkpoint.assert_match`）**：
   - 数值类参数不一致 → 说明"数值类参数可在项目列表点「套用参数」一键还原"；
   - **链结构**（chain：生成模式/首帧/参考素材组合）或**实验性功能开关**不一致 →
     说明需人工把这些切回存档状态（这类不能静默改：结构变化需要人决策）。

配套：手动「套用参数」按钮复用同一核心（`applyParamsToCanvas` → `applyChainParams`），
覆盖面从 7 项扩到 11 项（补递减锚定/门控三件套）；自定义画幅套用时**同时把宽高比
切到「自定义」**（只写宽/高控件不生效——后端非自定义时按 AR×MP 重新换算覆盖）。

## 四、修复 3：移除二采手动选择模式

- 下拉只剩「关闭 / 跟随生成」；段卡片的二采勾选框、链完成后的页脚「执行二采
  （已勾选 N 段）」按钮、相关 CSS 全部删除；
- 单段重做二采走段卡片「🔄 重新二采此段」即可（上版已实现，覆盖全部场景）；
- 「手动选择」作为**后端内部模式保留**：单段重做按钮临时切「手动选择+只勾本段」
  提交后立即还原，用户全程不可见——这是「只重渲这一段」的实现通道，非用户功能；
- 旧存档里 `upscale.mode == "手动选择"` 的残留值读回时归一化为「关闭」（界面无此项）。

## 五、修复 4：插入视频/序章不再二采

**判定**：插入段/序章是外部成品素材，VAE 重编码后神经放大+低强度重采样只引入二次
损失，分辨率增益对成品素材意义有限——上版的"插入段二采"属于过度设计，移除。

- 后端 `_up_hi`：`kind != "prompt"` 直接跳过（序章/插入段不进二采渲染管线）；
- 单段重做按钮对插入段不再显示（前后端口径一致）；
- **成片拼接（`upscale.try_final`）混合源处理**：
  - 生成段：必须有有效高清记录，缺任一段回退基础分辨率成片（原行为）；
  - 外部素材段：无记录时按**基础分辨率分段直通**（`seg_NNN.mp4` 即基础产物）；
  - 拼接目标画幅 = 首个带记录分段的实测尺寸，直通段 `reformat` 缩放对齐
    （`media.concat_av_mp4` 已有 reformat 能力，同画幅时零损直通）；
  - 基础分段缺失（文件被删）→ 回退基础分辨率编码并在报告说明，不静默丢段；
  - **旧档兼容**：上版已二采过的插入段记录仍有效（记录有效=文件已是高清），
    拼接时按记录尺寸走，不会被降级；
- 报告行明示构成：「外部素材段 N 段按基础分辨率缩放对齐」。

## 六、修复 5：实验性功能面板不刷新

- 渲染签名 `esig` 并入 `!!data.node`：节点未就绪时渲染的"没有节点"占位与节点就绪后
  的真实面板签名不同 → 节点就绪后的下一次刷新必然重建（此前 experiments 全空时
  两头签名相同，面板永久停留在"没有节点"，要手动改点别的操作才触发重建）；
- `app.registerExtension` 增加 `loadedGraphNode` / `nodeCreated` 钩子：画布载入工作流
  或新建 H3 节点时主动 `scheduleRefresh(250)`——面板首刷早于工作流载入的场景
  （开 ComfyUI 直开导演台）不再需要"先做点什么"。

## 七、测试

- `tests/_js_harness.js`（195 项断言全过）：
  - 新增 `applyChainParams` 14 例：组合画幅迁移 / 时长按帧数换算 / 引导帧数写回 /
    步数 CFG 采样器调度器写回 / 递减锚定数值写回 / 门控三件套写回 / 套用报告
    覆盖全部改动项 / 幂等重放零改动 / 自定义画幅切宽高比 / fade 0 回落关闭 /
    选项表外组合值跳过；
  - 内部模式透传 3 例：「手动选择」widget 原值透传（setDs 不归一化）、getDs 读回
    归一化回落「关闭」、内部勾选读回保留；
  - 移除已删函数 `toggleUpscaleInclude` 的旧 4 例。
- `tests/test_upscale.py` 新增 `test_try_final_external_passthrough`：序章+插入段无
  高清记录时按基础分段直通、目标画幅取首个带记录段实测尺寸、外部素材段计数进
  报告、基础分段缺失回退不静默丢段。
- 后端全量：170 passed, 5 skipped（torch 相关模块在无 torch 环境按设计跳过，
  ComfyUI 环境全跑）。

## 八、部署

同步到 ComfyUI（`同步到ComfyUI.bat` 或 `sync_to_comfyui.ps1`）→ 重启 ComfyUI →
浏览器 Ctrl+F5。旧项目存档无需迁移：二采模式旧值读回归一化、插入段旧二采记录
沿用、无任何 manifest 键变化。

## 九、追加修复：重新二采此段提交后不生效

**现象**：点段卡片「🔄 重新二采此段」后，运行日志只有模型加载和
「[H3自动保存] 编码完整成片」——主模型没参与采样，二采渲染根本没发生，
最后只得到一次基础分辨率的全链回放+成片。

**根因**：`doUpscaleSeg`（重新二采提交）缺少 `submitRedo`（重摇提交）已有的健壮性
处理——重新二采走的是「同一条链、参数必须与存档一致」的校验路径，而提交前
画布控件若被改过（步数/CFG/画幅等），后端 `assert_match` 直接让**整次运行失败**；
失败后用户再点普通运行，就只剩全链回放+基础成片。具体四个缺口：

| 缺口 | 后果 | 修复 |
|---|---|---|
| 提交前不套用存档参数 | 画布参数漂移 → 后端 assert_match 整次失败，二采记录已清但渲染没跑 | 提交前静默套用存档共享参数（`applyChainParams`，与重摇同口径），还原项写进 LED |
| 不清残留重摇标记 | 之前标记的 redo_segs 优先级更高，劫持本次提交变成"重采样基础链" | 提交时临时清空（队列快照在服务端），完成后恢复原标记——不丢用户状态 |
| 不关「审片模式=逐段确认」 | 全链回放被逐段打断，要连点 N 次运行才轮到目标段 | 提交时临时置「关闭」，完成后恢复 |
| 队列提交失败只 console.warn | queuePrompt 异常时用户毫无感知，LED 仍显示"已提交" | 失败走 `setApiError` 明示（并说明该段记录已清、下次会自动补渲） |

另加一道**前置校验**：提交前请求 `/h3chain/upscale_models` 确认所选放大权重文件
仍在 `models/latent_upscale_models/` 目录里——换显卡/重装 ComfyUI 后权重文件
丢失时，后端 `load_net` 降级只在运行报告里留一行（容易漏看），现在提交当场
弹窗拦下。

**与显卡无关的说明**：换 32G 魔改 4080S 本身不影响二采决策链——显存预检在
DynamicVRAM（aimdo）环境下账面紧张只警告不硬停；本次问题的触发点在参数校验
与提交时序，不在硬件。若之后遇到「二采健康预检未通过」字样的整链终止，
那才是显存预检在拦截（报告里会给出建议倍率/帧数）。

**自愈性**：`upscale_reset` 清掉记录后即使提交失败也不白清——之后任何一次开启
二采的运行（跟随生成或再次重新二采）都会按"记录缺失"自动补渲染该段。
