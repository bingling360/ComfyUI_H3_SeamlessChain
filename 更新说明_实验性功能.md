# 更新说明：实验性功能（生成期 in-process 干预）

第二阶段优化的交付物：一个**默认全关、可任意组合、后端可强制关闭**的实验性功能框架 + 四项生成期 in-process 干预。所有功能都放在导演台前端的「🧪 实验性功能」面板下，用户可逐项开启实测效果、再试组合，全程通过 `tools/ab_report.py` 量化对比择优。

## 核心约束（A/B 实验干净性的保证）

1. **默认全部关闭**：四项实验出厂全关，前端一键可全关。
2. **后端硬开关**：环境变量 `H3_EXPERIMENTS=0` 强制全关，优先级最高，忽略一切前端开关。
3. **全关 = 现状逐字节一致**：所有实验逻辑都用 `exp.has("eN_xxx")` 包裹，分支外不触碰任何已有变量的默认值；seed 序列默认不受实验影响（同起点可对比）。
4. **切换实验触发整链重做**：`ExperimentContext.fingerprint()` 参与 `ckpt_params` 指纹，不同实验组合在同一项目文件夹下会被判为参数不一致而整链重做，杜绝缓存污染。

## 四项实验一览

| 编号 | 名称 | 分组 | 核心思路 |
|---|---|---|---|
| E1 | 强化引导桥 + 段内分片 | 生成结构 | 把段间引导桥从单帧硬切改为滑窗/重叠条件采样；段内拆子片抑制中段漂移 |
| E2 | 全局记忆锚 | 长链记忆 | 从首段提取关键帧 latent，沿整链恒定注入，抑制长链逐段累积漂移 |
| E3 | 运动感知闭环门控 | 闭环调控 | 把接缝重摇的触发信号从「帧差」扩展为帧差 + 光流/相机 z-score |
| E4 | 双向过渡重生成 | 过渡重采样 | 对超阈值缝区做 past\|transition\|future 三窗双锚 + 缝区独占噪声的定向重采样 |

### E1 强化引导桥 + 段内分片

**参数**：
- `滑窗token`：引导桥拆成的子窗口大小（token 单位）。0 = 退化单窗（= 现状）。
- `子片帧数`：段内分片的子片长度（像素帧）。0 = 不分片。
- `重叠token`：相邻子窗口的重叠 token 数。

**实现**：
- `experiments.e1_windows()` + `experiments.e1_window_kf()` 纯逻辑计算滑窗布局。
- `nodes.py _apply_guide(e1_windows=None)` 支持多窗 keyframe 序列。
- 全关时 `e1_kfs=None`，走原单桥路径。

### E2 全局记忆锚

**参数**：
- `记忆帧数`：从首段开头提取多少帧作为全局 reference。
- `注入位置`：`段首`（只在段首 0 帧位置注入）/ `全程`（段首 + 段中各注入一个锚）。

**实现**：
- 首段生成后，把开头 `mem_t` 个 token 的视频 latent 落盘为 `memory_anchor.pt`（项目目录内）。
- 后续段采样前载入并构造成 keyframe，追加到 `_apply_guide` 的 keyframes 列表。
- 记忆锚记录写入 manifest `memory_anchor` 键（哈希 + mem_t）。
- `truncate(start==0)` 整链重做时自动清除旧锚文件与记录。

### E3 运动感知闭环门控

**参数**：
- `运动z阈值`：flow_z / cam_z 的阈值（σ），默认 2.0（对齐验收线）。
- `触发动作`：`重摇`（换种子重采整段）/ `重锚`（用更硬的锚定再采一次）。

**实现**：
- 首次采样后（attempt==0）算一次 `metrics.evaluate_local` 得 flow_z/cam_z。
- `experiments.e3_motion_trigger()` 纯逻辑判定是否触发。
- 触发时 `motion_extra += 1`（额外一次重采）；重摇动作沿用现有换种子循环；重锚动作把 `aug` 减半（锚定更硬）。
- 全关时 `motion_extra=0`，重摇判定与现状完全一致。

### E4 双向过渡重生成

**参数**：
- `过渡窗帧数`：transition 窗的长度（像素帧，向下对齐 17k+5 网格）。
- `重生成步数`：transition 窗的采样步数。
- `双锚强度`：双锚的锚定强度倍率（越大锚越硬）。

**实现**：
- `transition.transition_windows()` 计算 past/transition/future 三窗（网格对齐）。
- `transition.dual_anchor_keyframes()` 构造缝前+缝后双锚 keyframe。
- 首次采样后若缝差超阈值且 E4 开，则对 transition 窗用全新随机噪声（缝区独占噪声）做低步数重采样，只替换 transition 窗的 latent，前后帧不动。
- **与整段重摇互斥、E4 优先**：E4 后仍不合格再回退整段重摇。
- 全关时完全跳过，与现状逐字节一致。

## 使用方法

### 前端操作

导演台面板底部新增「🧪 实验性功能」分区：
- 每个实验一个卡片，含复选框 + 参数输入。
- 顶部「✕ 全部关闭」按钮一键清空。
- 底部说明文案提示：切换实验组合会触发对应段重新生成。

### A/B 对比

```bash
python tools/ab_report.py <基线目录> <实验目录> --md report.md
```

报告在参数差异区显式打印 A/B 的实验性功能状态与 E2 记忆锚记录，便于单变量自检。

### 后端强制关闭

```bash
# Windows PowerShell
$env:H3_EXPERIMENTS = "0"

# Linux / macOS
export H3_EXPERIMENTS=0
```

强制关闭后，生成序列与实验性功能出现前**逐字节一致**，报告首行标注：`实验性功能：后端已强制关闭（H3_EXPERIMENTS=0）`。

## 验证清单

- [x] 全关回归：实验全关时，生成逻辑与现状逐行一致（代码走原分支）。
- [x] 框架单测：`tests/test_experiments.py` 覆盖开关/指纹/全关路径/E1/E2/E3 纯逻辑。
- [x] E4 纯逻辑单测：`tests/test_transition.py` 覆盖三窗划分/双锚构造/互斥优先级。
- [x] 前端 `node --check` 通过。
- [ ] 真回归：在 ComfyUI 环境跑 `pytest tests/`（需要 torch + cv2）。
- [ ] 四端到端冒烟：逐项开启 → 单段生成不报错；组合开启 → 报告/存档正确标注。

## 文件清单

| 文件 | 改动 |
|---|---|
| `experiments.py` | 新增：实验定义、`ExperimentContext`、`FORCE_DISABLED`、E1/E2/E3 纯函数 |
| `transition.py` | 新增：E4 三窗划分、双锚 keyframe、互斥判定纯逻辑 |
| `nodes.py` | 编辑：execute 解析 ctx、报告标注、指纹；E1/E2/E3/E4 注入分支 |
| `checkpoint.py` | 编辑：manifest 新键 `experiments`/`memory_anchor`、assert_match 严格比对、truncate 清锚、记忆锚存取 |
| `web/h3_director.js` | 编辑：实验性功能面板 + 状态 JSON 读写 |
| `tools/ab_report.py` | 编辑：参数差异区追加实验状态与记忆锚记录 |
| `tests/test_experiments.py` | 新增：框架 + E1/E2/E3 纯逻辑单测 |
| `tests/test_transition.py` | 新增：E4 纯逻辑单测 |
