# 更新说明：评测基线 + 删帧止损 + 糊缝修复

批次 0（五批路线图首批）：把"缝好不好"变成可量化对比，同时修掉两个最痛的问题——删帧失控与接缝发糊。

---

## 一、本轮更新了什么

### 1. 新增评测基线 `metrics.py`（五维接缝 z-score）

单一帧差看不出断续类型。现在每条缝自动测五个维度，统一量纲为**缝处值 vs 段内稳健基线（median/1.4826×MAD）的 z-score**，**|z| < 2.0 为合格缝**：

| 指标 | 实现 | 断续归因 |
|---|---|---|
| flow_z / flow_accel_z | OpenCV Farneback 光流 + 速度二阶差分 | motion 断续 |
| lpips_z | `pip lpips`（AlexNet），缺包降级梯度结构差 | 时序整体突变 |
| emb_z | open_clip 余弦距离，缺包降级 RGB 直方图 | appearance 漂移 |
| cam_z | ORB + 仿射分解（平移/旋转/缩放速率） | camera 跳变 |
| pose_z | DWPose（controlnet_aux），默认关 | geometry 断续 |

- 依赖**全部可选**：cv2 是 ComfyUI 自带；lpips/open_clip 缺什么降级什么，绝不硬依赖、绝不抛错；
- 写入 manifest 新键 `seam_metrics`（旧 `seams` 键格式不变，向后兼容）；
- 接缝体检节点（H3SeamDoctor）报告同步新增 `[基准]` 行与汇总行，与存档同标定；
- 单测 `tests/test_metrics.py`：合成"匀速运动 + 可控突变缝"验证方向性（好缝 z 低、瞬移/漂移缝对应维度 z 飙升）。

### 2. 删帧止损（修"30 秒被删 7 秒"）

定位到三处删帧叠加：**智能切镜**（段尾 1/3 无上限搜索，124 帧段最多丢约 41 帧）+ **门控回退**（每段最多 34 帧）+ **token 网格向下对齐**（0–16 帧/段）。两个新控件（均追加在控件列表尾部，旧工作流 JSON 兼容）：

- **切镜最多丢帧**（默认 17，0=只标注不裁剪，进存档指纹）：单段切镜上限；
- **全链丢弃预算**（默认 48 帧=2s，进存档指纹）：整链累计上限，超预算后智能切镜自动降级为"只标注不裁剪"，门控回退仍生效但记 WARN；
- 链尾新增汇总行：`累计丢弃 X 帧（X.Xs，占计划时长 Y%）`。

### 3. 糊缝修复（修"接缝容易糊掉"）

糊的三个来源逐一处理：

- **自适应精修**（新控件，默认开）：按缝差分档 <0.04→0.30 / 0.04–0.08→0.45 / >0.08→0.55——固定 0.45 整窗重去噪会把好缝也重新去噪一遍，细节软化；
- **双端锚定**：精修窗口注入第二条 keyframe（本段侧窗口末 2 token=5 帧，锚 `resolved_frame_index=窗口帧数-5`）。此前只锚缝前侧，模型自由改写无锚端，末端羽化回原帧造成二次边界；
- **兜底策略修正**：精修后缝差反而升高时**保留精修结果**（不再回退像素混合——加权平均是糊的第二来源），仅精修抛异常时才用像素混合；窗口不足时跳过该缝处理；
- smoothstep 像素混合模式在报告中标注"建议仅调试使用"。

### 4. 新增 `tools/ab_report.py`（A/B 实验统一出口）

```
python tools/ab_report.py <基准存档目录> <实验存档目录> --md 对比.md
```

输出：每缝帧差 + 五维 z 对比表（A/B 并排）、帧数守恒与丢弃统计（含预算核查）、参数指纹差异自检（A/B 应只差在实验开关上，否则结论不可信）、种子一致性。纯标准库，任何 python 可跑。

### 5. 兼容性

- 旧存档续跑：manifest 缺 `smart_cut_max`/`drop_budget` 等新参数键时按"沿用当前值"处理，**不再误报参数不一致**（checkpoint.assert_match 向前兼容）；
- 旧存档回放：`seam_metrics` 键缺失自动补齐（纯回放运行兜底回写机制自愈）；
- `truncate` 回滚同步截断新键。

---

## 二、参数速查

| 新控件 | 默认 | 建议 |
|---|---|---|
| 切镜最多丢帧 | 17 | 30fps 慢节奏可放宽到 34；0=只要切镜点信息不裁 |
| 全链丢弃预算 | 48 | 一镜到底宁少勿多：可降到 24；接受切镜叙事可放宽 |
| 自适应精修 | 开启 | 关闭后用原「精修强度」固定值（0.2–0.7） |

## 三、A/B 验证建议（用此前有问题的 3 段 r2v 存档）

1. 跑当前版（默认参数），得到存档 B；与改动前的存档 A 对比：
   `python tools/ab_report.py A目录 B目录`
2. 看三件事：光流 z 是否 < 2、丢弃帧数是否 ≤ 预算、精修行是否出现"缝差略升"WARN；
3. 糊不糊的主观验证：对比 A/B 同一接缝处截图清晰度（精修行报告里有强度值，自适应档位 0.30 的缝应明显比固定 0.45 清爽）；
4. 部署：本地「同步到ComfyUI.bat」；AutoDL 端 `git pull origin main` 后重启 ComfyUI。

## 四、路线图位置

- 批次 0（本批）：评测基线 + 止损 + 糊缝修复 ✅
- 批次 1：LTX 式时间滑窗 + 重叠条件采样 v2 + latent 统计归一化（生成核心重构，取代硬桥）
- 批次 2：双向 transition 重生成（past|transition|future 双锚 + 缝区独占噪声，forward 干预做成可开关、默认关）
- 批次 3：长记忆多锚 + 运动状态感知门控（光流 z 触发重摇）
- 批次 4（研究）：AR/KV cache——结论已沉淀：推理期跨段 KV cache 无可用开源实现，误差累积≈低频漂移，用统计归一化 + 周期性重锚近似补偿
