# 更新说明：按官方 h3-prompt-writing skill 重构提示词组装

日期：2026-08-19　　范围：`ComfyUI_H3_SeamlessChain` 主链（H3SeamlessChainSampler）提示词层

## 背景

旧版把「场景提示词 + 角色提示词 + 主提示词」三行换行拼接后直接送模型，与官方
[h3-prompt-writing](https://github.com/MiniMax-AI/MiniMax-H3/tree/main/skills/h3-prompt-writing)
规范的三字段结构完全不符，且**音频两字段（环境音/配乐）完全缺失**——插件卖点恰是
音视频无缝续拍，音频却只能靠画面文本的边角信息生成。

## 改动清单

### 后端 nodes.py

- 新增 `_compose_official()`：运行时把每段提示词组装为官方三字段结构
  `integrated_multimodal_description: [Shot 1] 场景。角色。主提示词` +
  `overall_soundscape:` + `non_diegetic_music:`；
- 新增 `_wrap_dialogue()`：中文对白「……」自动转官方 `<d>[中文] ……</d>`
  （主提示词已手写 `<d>` 时整体跳过；英文双引号是官方可见文字记法，不动）；
- **官方格式直通**：主提示词含任一官方字段标签 → 原样发送不二次包装
  （可贴官方完整格式提示词，r2v 可手写官方六段结构）；
- **i2v 首段指令行**：接「首帧图片」时首段自动前置官方 I2VA 固定指令行
  `For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.`；
- **参考头升级**：多参模式段首自动注入的参考定义从中文单行
  `参考：<Picture 1>=角色1。` 升级为官方 `subject_definitions:` 块（每资源一行）；
- 组装对**画布直连模式同样生效**（此前只有导演台 JSON 状态才走分段处理中心）；
- `[Shot N]` 前缀去重：主提示词已带镜头标签时剥掉重加，保留用户镜号；
- 环境音/配乐留空 → 整个字段省略（不写 N/A——官方 N/A = 明确请求无声，省略 = 不约束）；
- 报告行新增「官方三字段组装」与「对白转换计数」。

### 前端 web/h3_director.js

- 段卡片「分段处理」折叠区从 场景/角色 两框扩为 **场景 / 角色 / 环境音 / 配乐** 四框
  （旧 JSON 无新键自动按空处理，向后兼容）；
- 全部 placeholder 改为官方写法引导（场景=风格+环境+光照；角色=外观+服饰+位置；
  环境音=ambient+动作音；配乐=乐器+节奏+动态；主提示词=时间线+对白格式+官方运镜词汇）；
- 引用语模板库更新为官方职责描述措辞，新增「说话人」模板（`(S1)` + 「」格式）。

### 测试 / 文档

- 新增 `tests/test_prompt_compose.py`（直通 / 三字段 / 留空省略 / 对白转换 /
  Shot 前缀 / i2v 指令行 / 旧 JSON 兼容）；
- `test_assets_kinds.py` / `test_official_params.py` 参考头断言更新为新格式；
- README 新增「提示词写法（官方 H3 结构，自动组装）」章节（三字段速查、运镜词汇表、
  对白/说话人记法、段内多镜记法、直通规则、禁则）。

## ⚠️ 升级必读：旧项目会整链重做

提示词哈希在**组装之后**计算（维持既有设计），组装格式变化 = 每段哈希变化 =
改动前生成的 v3 项目续跑时**自动从段 1 重做整链**。这是正确语义：发给模型的
条件文本确实变了，沿用旧段会新旧格式混链。如果旧项目舍不得：跑之前备份
`output/h3_projects/<项目名>/` 即可（或干脆用旧版本跑完再升级）。

## 部署与验证

1. 运行项目根目录「同步到ComfyUI.bat」→ **重启 ComfyUI** → 浏览器 Ctrl+F5 强刷；
2. 导演台段卡片展开「分段处理」→ 应看到 场景 / 角色 / 环境音 / 配乐 四个输入框；
3. 四框 + 主提示词各填内容（主提示词里写一句对白「……」）→ 跑一段 →
   - ComfyUI 控制台报告出现「分段处理：1/N 段含场景/角色/声音提示词（官方三字段组装）」
     和「对白格式：1 处「」已转 <d>[中文]」；
   - `output/h3_projects/<项目>/manifest.json` 的 `prompts[0]` 应以
     `integrated_multimodal_description: [Shot 1]` 开头，含
     `overall_soundscape:` / `non_diegetic_music:` 行，对白为 `<d>[中文] ……</d>`；
4. i2v（首帧图片）模式跑首段 → prompts[0] 首行应为 `For the target video, at 0.00 seconds...`；
5. 多参模式段未显式写 `<Picture k>` → prompts 首块应为 `subject_definitions:`；
6. 贴一段官方完整格式提示词 → 原样发送（不出现双重包装）。
