# 更新说明：前端显示修复（机翻免疫 + 编辑即刷新 + 目标画布口径）

> 2026-08-23。修三个老毛病：界面文字被浏览器整页机翻改写（2D→二维、simple→简单的、
> 整句重写）、改设置后徽章/面板不动、二采目标画布数字与主面板徽章打架。

## 1. 免疫浏览器自动翻译（乱文根因）

**现象**：整套界面文字"翻译错乱"——「网络架构」显示"二维/3D"、调度器值 "simple" 显示成
"简单的"、徽章文案被整句改写。源码与服务器吐出的字节均正确（已核对：无任何 git 版本
含这些错字），乱在浏览器层：整页自动翻译会把中英混排文案全面改写。

**修复**：导演台根节点、侧栏迷你卡、悬浮球全部标注 `translate="no"`（随 DOM 继承，
整棵子树免翻译），同时加 `notranslate` class 兜底。浏览器侧仍建议对该站点关闭整页翻译。

## 2. 二采/链参数「编辑即刷新」

**病根**：数值框/下拉以回车提交（onchange）后焦点仍在区内控件上，updateDesk 的焦点
守卫会跳过重渲，且此后没有事件再触发刷新——目标画布徽章、换算徽章、「已开启」角标
一直停留旧值，看起来就是"改了没反应、数字不对"。

**修复**：移植实验区 repaintExperiments 的局部重渲机制到二采区与链参数区：

- `repaintUpscale()`：改倍率/模式/架构/精度、段落卡勾选 → 当场重建二采面板；
  mf/state/模型列表沿用最近一次全局刷新缓存（desk.lastData），零网络往返。
- `repaintParams()` + `repaintAfterWidget()`：画幅四件（宽高比/百万像素/宽/高）变更 →
  重建链参数区（换算徽章、自定义宽高切换）并联动二采区目标画布。
- 两者的 dataset.sig 与 updateDesk 同公式同步，紧随的全局刷新不会重复重建。

## 3. 目标画布换算口径统一

**病根**：`upTargetCanvas` 只读「宽度/高度」控件，而非"自定义"画幅下这两个控件是旧
残留（实际画幅由 宽高比×百万像素 在后端换算）——二采徽章因此与主面板换算徽章打架。

**修复**：与后端 `_resolve_canvas` / 链参数徽章同源——非「自定义」按 宽高比×百万像素
经 `resolveCanvas` 换算，「自定义」才读宽高控件；32 倍数对齐逻辑不变。

## 4. 同步脚本指向真正运行的目录

**病根**：`sync_to_comfyui.ps1` 一直同步到安装目录的 custom_nodes，而 ComfyUI 实际以
`--base-directory D:\Comfy-Desktop\ComfyUI-Shared` 启动，custom_nodes 从那里加载——
同步从未到达运行实例。

**修复**：主目标改为 `D:\Comfy-Desktop\ComfyUI-Shared\custom_nodes\...`，原安装目录
保留为镜像双写。同步后需重启 ComfyUI 或浏览器强刷（Ctrl+F5）。

## 涉及文件

- `web/h3_director.js`：translate 标注、repaintUpscale/repaintParams/repaintAfterWidget、
  upTargetCanvas 口径、setUpscaleField/toggleUpscaleInclude/renderWidgetField 挂接。
- `tests/_js_harness.js`：目标画布新口径用例（AR×MP 换算 / 自定义宽高 / 过期残留不作数）。
- `sync_to_comfyui.ps1`：双目标同步。

## 5. 入口三重保底（AutoDL 新前端标签点开空白）

> 2026-08-23 追加。AutoDL 上 ComfyUI 0.33.0 / 前端 1.49.6：侧栏「长片导演台」标签
> 注册成功（图标标题都在），但点开后面板空白——前端未调用 custom 标签的 render 回调，
> 迷你卡（含「打开长片导演台」按钮）从未挂载。本地桌面版前端无此问题，故"本地好的、
> 云上没有"。

**修复（三层，不依赖任何单一前端机制）**：

1. **悬浮球常驻**：`mountFallbackFab` 不再只作旧前端兜底，而是始终挂载——悬浮球
   直接 append 到 `document.body`，与侧栏机制完全无关；侧栏标签内容首次渲染成功后
   `removeFab()` 自动撤下（标签好用时看不到球，标签坏时球一直在）。
2. **占位卡先行**：`mountSidebar` 在 `refresh()` 之前先 `renderMini` 一张空数据占位卡
   （空值路径全防御），render 被调用的瞬间就有完整入口按钮，不等待任何网络请求。
3. **刷新失败保底卡**：`refresh()` 出错时渲染 `renderMiniFallback`（报错信息 + 打开
   按钮），标签永不空白。

另加版本标记：控制台过滤 `[h3-director]` 可见 `loaded 0b860b8+entry-guard`，
用于确认浏览器跑的是新 JS 还是缓存旧版（git pull 后必须 Ctrl+F5 强刷）。
