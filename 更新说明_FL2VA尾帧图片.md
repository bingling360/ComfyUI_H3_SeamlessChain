# 更新说明：FL2VA 官方尾帧图片 + 「尾帧锚定」改名「每段尾帧锚定」

> 2026-08-23。补上由来已久的概念空缺：官方 FL2VA 的"尾帧"（剧情终点）此前从未暴露——
> 末帧 keyframe 通道被"尾帧锚定"（身份防漂移）独占。现在两者并存且正名。

## 背景

代码里有三个"尾帧"，语义截然不同：

1. **段间尾帧桥**：上段尾 latent 注入下段头部（续拍衔接，独立镜头段屏蔽）；
2. **每段尾帧锚定**（原「尾帧锚定」）：一张身份锚点图注入**每段**末帧 keyframe，
   全链恒定，防主体漂移，有锚定加噪/递减锚定软硬控制；
3. **官方尾帧参考（FL2VA end frame）**：剧情终点画面——模型生成"走到这张图"的过程。

机制上 2/3 走同一条官方 `minimax_keyframes` 末帧通道，但 3 从未暴露给用户。

## 新增：尾帧图片（FL2VA 官方首尾帧）

- **输入**：主节点新输入槽 `尾帧图片`（槽位 5，紧跟首帧图片）；导演台「首帧视频」模式
  素材区新增上传卡片（`ds.end_frame`）；画布镜像节点「目标尾帧图」（模板已预连线）。
- **语义**：整链**最后一段**的末帧 keyframe＝剧情终点。首帧开场 → 尾帧收尾，
  即官方 FL2VA 在链上的推广；只接尾帧不接首帧＝L2VA（报告行单独标注）。
- **末段官方指令行**：自动前置（镜像 I2VA 首段行句式，时间点=末段时长，
  Picture 编号按首帧是否在位取 1/2）；已是官方格式的段不注入。
- **与每段尾帧锚定的关系**：末段同一位置只有一个锚——设了尾帧图片，末段不再叠加
  每段尾帧锚定（其余段照常锚定）；报告行明示。
- **约束**：仅「首帧视频」模式（运行期硬校验，文生/多参接了直接报错）；
  切模式时 JSON 层自动清空。
- **写法**（skill 已同步）：末段提示词写"如何走到这个画面"的过程，
  不复述尾帧图内静态内容。

## 改名：尾帧锚定 → 每段尾帧锚定

输入槽/导演台卡片/工具提示/断链提示/报告行全部更名（`ds.last_frame` JSON 键不变，
旧项目存档不受影响）。⚠️ 旧工作流若在画布上手工连过「尾帧锚定」输入需重连一次
（配套模板已改为新名并预连线；导演台上传路径不受影响）。

## 槽位变更（配套模板同步改号）

```
4 首帧图片  5 尾帧图片(新)  6 每段尾帧锚定  7 起始视频  8 起始视频音轨
9..11 提示词_0..2  12..20 参考图片  21..23 参考视频  24..26 视频音轨  27..29 参考音频
```

模板新增隐藏节点「目标尾帧图」（id 6，槽位 5 预连线）。

## 验证

1. `python -m pytest tests/ -q`：162 passed（结构测试含新输入名）；
2. `node tests/_js_harness.js`：全过（模板槽位顺序/预连线断言已更新，
   含「目标尾帧图→槽位5」「尾帧图→槽位6」）；
3. `node tools/check_skill_prompt_format.js`：全过（skill 模式表/互斥表/机制节已同步）。
