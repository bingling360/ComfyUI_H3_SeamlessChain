# 更新说明：接缝处理统一后端

把「接缝处理 / 锚定加噪 / 递减锚定 / 自适应精修 / 精修强度 / 精修窗口 / 混合帧数」7 个独立旋钮统一收拢为一个「接缝处理」下拉——这些方案自由组合根本不靠谱，容易配出伤画质的组合，是接缝发糊的重要来源。

---

## 一、改了什么

### 「接缝处理」升级为统一入口（refine.resolve_profile）

| 档位 | 强度分档（好/中/坏缝） | 窗口 | 羽化 | 锚定加噪 / 递减锚定 |
|---|---|---|---|---|
| **标准**（新默认） | 0.30 / 0.45 / 0.55 | 39 帧 | 6 帧 | **一律关闭** |
| **轻量** | 0.22 / 0.30 / 0.40 | 22 帧 | 4 帧 | **一律关闭** |
| **强力** | 0.40 / 0.55 / 0.65 | 39 帧 | 8 帧 | **一律关闭** |
| **自定义** | 读细粒度控件 | 控件 | 控件 | 读控件（实验用） |
| 潜空间精修 | 旧值兼容（=自定义档透传） | 控件 | 控件 | 读控件 |
| smoothstep像素混合 | 旧值兼容（纯像素） | — | 控件 | 读控件 |

核心决策：

- **预设档内部固化全部参数**——选「标准」一个下拉就够，不再需要理解 7 个旋钮怎么配；
- **锚定加噪与递减锚定在预设档一律关闭**：实测这两项对画质是净伤害（锚定偏软 → 细节变糊、段首方差增大），只保留在「自定义」档供 A/B 实验；
- 保留全部精修机制升级：双端锚定（缝前侧整段 + 本段侧末端 5 帧）、自适应强度分档、精修后缝差略升时保留结果不回退像素混合；
- **窗口不足完全跳过该缝**（此前会回退像素混合——加权平均本身就是糊感来源）；仅精修抛异常时才像素混合兜底。

### 旧工作流兼容（零破坏）

- 细粒度控件**不删、不移、不插**（widgets_values 按控件位填充，动了会错位）——它们仍在原位，只是仅在「自定义/旧值档」时生效，tooltip 已注明；
- 旧值「潜空间精修 / smoothstep像素混合 / 关闭」保留在选项里且**行为与旧版完全一致**（透传控件）——旧工作流加载后跑出的结果不变；
- 预设档之间生成期参数（加噪=0、递减=0）相同，只有后处理差异——**档位互切换可无缝续跑旧存档**，不触发参数不一致报错。

### 代码结构

- `refine.py`：新增 `SEAM_PROFILES`（三档预设）+ `resolve_profile()`（统一入口：模式+控件值 → 执行参数 dict）；`adaptive_strength()` 支持分档表参数；
- `nodes.py`：参数解析区一次 resolve，生成期（aug/fade）与后处理（精修/羽化/像素混合）全部从 profile 取值；报告开头一行注明生效档位与固化参数；
- manifest `seam_refine` 键记录规范化档位与分档表（A/B 对比可读）。

## 二、怎么用

- **正常出片**：接缝处理=标准（默认），其他都不用动；
- 缝都不太坏、想最大限度保细节：轻量；坏缝多（缝差普遍 >0.08）：强力；
- **做实验**（对比加噪/递减/固定强度效果）：接缝处理=自定义，再调下方细粒度控件——配合 `tools/ab_report.py` 每轮只改一个变量。

## 三、验证

- 12 个测试文件全绿（新增 resolve_profile 预设固化/自定义透传/旧值映射/分档表 4 组测试）；
- 部署：本地「同步到ComfyUI.bat」→ 重启 ComfyUI；AutoDL `git pull origin main` 后重启；
- 建议验证路径：用之前糊缝明显的存档，接缝处理分别跑「轻量」和「标准」，对比同缝清晰度（轻量档强度更低，应当明显更清爽）；`python tools/ab_report.py <旧存档> <新存档>` 看 z 指标。
