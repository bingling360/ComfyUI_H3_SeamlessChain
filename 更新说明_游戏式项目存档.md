# 更新说明：游戏式项目存档系统

把旧「断点续拍存档系统」整个重构为**游戏存读档式项目系统**：一个项目一个文件夹，视频、提示词、成片、续拍 latent 全在一起；导演台打开即列出全部项目，随时读档续拍、一键删除。同时重写 HTTP 路由层，根治删除按钮 404/405 的老毛病。

---

## 一、为什么要重构

旧系统的四个硬伤：

1. **存储分散**：一个项目的内容散在 `output/checkpoints/<名>/`（latent+manifest）、`output/h3_auto/<名>/`（分段+成片副本）、`output/<prefix>/`（成片历史）三处，资源管理器里找不到一套完整东西；
2. **项目列表失灵（"指向没用"）**：前端读一个根本不存在的 `h3chain_index.json`，降级到浏览器 localStorage——列表里只有名字没有进度，点切换也只改一个字符串，不载入项目内容；
3. **删除 404/405**：只有 2 条 POST 路由，注册在 ComfyUI Desktop 环境经常因加载时序失败，前端点删除直接弹"路由未注册"；
4. **难用**：没有封面、没有项目名、没有"从某段继续"的直观入口。

## 二、新系统长什么样

```
output/h3_projects/
  h3chain_state.json              # 当前链指针
  <项目名>/
    manifest.json                 # 提示词/参数/进度/成片清单（title/created_at/updated_at/finals）
    seg_001.mp4, seg_002.mp4 ...  # 每段视频（资源管理器直接可看）
    thumb_001.png ...             # 段缩略图（= 项目封面）
    final_20260819_120000.mp4     # 成片（自动保存直接落这里）
    keyframes/                    # 分镜模式关键帧
    seg_001.pt ...                # 续拍 latent（约 5MB/段，前端不显示）
```

导演台左栏「项目存档」= 存档管理器：

- **项目列表**：后端实时扫描磁盘（不信任何索引文件或 localStorage，永不失真），每行显示封面缩略图、项目名、`done/total 段`、成片数、更新时间，按最近更新排序；
- **新建即落盘**：点「＋ 新建项目」确定时，后端当场建文件夹并写入 0 段初始 manifest（游戏存档槽语义——此前文件夹要等首次运行才建、清单要等首段采样完才写，点完新建磁盘上什么都没有）；首跑时空 `params` 按「沿用当前值」放行，跑完由真实参数覆写，title/created_at 全程保留；「存档目录」留空（指纹自动命名）的项目仍走首跑惰性创建；
- **读档**：点项目 → 切「存档目录」+ 自动载入该项目提示词进导演台；「⚙ 套用参数到画布」把该项目的共享参数（画幅/每段时长/引导帧数/步数/采样器）一键回填控件；
- **从某段继续**：已完成段卡片上的「▶ 从这段继续」——保留 1..N 段，从第 N+1 段连续生成到链尾（N 之后有旧存档时先弹截断确认）；原来的「▶ 只跑这段 / 重跑这段 / 重摇此段」照旧；
- **删除项目**：🗑 = 删整个文件夹（视频/提示词/latent 全删）；删的是当前链时状态指针自动清理；文件被播放器占用时后端返回明确错误；
- **成片区**：项目文件夹内的成片（manifest.finals）排前，H3ChainSaver 保存历史排后；删项目内单个成片走新接口（不动项目其余内容）。

## 三、路由层防弹（修 404/405）

新路由 7 条（`routes.py`）：

| 路由 | 方法 | 功能 |
|---|---|---|
| `/h3chain/ping` | GET | 诊断：前端开面板时探测，失败显示横幅 |
| `/h3chain/projects` | GET | 扫描磁盘列全部项目 |
| `/h3chain/project?dir=x` | GET | 读单个项目 manifest（读档用） |
| `/h3chain/create_project` | POST | 新建项目：当场建文件夹 + 0 段初始 manifest |
| `/h3chain/delete_project` | POST | 删项目 = 删整个文件夹 |
| `/h3chain/delete_file` | POST | 删项目内单个文件（成片等） |
| `/h3chain/delete` | POST | 删 saver 输出文件（旧功能保留） |

注册三保险：① 导入期多路径回退（app.router 优先 → PromptServer.instance.routes → 构造钩子）；② **运行期兜底**——节点每次 execute 调 `ensure_registered()` 幂等重试，即使导入期全部失败，第一次排队执行后路由就位；③ 前端每次刷新都 ping 一次，不通时顶部显示诊断横幅（含应检查的控制台日志关键字），不再是空白或裸 alert。

**补丁（上线当天修复）：每条路由双前缀注册。** 首轮部署后导演台仍报「项目接口未注册」，排查实锤：ComfyUI 0.33.x 的 `PromptServer.add_routes()` 只在启动那一刻给当时已知的路由生成 `/api` 前缀副本，自定义节点加载晚于该时点；而新版前端（comfyui-frontend 1.48+）的 `api.fetchApi()` 会给所有不以 `/api` 开头的路径**强制加 `/api` 前缀**（`ComfyApi.apiURL`）。结果就是：路由挂在根路径、前端却去打 `/api/h3chain/ping` → 404 → 横幅（与缓存无关，所以强刷无效、桌面端网页端同样中招）。修法：`add_routes()` 把每条 handler 同时挂根路径与 `/api + 路径`两份（按对象身份防重复挂载），新旧前端与浏览器直访全通。

## 四、兼容与迁移

- **旧档不迁移**：旧 `output/checkpoints/`、`output/h3_auto/` 不读不写，确认无用后可整目录手动删除（用户已拍板放弃旧档）；
- schema 升 `h3seamless/ckpt-v3`，manifest 新增 `title/created_at/updated_at/finals` 顶层可选键——续跑校验只看 `params` 子字典，不受影响；
- 分镜节点「从断点导入」填旧 checkpoints 目录的**绝对路径**仍可读 latent（torch 层无 schema 校验）；
- 旧工作流照常加载，「存档目录/断点目录」控件名两写法都认。

## 五、验证

- 单测：`test_projects.py` 新建（扫描/排序/跳过坏目录/创建落盘/删除/穿越防护），`test_routes.py` 覆盖全部路由 + 双前缀 + `ensure_registered` 幂等，其余测试文件路径与字段适配，全量带 torch venv 跑全绿；
- 部署：本地「同步到ComfyUI.bat」→ **重启 ComfyUI**（控制台应出现 `[ComfyUI_H3_SeamlessChain] 路由已注册（...）` 完整 7 条路由日志）→ 浏览器 **Ctrl+F5 强刷**（旧 JS 有缓存）；
- 手动清单：
  1. 打开导演台：无红色诊断横幅，左栏出现项目列表；
  2. 新建项目 → 磁盘上 `output/h3_projects/<名>/` 立即出现（内含 0 段 manifest），列表马上能看到它；跑 2 段 → 封面/进度/时间更新，资源管理器确认文件夹内有 seg mp4 / thumb / latent；
  3. 点另一项目读档 → 提示词载入、「套用参数到画布」回填正确；
  4. 已完成段「▶ 从这段继续」→ 只重生成其后段落；
  5. 🗑 删除项目 → 文件夹整体消失、当前链指针清理、列表即时刷新；
  6. 跑完链 → 成片 `final_*.mp4` 落在项目文件夹内，成片区可见可删。
