# 改造方案：逐段即时落盘（断点续跑） + 桥帧质量门控

针对 `H3SeamlessChainSampler` 的两项稳定性改造。均保持项目铁律：**零额外依赖、不 monkey-patch、官方协议**。

---

## 0. 现状与问题

当前主循环（`nodes.py` L193-244）单次执行内完成全部段：

```
for i, prompt in enumerate(seg_prompts):
    构建 conditioning（官方节点）
    _apply_guide(cond, guide, sampled_fc)          # 注入上段尾 keyframe
    common_ksampler(seed+i)                        # 采样
    av = sampled["samples"]                        # AV latent
    decode -> 裁头 ctx 帧 -> 累积到 all_frames/all_wav
    guide = _tail_keyframe(av, ctx, ...)           # latent 直切尾部
节点结束后才返回输出；分段保存支路也只在节点完成后生效
```

两个结构性风险：

- **风险 1（工程）**：N 段长片在第 k 段崩溃（OOM / 中断 / 断电），前 k-1 段的计算全部作废，只能整链重跑。
- **风险 2（质量）**：`_tail_keyframe` 固定取尾部 `ctx` 帧，不做质量检查。若尾帧运动模糊 / 崩坏，会通过锚定桥**忠实传播**到后续所有段——桥越稳，坏得越彻底。

---

## 1. 方案 A：逐段即时落盘与断点续跑

### 1.1 设计原则

1. **断点存 latent，不存像素**。采样输出 `av`（video_t + audio_t）约 5 MB/段（864×480、146 帧、fp16 估算），而解码后像素约 300 MB/段。续跑只需：各段 latent（重解码得到输出）+ 末段尾切片（构造下段 guide）。VAE 解码秒级，相对采样可忽略。
2. **原子写**。先写 `.tmp` 再 `os.replace`，manifest 最后更新——任何时刻崩溃都留下一个完整可续的状态。
3. **确定性续跑**。同参数下，从断点继续生成的结果与一次跑完**完全一致**（每段种子 = `种子 + i`，无隐藏状态）。
4. **不改变输出契约**。六个输出（图像/音频/帧率/报告/分段图像/分段音频）语义不变；`.pt` 断点是副产物，最终成片仍由官方 Create Video → Save Video 输出。

### 1.2 断点目录与文件

```
output/checkpoints/h3chain_{seed}_{W}x{H}_{length}f_ctx{ctx}_{8位哈希}/
├── manifest.json        # 参数指纹 + 完成进度（原子写）
├── seg_000.pt           # 段 0 的 AV latent（video_t, audio_t, sample_rate）
├── seg_001.pt
└── ...
```

`manifest.json`：

```json
{
  "schema": "h3seamless/ckpt-v1",
  "done": 3,
  "params": {
    "prompts": ["...", "...", "..."],
    "width": 864, "height": 480, "length": 124, "ctx": 22,
    "seed": 42, "steps": 25, "cfg": 1.0,
    "sampler": "res_multistep", "scheduler": "simple",
    "chain": "t2v", "keyframe_audio": true,
    "gate": {"mode": "自动回退", "trims": [0, 17, 0]}
  }
}
```

目录名中的 8 位哈希取自 `params` 全量 JSON（提示词变化即换目录），避免误续。

### 1.3 新增控件（2 个）

| 控件 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `断点续拍` | Combo `["自动续跑", "关闭"]` | `关闭` | 自动续跑：目录存在且校验通过则跳过已完成段 |
| `断点目录` | String | `""` | 空 = 按上述规则自动命名；非空 = 固定目录（便于手动管理） |

### 1.4 新模块 `checkpoint.py`（关键实现）

```python
"""断点读写：manifest 校验 + 段 latent 原子落盘。"""
import json, os, tempfile
import torch
from folder_paths import get_output_directory

def ckpt_dir(params: dict, custom: str) -> str:
    if custom:
        root = os.path.join(get_output_directory(), "checkpoints", custom)
    else:
        h = abs(hash(json.dumps(params["public"], ensure_ascii=False, sort_keys=True)))
        name = f"h3chain_{params['seed']}_{params['width']}x{params['height']}_{params['length']}f_ctx{params['ctx']}_{h % 10**8:08d}"
        root = os.path.join(get_output_directory(), "checkpoints", name)
    os.makedirs(root, exist_ok=True)
    return root

def _atomic_write(path: str, data: bytes):
    fd, tmp = tempfile.mkstemp(dir=os.path.dirname(path), suffix=".tmp")
    try:
        with os.fdopen(fd, "wb") as f:
            f.write(data)
        os.replace(tmp, path)
    finally:
        if os.path.exists(tmp):
            os.remove(tmp)

def save_manifest(root, manifest: dict):
    _atomic_write(os.path.join(root, "manifest.json"),
                  json.dumps(manifest, ensure_ascii=False, indent=1).encode("utf-8"))

def load_manifest(root) -> dict | None:
    p = os.path.join(root, "manifest.json")
    if not os.path.exists(p):
        return None
    with open(p, "r", encoding="utf-8") as f:
        return json.load(f)

def save_segment(root, idx: int, av):
    video_t, audio_t = av.unbind()
    payload = {"video": video_t.cpu().clone(), "audio": audio_t.cpu().clone()}
    buf = tempfile.SpooledTemporaryFile(max_size=64 * 1024 * 1024)
    torch.save(payload, buf)
    buf.seek(0)
    _atomic_write(os.path.join(root, f"seg_{idx:03d}.pt"), buf.read())
    buf.close()

def load_segment(root, idx: int):
    with open(os.path.join(root, f"seg_{idx:03d}.pt"), "rb") as f:
        p = torch.load(f, map_location="cpu", weights_only=True)
    return p["video"], p["audio"]   # 调用方按需 .to(device)
```

### 1.5 主循环改造（伪代码，diff 视角）

```python
# execute() 开头，构建 params 指纹
params = {"prompts": seg_prompts, "width": width, ..., "gate": gate_params}
resume = 断点续拍 == "自动续跑"
root = ckpt_dir(params, 断点目录) if resume else None
manifest = load_manifest(root) if resume else None
if manifest is not None:
    _assert_match(manifest["params"], params)        # 见 1.6
    seed = manifest["params"]["seed"]                # 断点种子优先，见 1.7
    report.append(f"断点续跑：已完成 {manifest['done']}/{len(seg_prompts)} 段，目录 {root}")
done = manifest["done"] if manifest else 0

for i, prompt in enumerate(seg_prompts):
    if i < done:
        video_t, audio_t = load_segment(root, i)     # 重解码复用下方同一代码路径
        av = ...  # 组装回 sampled 结构（或直接跳过采样，单独走 decode 分支）
    else:
        cond, latent = 官方节点构建(...); cond = _apply_guide(...)
        av = common_ksampler(...)[0]["samples"]
        if resume:
            save_segment(root, i, av)
            save_manifest(root, {**manifest, "done": i + 1, "params": params})

    # ↓ 以下 decode/裁剪/guide 构造对「新采样」与「断点重放」完全同一条路径
    video_t, audio_t = av.unbind()
    frames = video_vae.decode(video_t); wav, sr = _decode_audio(audio_vae, audio_t)
    ...裁剪、累积、报告...
    guide = _tail_keyframe(av, ctx, KEYFRAME_AUDIO_SUPPORTED)

if resume and done == len(seg_prompts):
    report.append(f"全部段来自断点，未触发采样；断点目录可删除：{root}")
```

要点：

- **断点重放走与正常段相同的 decode/裁剪代码**，保证逐字节一致，不另写一套。
- manifest 的 `done` 在该段 `.pt` 落盘成功后更新；两者之间崩溃 → 该段重采样（无损，只是浪费一次）。
- 报告末尾附断点目录路径，方便用户跑完后清理。

### 1.6 续跑校验规则（`_assert_match`）

| 字段不一致 | 处理 |
|---|---|
| prompts / 宽高 / 每段帧数 / 引导帧数 / 步数 / CFG / 采样器 / 调度器 / 链路 | `ValueError`，提示「参数已变化：请新开断点目录或删除旧目录」 |
| seed | 以 manifest 为准，报告中注明「控件种子 Y 已被忽略，使用断点种子 X」 |
| keyframe_audio / 插件 gate trims | 以 manifest 为准并注明（ComfyUI 版本或插件行为变化的场景） |
| manifest 有、`.pt` 缺失（段文件被手删） | 从缺失段起重采样 |

### 1.7 边界与风险

- **`control_after_generate` 种子自增陷阱**：当前种子控件开了 `control_after_generate=True`，ComfyUI 每次运行后自动 +1。断点续跑**必须**以 manifest 记录的种子为准（1.6 已处理），否则续跑结果与首跑不一致。文档（README）需同步注明。
- **磁盘占用**：约 5 MB/段，64 段满配 < 400 MB，可接受；README 提示定期清理 `output/checkpoints/`。
- **同目录并发**：同一断点目录同时跑两个队列属误用，manifest 的 `done` 只增不减，最坏情况是重复采样同一段，不会产生错误结果；V1 不加锁，README 说明即可。
- **模型/VAE 文件变更**：不校验（无法可靠取指纹）。decode 结果可能轻微不同，属可接受漂移，报告注明。

---

## 2. 方案 B：桥帧质量门控

### 2.1 打分算法（借鉴总控台 `LtoJ_HighMotionFrameSelector` 的思路，latent 域适配）

对**解码后的尾部像素帧**逐帧打分（解码产物现成，零额外开销）：

```
清晰度 s = Laplacian 方差（灰度、最长边缩到 ≤512 计算）
曝光惩罚 c = 灰度 <0.02 或 >0.98 的像素占比
总分 = log1p(s × 1000) + 对比度 × 2.0 − c × 2.5      # 与总控台同形，便于标定
```

### 2.2 关键约束：回退粒度必须踩 latent 网格

guide 切片是 latent token 级（`video_t[:, :, -vt:]`），token→帧映射为 `[1,4,4,4,4]` 循环（`grid.latent_t_to_frames`）。**回退 5 个 token = 精确 17 帧**，取 `δ ∈ {0, 5, 10}` token，即 0 / 17 / 34 帧三档——天然落在 17k+5 网格上，不需要新对齐逻辑。

回退后三处联动（全部确定重放，与方案 A 兼容）：

| 量 | 原值 | 回退 δ token（=17k 帧）后 |
|---|---|---|
| guide 视频切片 | `video_t[..., -vt:]` | `video_t[..., -vt-δ : -δ]` |
| guide 音频切片 | `audio[..., -at:]` | `audio[..., -at-aδ : -aδ]`，`aδ = round(17k × 5/3)` |
| 本段可见输出 | `frames[skip : skip+L]` | `frames[skip : skip+L−17k]`，音频同步按比例 |

时间线仍然连续：段 i 输出到 `L−17k` 帧处，段 i+1 的桥正是从那里接续——等价于把段 i 的坏尾剪掉，从更早的好帧续拍。下段 `skip_f = ctx` 不变。

### 2.3 新增控件（3 个）与三档模式

| 控件 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `桥帧门控` | Combo `["关闭", "标注", "自动回退"]` | `标注` | 见下 |
| `清晰度阈值` | Float 0–100 | `30` | 尾帧总分低于阈值视为「坏尾」 |
| `回退上限` | Int，step 17 | `34` | 最多回退帧数，自动向下取 17 的倍数 |

- **关闭**：现行为，零开销。
- **标注**：只打分写报告（`段2 尾帧总分 21.3 ↓ 低于阈值 30 [模糊尾帧]`），不改行为。**默认档**，先积累标定数据。
- **自动回退**：尾帧不达标 → 依序尝试 δ=17、34 帧，取首个达标位置构造 guide 并裁剪本段输出；全部不达标 → 保持原尾部 + 报告醒目警告（此时建议人工检查该段，而不是硬剪 34 帧了事）。

### 2.4 新模块 `qc.py`（关键实现）

```python
"""桥帧质量打分：纯 torch 数学，不依赖 ComfyUI 运行环境，可独立单测。"""
import torch
import torch.nn.functional as F

def frame_scores(frames: torch.Tensor) -> dict:
    """frames: [N,H,W,3] float。返回每帧 {sharp, contrast, clipped, score}。"""
    rgb = frames[..., :3].float().movedim(-1, 1)                 # [N,3,H,W]
    gray = rgb[:, 0] * 0.299 + rgb[:, 1] * 0.587 + rgb[:, 2] * 0.114
    h, w = gray.shape[-2:]
    scale = 512 / max(h, w)
    if scale < 1:
        gray = F.interpolate(gray.unsqueeze(1), scale_factor=scale,
                             mode="bilinear", align_corners=False).squeeze(1)
    k = gray.new_tensor([[0,1,0],[1,-4,1],[0,1,0]]).view(1,1,3,3)
    lap = F.conv2d(gray.unsqueeze(1), k, padding=1).squeeze(1)
    sharp = lap.flatten(1).var(dim=1, unbiased=False)
    contrast = gray.flatten(1).std(dim=1, unbiased=False)
    clipped = ((gray < 0.02) | (gray > 0.98)).float().flatten(1).mean(dim=1)
    score = torch.log1p(sharp * 1000) + contrast * 2.0 - clipped * 2.5
    return {"sharp": sharp, "contrast": contrast,
            "clipped": clipped, "score": score}

def pick_backtrack(score: torch.Tensor, ctx: int, limit: int,
                   threshold: float) -> tuple[int, float]:
    """在尾部窗口内选回退档位。返回 (回退帧数, 该位置得分)。"""
    steps = sorted({0, 17, 34} & set(range(0, limit + 1)))       # 网格档位
    for back in steps:
        idx = len(score) - 1 - back
        if idx < 0:
            break
        if float(score[idx]) >= threshold:
            return back, float(score[idx])
    return 0, float(score[-1])                                   # 全不达标：不硬剪
```

`_tail_keyframe` 增加偏移参数（`nodes.py` 内 3 行改动）：

```python
def _tail_keyframe(av_samples, ctx_frames, with_audio, back_tokens=0, back_audio=0):
    video, audio = av_samples.unbind()
    vt = video_latent_t(ctx_frames)
    end = video.shape[2] - back_tokens
    kf = {"resolved_frame_index": 0, "latent": video[:, :, end - vt:end].clone()}
    if with_audio and audio is not None:
        at = audio_tokens_for_frames(ctx_frames)
        aend = audio.shape[-1] - back_audio
        if 0 < at <= aend:
            kf["audio_latent"] = audio[..., aend - at:aend].clone()
    return kf
```

### 2.5 主循环接入点（方案 A 的伪代码中「decode 之后、guide 构造之前」）

```python
frames_full = video_vae.decode(video_t).reshape(...)              # 现有代码，裁剪前
if 桥帧门控 != "关闭" and i + 1 < len(seg_prompts):
    tail = frames_full[-(ctx + 34):]                              # 覆盖全部回退档位
    s = frame_scores(tail.cpu())["score"]
    if 桥帧门控 == "标注":
        report.append(f"段{i+1} 尾帧总分 {float(s[-1]):.1f}"
                      + (" ↓ 低于阈值" if float(s[-1]) < 清晰度阈值 else ""))
    else:  # 自动回退
        back, hit = pick_backtrack(s, ctx, 回退上限, 清晰度阈值)
        back_tokens = back // 17 * 5
        guide = _tail_keyframe(av, ctx, KEYFRAME_AUDIO_SUPPORTED,
                               back_tokens=back_tokens,
                               back_audio=round(back * 5.0 / 3.0))
        if back:
            length_i = length - back                              # 本段可见帧数
            report.append(f"段{i+1} 尾帧低质，回退 {back} 帧续拍（回退点得分 {hit:.1f}）")
        # length_i 用于替换下方 frames/wav 的裁剪长度；trims 记入 manifest
```

### 2.6 边界与风险

- **阈值需要标定**：先跑「标注」档积累真实分布（报告里有原始分），再定阈值；`log1p(方差)` 对画风敏感，写实 vs 动漫的合理阈值不同。
- **每段可见帧数不再恒定**：回退段少 17/34 帧。总时长 = Σ各段实际帧数，报告末尾给出合计；分段保存支路长度同样随之变化（语义自洽）。
- **回退上限 34 帧的安全垫**：段长最短 124 帧训练下限，回退后仍 ≥ 90 帧，远离边界。
- **与 r2v 链的既有边界**：回退只改切片位置，不触碰 conditioning 协议，不影响「guide 与参考素材冲突」的现状（该问题仍由 PR #15439 解决）。
- **音频对齐取整**：`round(back × 5/3)` 有 ±1 token 取整误差，与现有音频锚定的近似语义一致，无新增风险。

---

## 3. 实施顺序与工作量

| 阶段 | 内容 | 改动文件 | 预估 |
|---|---|---|---|
| A1 | `checkpoint.py` + 单测（原子写、指纹、断点读写） | 新文件、`tests/test_checkpoint.py` | ~120 行 |
| A2 | 主循环接入续跑 + 2 控件 + README | `nodes.py` | ~60 行 |
| B1 | `qc.py` + 单测（打分纯函数、档位选择） | 新文件、`tests/test_qc.py` | ~90 行 |
| B2 | `_tail_keyframe` 偏移 + 主循环门控接线 + 3 控件 | `nodes.py` | ~50 行 |

建议顺序 A1→A2→B1→B2：先解决「跑废全重来」的工程风险，再做质量门控；B 的 trims 进 manifest 依赖 A 先就位。

## 4. 测试清单

**方案 A**
- [ ] 3 段链在第 2 段抛异常（临时注入）→ 重跑自动续跑，输出与一次跑完逐帧一致（`torch.equal`）
- [ ] 续跑时改提示词 → 报错信息正确
- [ ] 续跑时种子控件被自增 → 实际使用 manifest 种子，报告注明
- [ ] 删掉中间一个 `.pt` → 从该段重采样
- [ ] manifest done=3 且全部完成 → 重跑零采样、报告提示可清理

**方案 B**
- [ ] `frame_scores`：对合成图（清晰块 / 高斯模糊 / 过曝）打分单调性正确
- [ ] `pick_backtrack`：尾帧坏、回退 17 帧处好 → 选 17；全坏 → 返回 0 不硬剪
- [ ] 回退 34 帧后成片总时长 = Σ(段长 − 回退)，接缝无跳变（人工抽检 + 帧差分）
- [ ] 回退段与正常段混排的断点续跑（A×B 交互：trims 重放一致）
- [ ] r2v 链上回退门控与参考素材共存不报错
