# 更新说明：分镜模式 + 锚定加噪

提交 `64983dd` · 双范式落地：续拍链（一镜到底）+ 分镜链（长叙事）

---

## 一、本轮更新了什么

### 1. 新增节点：H3StoryboardChain（分镜链）

即梦「智能多帧」同范式的本地实现。N 张关键帧图把长片切成 N-1 段，每段**首尾双向锚定**独立生成，误差不沿链传播。

- **首帧锚定**：走官方 i2v 路径（`first_frame=关键帧 i`，质量最好的训练路径）
- **尾帧锚定**：自注入官方 Add Guide keyframe 协议——关键帧 i+1 堆叠 5 帧编码为 2 token latent，钉 `resolved_frame_index=段尾-5`（天然踩 17k+5 网格 token 边界）
- **关键帧来源**：可上传 `LoadImage`，也可**从续拍链断点导入**（两遍法精修）
- r2v 链（带参考素材）官方无 first_frame 接口，首尾两锚均自注入，`<Picture i>` 等参考引用与续拍节点完全一致

### 2. 续拍链增强：锚定加噪参数

新增控件「锚定加噪」（`H3SeamlessChainSampler` 节点上），缓解段首凝滞与内容重演。

- 技术路径：写入 H3 模型官方自带的 `minimax_visual_cond_noise_aug` 参数（SkyReels-V2 addnoise_condition 思路），**零 monkey-patch**
- 音频加噪自动减半（音频桥窗口短，过噪伤听感）
- **不进断点指纹**：改参数不触发重跑，可在断点续跑状态下直接调着试
- 与「接缝混合」互补：加噪管生成时起步顺滑，smoothstep 管拼接时吸收残余帧差

### 3. 链路总结报告

运行结束后「报告」输出末尾追加链路总结：全链接缝帧差、桥分、响度跳变的**平均值与最差段**，长片质量诊断一目了然。

### 4. 审片面板适配分镜模式

`web/h3chain_panel.js` 升级：

- 自动识别 `manifest.params.mode === "storyboard"` → 渲染**关键帧时间线**（点图可放大 lightbox）
- 分镜段卡片显示「转场帧差 / 尾锚达成度」替代续拍链的「接缝 / 桥分」
- 两类节点（`H3SeamlessChainSampler` / `H3StoryboardChain`）共用同一面板

### 5. checkpoint schema v3

`checkpoint.py` 升级断点引擎支持分镜模式：

- 段哈希 = 提示词 + 首尾关键帧哈希组合
- 改关键帧 i → 仅段 i-1（尾锚变了）与段 i（首帧变了）重跑，其余段沿用断点
- 关键帧副本存 `<断点目录>/keyframes/kf_NNN.png`（断点自包含，面板可显示）

### 6. 响度对齐

`qc.py` 新增 `loudness_align_head`：段首增益匹配上段尾 RMS（±6dB 钳制、1 秒内渐出）。分镜段间画面是镜头切换，但响度跳变仍会突兀；增益只作用于段首窗口、**不沿链累积**。

### 7. 新增示例工作流

- `example_workflows/h3_storyboard_base.json`：**分镜模式最小示例**——3 张关键帧图 + 2 条段提示词

---

## 二、两个节点的区别

| | 续拍链 H3SeamlessChainSampler | 分镜链 H3StoryboardChain |
|---|---|---|
| 锚定方式 | 边生成边定锚（上段尾 latent 直切钉下段头） | 先定锚后生成（关键帧图首尾双锚） |
| 段间关系 | 无缝续接（同一镜头继续） | 镜头切换 / 转场 |
| 误差传播 | 沿链传播（长链渐糊） | **不传播**（每段独立） |
| 适合 | 一镜到底、连续动作、vlog | 长叙事、多场景、有分镜脚本的片 |
| 20 段以上 | 不推荐 | 推荐用法 |
| 前期准备 | 只需写提示词 | 需要先有关键帧图 |

一句话：**拍连续动作用续拍链，讲故事用分镜链，拿不准就两遍法。**

---

## 三、怎么更新

### AutoDL（git 安装的）

```bash
cd ~/ComfyUI/custom_nodes/ComfyUI_H3_SeamlessChain
git pull origin main
```

然后重启 ComfyUI。若报本地修改冲突：`git stash && git pull origin main`。

### 本机（开发目录）

双击项目根目录的「同步到ComfyUI.bat」，会自动镜像到 `D:\Comfy-Desktop\ComfyUI-Shared\custom_nodes\ComfyUI_H3_SeamlessChain`。

### 拷贝安装的

把整个 `ComfyUI_H3_SeamlessChain` 文件夹覆盖到 `custom_nodes/` 同名文件夹，重启 ComfyUI。**两个节点在同一个文件夹里，不需要分开。**

---

## 四、续拍链用法（含新参数）

### 接线

```
UNETLoader ──┐
              ├─→ ModelSamplingMiniMaxH3 ──→ H3SeamlessChainSampler ──→ CreateVideo ──→ SaveVideo
CLIPLoader ──┤                                ↑       ↑       ↑
VAELoader ───┘                              提示词组  首帧图片  起始视频（可选）
```

- **UNET**：不接参考素材用 `fl2va`，接了参考素材用 `ref2va`
- **CLIP**：type 必须选 `minimax`（Qwen3-VL）
- 提示词组一个输入框 = 一段视频，点 + 加段
- 加分项：「分段图像 / 分段音频」再接一组 CreateVideo，每段单独存片检查

### 新参数：锚定加噪

| 值 | 效果 |
|---|---|
| 0（默认） | 不干预，锚定最强（段首容易刹车） |
| 0.1 | 微调，几乎无损 |
| **0.2** | **标准值，推荐起步** |
| 0.3+ | 干预强，段首最顺但画面细节变软 |

**什么时候开**：接缝处出现段首画面卡住不动、或模型把上段动作又演一遍时。

### 写提示词的技巧

每段提示词开头留约 2 秒「气闸期」——保持上段结尾构图，只描述微动作（呼吸、视线转移），之后再引入新内容，接缝会顺很多。

### 操作流程

1. 跑一次 → 左侧「长片审片」面板看这一段
2. 满意点「继续下一段」，不满意点「重摇此段」或改词重跑
3. 全程断点自动落盘，中途崩了重开直接续

---

## 五、分镜链用法

### 接线

```
UNETLoader ──┐
              ├─→ ModelSamplingMiniMaxH3 ──→ H3StoryboardChain ──→ CreateVideo ──→ SaveVideo
CLIPLoader ──┤                                ↑       ↑
VAELoader ───┘                              分镜图组  提示词组
```

### 步骤

1. **准备关键帧图**：AI 画图、即梦生成、视频截图都行，**建议同一尺寸**（不同尺寸会自动 cover 对齐）。N 张图定义 N-1 段。
2. **「分镜图组」**按播放顺序逐张接 `LoadImage`
3. **「提示词组」**写 N-1 条，每条描述「从关键帧 i 演到关键帧 i+1」的过程（构图变化、动作、运镜）
4. **每段帧数用 124**（约 5 秒，17k+5 网格值，尾锚恰好钉在段尾）；非网格值模型会向上对齐（如 130→141）

### 特色控件

| 控件 | 默认 | 说明 |
|---|---|---|
| 尾帧锚定 | 开启 | 关闭则退化为关键帧首帧链（每段只锚首帧、尾帧自由发挥，段间为纯转场） |
| 锚定加噪 | 0.0 | 与续拍节点同参语义，对首尾锚定帧生效 |
| 响度对齐 | 开启 | 段首增益匹配上段尾 RMS（±6dB 钳制、1 秒内渐出） |
| 关键帧来源 | 上传 | 见下方两遍法 |
| 审片模式 | 关闭 | 开「逐段确认」每次运行只生成一段，预览后再运行继续 |

### 断点与失效规则

- 改关键帧 i → 段 i-1 与段 i 自动重跑，其余段沿用断点
- 改某段提示词 → 仅该段起重跑
- 关键帧副本存 `<断点目录>/keyframes/kf_NNN.png`

---

## 六、两遍法（长片推荐）

先用续拍链快速粗剪，再导入分镜链精修：

1. **第一遍（粗剪）**：用 `H3SeamlessChainSampler` 跑一条，不用管质量，只要构图走向对，开启「断点续拍=自动续跑」
2. **第二遍（精修）**：新建 `H3StoryboardChain` 节点：
   - 「关键帧来源」切为「从断点导入」
   - 「断点导入目录」填第一遍的断点目录名
   - 插件自动把续拍链各段的段首/段尾抽出来当关键帧
   - 换精修提示词跑分镜链

N 段续拍链自动变成 N 段分镜链，兼得两种模式优点。

---

## 七、验证

- 34 项测试全过（torch venv）
- JS 语法 + 4 个示例工作流 JSON 校验通过
- 控件值与工作流 JSON 对位核验无坑

---

## 八、版本要求

- ComfyUI **≥ v0.30.0**（含官方 MiniMax H3 节点与 `comfy_api.latest`）
- 段间多帧桥与音频锚定需要 ComfyUI 含 [PR #15439](https://github.com/comfy-Org/ComfyUI/pull/15439)（2026-08-09 之后构建）。旧版会自动降级为单帧桥 + 仅视频引导，升级后自动恢复完整引导帧数，无需改参数
- 旧版 v1 断点目录不兼容本版本（报错并提示换目录名），需重新开链
