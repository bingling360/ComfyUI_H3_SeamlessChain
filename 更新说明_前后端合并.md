# 更新说明 · 前后端合并（feature/director-ui → backend）

导演台前端与统一接缝后端合为一条线：导演台的一体化控制（素材池/段级引用/官方参数）
与后端的接缝治理（统一接缝处理/接缝重摇/智能切镜/尾帧锚定/丢帧预算）现在同节点共存。

## 合并要点

### 节点参数（两分支合集，34 控件）
- 导演台侧：宽高比/百万像素（官方 Resolution Selector 同款换算）、每段时长（秒，
  17k+5 网格吸附，段级 seconds 覆盖）、生成模式（文生/首帧/多参互斥校验）、
  导演台状态（JSON：提示词/素材池/分段处理，优先于画布接线）
- 后端侧：接缝处理（一个下拉决定全部衔接参数）、精修强度/窗口、接缝重摇
  （缝差超阈值自动换种子重采，取缝差最小）、智能切镜（运动低谷自然停顿点）、
  递减锚定、切镜最多丢帧/全链丢弃预算（防"30秒删7秒"）、自适应精修（按缝差分档）、
  尾帧锚定（last_frame keyframe 身份隧道）

### 语义合并（nodes.py 7 处冲突全部手工对齐）
- 主循环保留后端的 `_decode_crop` 重摇共用重构，段长从全局 `length` 换成
  导演台的 `seg_lengths[i]`（每段独立时长与门控回退/智能切镜/网格对齐全程兼容）
- 参考素材注入换成导演台的段级压实：只把本段勾选的素材压进 conditioning
  （未勾选不进段），图/视/音三类独立编号 <Picture k>/<Video k>/<Audio j>
- 逐段哈希两侧语义叠加：改时长/改段级勾选自动从该段重做（旧存档续跑不受影响），
  后端的 seam_refine 纯后处理参数只记录不进指纹

### 前端适配（web/）
- h3_default_workflow.js：配套模板 widgets_values 升级为 35 值布局
  （接缝处理=标准 + 新增 10 个接缝控件默认值），否则载入即错位
- h3_director.js：高级设置收纳全部新接缝控件；「接缝混合」改名「接缝处理」；
  新增 25 值（导演台时代）→ 35 值旧工作流迁移（接缝混合旧值映射旧值档，
  与后端 backfill 同规则），连同已有 24 值（更老）迁移一起自动处理

### 测试基建修复
- tests/conftest.py（新增）：stub 统一安装 + 逐测试恢复——test_checkpoint/
  test_saver/test_routes 的临时环境退出时会 del sys.modules 条目，合集运行
  此前会污染后续测试
- test_node_structure._install_stubs：有真 torch（AutoDL/ComfyUI 环境）时
  不再装假 torch stub（真假以 __version__ 区分，stub 重装幂等保持对象身份）
- test_metrics/test_qc/test_refine/test_smart_cut/test_seam_doctor/test_media：
  识别假 torch stub 自动 SKIP——合集运行（pytest tests/）与单文件脚本
  （python tests/test_xxx.py）现在都是绿的

## 验证
- pytest tests/：68 passed, 4 skipped（4 个为真 torch 依赖，本机无 torch 跳过）
- node tests/_js_harness.js：all checks passed（含 25→35 迁移与模板 35 值校验）
