# 更新说明：潜空间放大二采（高清重制）

整合上游两个仓库的能力，给本插件补一条**独立的高清重制通道**——主链跑完后对任意段做「神经放大 latent → 低强度重采样补细节」，导演台有专门面板（类似高级设置）控制全部参数。主链的生成、桥接、断点、重跑语义**零改动**：二采只是对已产出视频的额外后处理。

- 放大网络：[LBH-123-AI/Comfyui_Minimax_h3_latent_Upscaler](https://github.com/LBH-123-AI/Comfyui_Minimax_h3_latent_Upscaler)（2D 残差骨干 + 纯 3D 卷积两种，网络逐行移植进 `upscale_net.py`，推理节点不移植）；
- 二采范式：[wjluoxiao/ComfyUI-JZL-MiniMax-H3](https://github.com/wjluoxiao/ComfyUI-JZL-MiniMax-H3) 的 CondSync 思路——latent 放大后用官方 `common_ksampler` 以 denoise<1 重去噪，conditioning 里的 keyframe 同步放大到目标尺寸，避免按原尺寸重编码。

---

## 一、技术机制

**单段二采流水线**（`upscale.run_pass`，主循环收尾后清扫）：

| 步骤 | 实现 | 说明 |
|---|---|---|
| 读段 | `checkpoint.load_segment(root, g)` | 基础链段 AV latent（只读，不改） |
| 神经放大 | `upscale_net.load_model` + `upscale_video` | 24 通道 latent 归一化（LATENTS_MEAN/STD）→ 网络 → 反变换；**时间维 T 绝对不变**（17k+5 token 网格硬约束），H/W × 倍率后偶数对齐（latent 偶数 = 像素 32 倍数，官方画布口径） |
| cond 构造 | 官方 `MiniMaxH3ImageToVideo / ReferenceToVideo.execute` | 按目标分辨率重构造（refs/首帧由官方节点重编码到目标尺寸）；插入视频/序章用空提示词轻精修 |
| 桥锚 CondSync | `_tail_keyframe` 尾切片 → `upscale_video` 放大 → `_apply_guide` | 上段**基础 latent** 尾切片神经放大后注入 keyframe——只依赖上段基础 latent + scale，与处理顺序/模式无关（单段可独立重做） |
| 重采样 | `common_ksampler(denoise=强度)` | 初始 latent = 放大后视频 latent + 原音频 latent；种子 = 基础段种子 + 1 派生（重放一致）；采样输出音频丢弃 |
| 解码裁剪 | 复刻主循环口径 | skip_f 裁头（引导桥重叠区）；插入/序章全量；`cond_audio_rows_guard` 保护音频行 |
| 接缝平滑 | `qc.smoothstep_blend_head` | 上段有二采产物时锚定上段高清尾帧（`uplast_*.png`）+ smoothstep 窗吸收段首偏差 |
| 落盘 | `upseg_NNN.pt / .mp4` + `upthumb/uplast` | 每段完成即原子落盘（崩溃安全）；manifest.upscale.segs 增量记录（写回前重读防竞态） |
| 高清成片 | `media.concat_av_mp4` | 全链记录齐且链完成时流式拼接 `final_up_时间戳.mp4` |

**两种网络架构**（面板可选，加载时按权重键名自动校验，不匹配明确报错）：

- **2D**（上游默认，快）：残差骨干（channels=640）+ TemporalConv 时序块，逐帧 2D 卷积 + bilinear 插值；
- **3D**（时序一致性好）：纯 3D 卷积骨干（channels=512），trilinear 插值；3D 权重严格加载（strict=True），2D 因 attn 强制关闭用 strict=False。

## 二、两种触发模式（导演台面板）

右栏 **「✦ 潜空间放大二采」** 面板（折叠式，类似高级设置；开启时自动展开 + 边框高亮）：

- **跟随生成**：每次运行后自动对**新增/失效段**二采——逐段审片时即「生成一段、二采一段」；
- **手动选择**：段落卡片出现勾选框，勾选任意**已完成段**（含插入视频段/序章——外部素材段是否二采由勾选决定）后点「开始生成」执行；
- **关闭**（默认）：不执行，已产出的 `upseg_*` 不受影响。

面板参数：放大模型（`models/latent_upscale_models/` 目录扫描下拉，`GET /h3chain/upscale_models`）、网络架构、放大倍率 1.0–4.0（**目标画布徽章**实时显示，与后端 target_hw 同口径）、二采强度 0.05–1.0（0.35–0.55 常用）、二采步数 1–100、二采 CFG（H3 常用 1.0）、精度 fp32/fp16/bf16（只影响放大网络推理，重采样仍在原精度）。

段卡片可见性：已二采段显示「二采 W×H」青色徽章；手动模式下已完成段可勾选；「↺ 重置二采」按钮清该段记录与产物（`POST /h3chain/upscale_reset`，下次二采运行时重做）。

## 三、重做规则（manifest.upscale）

**二采参数不进基础链 ckpt_params 指纹**——改参数只重做二采，不动已生成段：

| 触发 | 范围 | 机制 |
|---|---|---|
| 二采参数变（hash 变） | 模式范围内全部段 | 记录 `hash` 不匹配 → 待做 |
| 基础链从段 k 重做（truncate） | upseg ≥ k 全清 | `checkpoint.truncate` 联动删文件 + 截记录，链完成后按新 base_hash 补做 |
| 某段基础提示词/种子变 | 仅该段 | 记录 `base_hash`（prompt_hashes[g] + seeds[g]）不匹配 → 失效 |
| 产物文件丢失（mp4/last） | 该段 | `_record_valid` 校验文件在，缺即重做 |

单段二采只依赖「该段基础 latent + 上段基础 latent + 二采参数」——与处理顺序、模式、其他段是否二采无关，可独立重做；音频链状态（响度对齐尾音）无论该段是否待做都按链序推进，任意子集二采结果一致。

## 四、工程细节

- **完全独立的后处理通道**：`nodes.py` 只在主循环 + 自动保存之后调 `upscale.run_pass`，`except Exception` 兜底——任何失败（权重缺失/架构不匹配/显存不足/编码失败）降级为报告一行「二采失败：...（基础链产物不受影响）」，不丢已生成结果；节点主输出 images 保持基础链结果（高清产物在项目文件夹，避免高清全帧驻留内存）；
- **零音频回归**：音轨沿用原声（重采样输出的音频丢弃）；段首 ±6dB 钳制响度对齐复刻主循环口径（归一化排除锚定区 + skip/vis 比例裁剪）；
- **模型目录注册延迟**：`upscale_net` 的 folder_paths 注册在首次扫描时做（无 ComfyUI 环境可 import 做前向单测）；模型缓存键 `名字::arch::device::precision`（同一权重按 2D/3D 分别加载）；FP8 权重自动转 FP16；
- **权重不匹配防护**：2D 权重键带 `resizer.` 前缀、3D 权重无前缀——选错架构时 `load_model` 抛明确 ValueError（提示切换「网络架构」），不会抛晦涩的 strict 加载错误；
- **路由**：`GET /h3chain/upscale_models`（模型列表，无 torch/目录时返回空列表）、`POST /h3chain/upscale_reset`（清段记录，ValueError → 400）；每条路由照旧双前缀注册（根路径 + `/api` 副本）。

## 兼容性

- **旧存档/旧工作流**：`ds.upscale` 缺失 = 默认关闭（前端 `defaultUpscale()` 注入、后端 `parse_state` 同口径），旧项目行为逐帧不变；节点控件零改动（全部走导演台状态，不动 widgets 顺序）；
- **manifest 前向兼容**：`upscale` 键缺失按空处理；`truncate` 只在键存在且超界时截断；
- **缓存失效**：二采参数存于「导演台状态」widget 值内，改参数自动触发节点重执行（ComfyUI widget 变化即失效缓存）；且二采仅在自动存档开启时运行（彼时 `IS_CHANGED` 恒 nan 强制真执行），无需额外判定。

## 验证

- 全量测试（带 torch venv）：**18 个测试文件全绿**，其中新增与扩展——
  - 新增 `test_upscale.py`（14 项）：`parse_state` 关闭/归一化/NaN 兜底/include 解析；`params_hash` 稳定性与参数敏感（mode/include 不进指纹）；`base_hash`；`target_hw` 偶数对齐；`pending_slots` 重做判定（hash/base_hash/文件缺失/手动范围/done 截断）；`resize_latent_bilinear` 形状与恒等；2D/3D 网络前向形状（含时序块开/关、scale 路径、同尺寸直通）；`upscale_video` 恒等克隆与 T 不变；`load_model` 缓存命中 / fp16 精度 / 架构不匹配明确报错 / 文件不存在 / 目录扫描；`_detect_arch` 权重结构推断；`save_upsegment` 落盘回读逐张量相等；`truncate` 联动清理；`upscale_reset` 记录/文件/越界报错；
  - 扩展 `test_routes.py`：`upscale_models` / `upscale_reset` 端点行为与 400 错误映射；路由注册表断言补入两条新路由；
  - 扩展 `_js_harness.js`：`defaultUpscale` 默认态；`getDs` 二采配置归一化（非法模式/精度/arch/数值钳制/include 过滤）与旧 JSON 注入；`setUpscaleField` 切模式清/留勾选；`toggleUpscaleInclude` 累积排序/重复取消；`upTargetCanvas` 目标画布（2x/1.5x/2.6x 取偶/缺控件）；
- `node --check` 四份 JS（含 harness）语法全过；修改的 Python 模块 `py_compile` 全过；
- 部署：本地跑「同步到ComfyUI.bat」→ 重启 ComfyUI（控制台应见 11 条路由含 `/h3chain/upscale_models`、`/h3chain/upscale_reset` 的注册日志）→ 浏览器 Ctrl+F5 强刷。

## 上手清单（建议实测顺序）

1. 从 HuggingFace `LBH-123-AI/Minimax_h3_latent_Upscaler` 下载权重放入 `ComfyUI/models/latent_upscale_models/`，重启后开导演台右栏「✦ 潜空间放大二采」→ 模型下拉应列出该文件；
2. 已有跑完的项目：模式选「手动选择」，勾 1 段 → 运行 → 报告应出现「段N 二采：W×H→W'×H' · 强度…」，项目文件夹出现 `upseg_*.mp4`，段卡片出现「二采」徽章；全链勾齐后再运行 → 出现 `final_up_*.mp4` 高清成片；
3. 改二采强度再运行 → 只重做二采（报告「待做 N 段」），基础链段不重跑；改某段提示词再运行 → 基础链从该段重做，二采记录自动清，链完成后补做；
4. 段卡片点「↺ 重置二采」→ 记录与 `upseg_*` 清掉，下次二采运行重做该段；
5. 插入视频段/序章在手动模式下勾选 → 同样产出高清版（空提示词轻精修）。
