# 更新说明：潜空间放大二采（主循环内渲染，v2 重构）

整合上游两个仓库的能力，给本插件补一条**高清渲染通道**——开启后每段**采样定稿、解码之前**立即做「神经放大 latent → 低强度重采样补细节」，**分段视频与成片直接保存二采后的高清结果**（单份产物，同名覆盖，不再有 `upseg_*` 副本族），全部产物都在项目文件夹里。

- 放大网络：[LBH-123-AI/Comfyui_Minimax_h3_latent_Upscaler](https://github.com/LBH-123-AI/Comfyui_Minimax_h3_latent_Upscaler)（2D 残差骨干 + 纯 3D 卷积两种，网络逐行移植进 `upscale_net.py`，推理节点不移植）；
- 二采范式：[wjluoxiao/ComfyUI-JZL-MiniMax-H3](https://github.com/wjluoxiao/ComfyUI-JZL-MiniMax-H3) 的 CondSync 思路——latent 放大后用官方 `common_ksampler` 以 denoise<1 重去噪，conditioning 里的 keyframe 同步放大到目标尺寸，避免按原尺寸重编码。

**v2 相对 v1（旧版独立后处理通道）的改动**：

| | v1（旧） | v2（现） |
|---|---|---|
| 执行时机 | 主链全部跑完后清扫（`run_pass`） | 主循环内逐段：采样定稿后、段落盘前 |
| 解码次数 | 基础段解码一次 + 二采再解码一次 | 二采路径只解码一次（高清帧直接落盘） |
| 分段产物 | `seg_NNN.mp4`（基础）+ `upseg_NNN.mp4`（高清）双份 | `seg_NNN.mp4` **就是**高清结果（单份） |
| 成片产物 | `final_*.mp4`（基础）+ `final_up_*.mp4`（高清）双份 | 全链记录齐时成片**直接**流式拼接高清分段 |
| 存储位置 | 项目文件夹（upseg 族） | 项目文件夹（与基础段同名，无额外文件族） |

---

## 一、技术机制

**单段二采流水线**（`upscale.render_segment`，主循环内逐段调用）：

| 步骤 | 实现 | 说明 |
|---|---|---|
| 神经放大 | `upscale_net.load_model` + `upscale_video` | 24 通道 latent 归一化（LATENTS_MEAN/STD）→ 网络 → 反变换；**时间维 T 绝对不变**（17k+5 token 网格硬约束），H/W × 倍率后偶数对齐（latent 偶数 = 像素 32 倍数，官方画布口径） |
| cond 构造 | 官方 `MiniMaxH3ImageToVideo / ReferenceToVideo.execute` | 按目标分辨率重构造（refs/首帧由官方节点重编码到目标尺寸）；插入视频/序章用空提示词轻精修 |
| 桥锚 CondSync | 本段生成时的 guide / 尾帧锚 latent → 同步神经放大 → `_apply_guide` | 上段尾桥与尾帧身份锚的**基础 latent** 放大到目标尺寸后注入 keyframe——锚住段首与上段高清尾的连续性（CondSync） |
| 重采样 | `common_ksampler` + `tail_refine_args` 换算 | **sigma 尾段精化**：起始σ × 精化步数两个直接语义，经换算层映射回 `common_ksampler(denoise)` 的尾段截断口径（σ≥0.95 = 明确请求全量重采）；初始 latent = 放大后视频 latent + 原音频 latent；种子 = 基础段种子 + 1 派生（重放一致）；采样输出音频丢弃 |
| 解码裁剪 | `video_vae.decode` + skip_f/vis_len | 裁剪口径与主循环完全一致——门控/切镜/重摇决策出的可见帧范围原样套用，**机制零漂移**（二采只接管「落盘的帧」） |
| 接缝平滑 | `qc.smoothstep_blend_head` | 上段有高清产物时锚定上段高清尾帧（`uplast_*.png`）+ smoothstep 窗吸收段首偏差；跨度镜像基础链接缝意图（精修/混合开=同跨度平滑，全关=硬切） |
| 落盘 | `seg_NNN.mp4` / `thumb_NNN.png` / `uplast_NNN.png` | **同名覆盖基础分段**——段视频就是二采结果；每段完成即原子落盘（崩溃安全），manifest.upscale.segs 增量记录（写回前重读防竞态） |
| 高清成片 | `media.concat_av_mp4` | 全链记录齐且尺寸一致时成片直接流式拼接高清分段 `final_时间戳.mp4`（单份产物，音轨沿用原声） |

**段内机制与二采的关系**：重摇 / 桥帧门控 / 智能切镜 / 接缝精修 / 接缝指标全部跑在基础分辨率帧上（决策语义零漂移），二采在**这些决策全部定稿之后**才接管落盘——裁多少帧、留多少帧与基础链完全一致。

**两种网络架构**（面板可选，加载时按权重键名自动校验，不匹配明确报错）：

- **2D**（上游默认，快）：残差骨干（channels=640）+ TemporalConv 时序块，逐帧 2D 卷积 + bilinear 插值；
- **3D**（时序一致性好）：纯 3D 卷积骨干（channels=512），trilinear 插值；3D 权重严格加载（strict=True），2D 因 attn 强制关闭用 strict=False。

## 二、两种触发模式（导演台面板）

右栏 **「✦ 潜空间放大二采」** 面板（折叠式，类似高级设置；开启时自动展开 + 边框高亮）：

- **跟随生成**：每段采样定稿后立即二采——逐段审片时即「生成一段、二采一段」；记录有效（参数与基础身份都没变）的已完成段直接沿用，不重复渲染；
- **手动选择**：段落卡片出现勾选框，勾选任意段（含插入视频段/序章——外部素材段是否二采由勾选决定）后点「开始生成」执行；
- **关闭**（默认）：不执行，已产出的高清分段不受影响。

面板参数：放大模型（`models/latent_upscale_models/` 目录扫描下拉，`GET /h3chain/upscale_models`）、网络架构、放大倍率 1.0–4.0（**目标画布徽章**实时显示，与后端 target_hw 同口径）、二采强度 0.05–1.0（=**尾段起始 σ**，0.3–0.45 常用；默认 0.35）、二采步数 1–100（=**尾段精化步数**，建议 3–8，与强度解耦；默认 6）、二采 CFG（H3 常用 1.0）、精度 fp32/fp16/bf16（**默认 fp16**，对齐参考工作流用法省显存；只影响放大网络推理，重采样仍在原精度）。

## 二之二、v3 参数语义：sigma 尾段精化的直接口径（对齐参考工作流）

**背景（对比两份外部参考工作流后的核实结论）**：`common_ksampler(denoise<1)` 的内部行为本就是「N 步调度序列取尾 `int(N×denoise)` 步」，flow-matching 下初始加噪 `(1-σ₀)x0+σ₀ε` 落在截断后的 σ₀——机制上已经是 sigma 尾段精化。参考工作流（SplitSigmas/H3SigmaRefiner/parity plan 一族）真正的优势是**参数直接语义**：σ 起点与精化步数独立可控。旧版 UI 的「总步数×强度」把两者经 `int(N×d)` 耦合并网格量化，用户只能间接凑数。

**换算层 `tail_refine_args(σ_start, n) -> (steps=N, denoise)`**（纯函数，单测覆盖）：

| 规则 | 公式 | 说明 |
|---|---|---|
| 调度网格 | `N = clamp(round(n/σ), n+1, 100)` | simple 线性调度下 σ₀≈n/N 即请求值；非 simple/带 shift 调度为网格近似（报告行打印实际生效值兜底） |
| 强度 | `denoise = (n+0.5)/N` | +0.5 抵御 `int(N·denoise)` 浮点截断少 1 步 |
| 全量重采 | `σ ≥ 0.95 → (n, 1.0)` | 只有明确请求才 denoise=1.0；部分精化档 `N≥n+1` 保证 denoise<1——**denoise=1 会完全丢弃放大后的 latent**，绝不能因换算意外触发 |
| σ 过小 | N 触顶 100 时反向钳 `n = int(100·σ)` | σ=0.05 时最多 5 格可走 |

常用档速查：默认 (0.35, 6)→17 步调度取尾 6 步 @ σ≈0.353；(0.45, 6)→13 步取尾 @ σ≈0.462；(0.25, 4)→16 步取尾 @ σ≈0.25。**旧配置自动迁移**（字段级，见「兼容性」）。

**精度默认 fp16**：两份参考工作流的放大网络均用 fp16 推理（上游 LBH 节点默认 fp32，fp16 是工作流侧的使用选择）；放大完自动卸回 CPU（offload_after 同思路，v2 已实现）。目标画布 >2.5MP 时 preflight 仍会警告 fp16 高频溢出花屏风险——出现花屏手动回 fp32。

**预留：时序细节强化（time-bias / STG / 尾段细分，未实现；源码已定位）**

参考工作流 B（「双时钟」）的二采在尾段精化之上叠加若干模型侧增强。**出处更正（2026-08-22 复核）**：这批节点不在 JZL 仓库（wjluoxiao/ComfyUI-JZL-MiniMax-H3 仅有 ref2va 参考编码节点，早前「机制无可参考的公开实现」的结论作废），而在 [T8mars/comfyui-minimax-h3-audio-T8](https://github.com/T8mars/comfyui-minimax-h3-audio-T8)（**GPL-3.0 开源**，`nodes_detail_sampling_advanced.py` + `detail_sampling_advanced.py`）——机制可考、可精确复刻：

| 机制 | 实现原理（源码实证） | 额外前向成本 | 对本插件的价值 |
|---|---|---|---|
| time-bias（Detail-Daemon 类） | 尾段平滑窗（进度 0.70→0.95）内**只偏置喂给共享 AV Transformer 的 sigma 值**（bias -0.025~-0.05，域 video_sigma/base_flow），不加噪声、积分 sigma 与 NFE 不变——让模型「看到稍干净的时间」抠细节 | **零** | 高——`step_cond_noise_guard` 就是同款 dit.forward patch 模板，挂载即用 |
| STG（skip-layer 差分引导） | 每激活步（进度 0.25→0.85）多跑一次跳块扰动前向（默认跳 double block 25），与完整前向差分加权；**不依赖 CFG 分支，CFG=1.0 下有效**（此前「空操作」的判断按 SLG 惯例推断有误，特此更正） | 每步 +1 次前向（约 +50%） | 中——T8 自己也默认关 |
| 尾段细分（extra_tail_steps 0-8） | 只对最后一段 sigma 区间再细分（3 步对应 75/50/25% 递减到零），三种间距（video_sigma_linear/cosine、base_flow_linear），0=严格直通 | 少量 | 中——两次 common_ksampler 串联可近似：第一次主精化、第二次更小 denoise 只跑尾格 |
| RF Restart | 正常轨迹到干净终点后 rectified-flow 插值重新加噪、二次下降（真随机重启） | 一段完整下降 | 低——与本项目「种子+1 重放一致」哲学冲突 |
| 双时钟 shift | 视频/音频独立 sigma 时钟（shift_video=12 / shift_audio=3），**二采档 shift_video 降到 6**——高分辨率下调度更线性、细节合成更充分 | 零 | 中——需 model_sampling patch（add_object_patch 包住二采调用再恢复），项目暂无 shift 操作代码 |
| TwoPass DetailMixer | 不重建完整轨迹，接收 parity plan 的 refine_sigmas 在其上叠加上述各效果（enable_tail/time_bias/stg/restart 四开关） | — | 参考其 **fail-closed 默认全关** + 输出 actual_nfe/report_json 审计的设计 |

**接入优先级**（暂缓实施，按收益/成本排序）：① 二采前后细节增益度量（`tools/ab_report.py` + 解码帧高频能量/拉普拉斯方差，报告行输出增益百分比——先能度量再谈增强）→ ② time-bias（零前向成本，patch 模板现成）→ ③ 频域细节混合（原创思路：低频走放大 latent 保结构零漂移、高频走精化输出取细节增益，纯 torch 可单测）→ ④ 段自适应 σ 起点（按 `metrics` 光流运动量调强度：高运动段降 σ 防拖影鬼影、静态对话段升 σ 抠脸——「打斗不糊/小脸不崩」的两个面）→ ⑤ 二采 shift 独立化 → ⑥ 尾段细分两段串联 → ⑦ STG / EAV（实验档，默认关）。

**接入预留位**：

- 挂载点：`upscale.render_latent` 的 `common_ksampler` 调用位（仅包住二采采样，不影响主链）；
- patch 模板：`nodes.step_cond_noise_guard`（nodes.py）——dit.forward 包裹 + 按步改 payload + try/finally 恢复的现成范式；
- 参数形态：`parse_state` 增加默认关闭的开关档（进 `_PARAM_KEYS` 指纹，启用才影响输出/重做判定）。

段卡片可见性：已二采段显示「二采 W×H」青色徽章；手动模式下已完成段可勾选；「↺ 重置二采」按钮清该段记录与产物（`POST /h3chain/upscale_reset`，下次运行时重做）。

## 三、重做规则（manifest.upscale）

**二采参数不进基础链 ckpt_params 指纹**——改参数只重做二采，不动已生成段：

| 触发 | 范围 | 机制 |
|---|---|---|
| 二采参数变（hash 变） | 模式范围内全部段 | 记录 `hash` 不匹配 → 待做 |
| 基础链从段 k 重做（truncate） | 记录与文件 ≥ k 全清 | `checkpoint.truncate` 联动删 seg mp4/thumb/uplast + 截记录，重跑时按新 base_hash 补渲染 |
| 某段基础提示词/种子变 | 仅该段 | 记录 `base_hash`（prompt_hashes[g] + seeds[g]）不匹配 → 失效 |
| 产物文件丢失（mp4/thumb/last） | 该段 | `_record_valid` 校验文件在，缺即重做 |

单段二采只依赖「该段基础 latent + 本段生成时的桥锚/尾锚 latent + 二采参数」——与处理顺序、模式、其他段是否二采无关，可独立重做。

## 四、工程细节

- **模型预加载**：主循环前 `load_net` 一次——权重缺失/架构不匹配当场报错，降级为基础分辨率整链运行（报告注明「面板已开启但本次跳过——…」），不让每段反复撞同一个错；
- **显存管理（抢占式，保真二采语义）**：H3 fl2va UNET fp16 权重常驻 ~26GB（32GB 卡基础采样后已占 27-28GB），2× 高清激活 ~5.4GB（基础 4×），26+5.4≈31.4GB 正好贴着 32GB 卡 31.36 限值上沿。**保「先放大→高清重采样」的真语义**，不做「重采样退回基础分辨率」的缩水替代，而是抢占式腾显存：放大网络放大完即卸回 CPU；重采样前 `unload_all_models()` 把 CLIP/videoVAE/audioVAE 全卸到 CPU（补偿 keyframe 的 CLIP/VAE 不再占显存），`common_ksampler` 原生机制只回载 UNET——26GB UNET + 高清激活勉强放下；若仍 OOM 自动降 `LOW_VRAM` 分块兜底（慢数倍但能出结果），报告行注「低显存模式」。VAE 解码自带官方 OOM→tiled 降级，无需干预；全部失败时该段降级基础分辨率，报告提示降倍率（1.5× 以下），改倍率重跑只补渲染二采、基础链不动；
- **单段失败降级**：`render_segment` 异常只降级该段（报告「段N 二采失败：…——本段按基础分辨率保存」），基础保存强制重编码（fresh=True）覆盖可能写坏的 mp4 自愈，整链照常；
- **记录沿用**：记录有效时跳过渲染，段视频直接沿用盘上高清 mp4（读取 `uplast_NNN.png` 续供下段接缝平滑锚）；
- **存档门控**：二采产物落项目文件夹（manifest 记录 + seg mp4 覆盖），需开启自动保存（或审片/自动存档）——否则报告提示并跳过；
- **零音频回归**：音轨沿用原声（重采样输出的音频丢弃）；
- **主输出不变**：节点主输出 images 仍是基础分辨率帧（ComfyUI 画布预览/后续节点不受影响），高清只在项目文件夹；
- **旧版产物清理**：重渲染某段时自动清旧版 `upseg_*/upthumb_*` 文件（`purge_legacy`）；`truncate`/`upscale_reset` 同步清扫；
- **路由**：`GET /h3chain/upscale_models`（模型列表，无 torch/目录时返回空列表）、`POST /h3chain/upscale_reset`（清段记录，ValueError → 400）；每条路由照旧双前缀注册（根路径 + `/api` 副本）。

## 「开了却没触发」排查清单

二采是否生效**看运行报告第一屏**——开启时会有一行「潜空间放大二采：跟随生成 · 3D 2× · 精化 6 步 @ σ≈0.35——…」；没有任何二采相关行 = 后端根本没收到开启状态，按序排查：

1. **后端代码未更新 / 未重启**（最常见）：ComfyUI 控制台启动日志应有 **11 条路由**注册记录（含 `/h3chain/upscale_models`、`/h3chain/upscale_reset`）——只有 9 条说明跑的还是旧版 Python，git pull 后必须重启 ComfyUI；
2. **浏览器缓存旧 JS**：Ctrl+F5 强刷（面板能显示不等于后端是新代码——JS 可能是缓存的）；
3. **模式仍是「关闭」**：面板状态存于「导演台状态」JSON widget——点运行即带当前值，但**刷新页面后是否保留取决于工作流保存**（网页版自动保存默认开；显式打开的 .json 工作流需 Ctrl+S）；
4. **未选放大模型**：报告会出现「面板已开启但本次跳过——未选择放大模型…」，按提示放权重进 `models/latent_upscale_models/`；
5. **未开自动保存**：报告会出现「需开启自动保存（或审片/自动存档）建立项目存档，本次跳过」。

## 兼容性

- **v1→v2 参数迁移（v3 参数语义）**：`ds.upscale` 无 `schema` 键的旧 JSON 由前后端同口径自动迁移（`getDs` 读时迁移并写回 schema:2，`parse_state` 读时迁移）：**显式存过** steps 的按旧口径折算 `n = int(旧steps × 强度)` 保行为（旧默认 15×0.45=6 恰为新默认步数）；**没存过**的字段直接落 v2 新默认（0.35/6/fp16——从未配置=用最新推荐值）。迁移后取值变化 → 二采参数指纹变 → 已有二采记录按新口径失效重做一次（一次性成本，符合预期；基础链产物不动）；
- **旧存档/旧工作流**：`ds.upscale` 缺失 = 默认关闭（前端 `defaultUpscale()` 注入、后端 `parse_state` 同口径），旧项目行为逐帧不变；节点控件零改动（全部走导演台状态，不动 widgets 顺序）；
- **v1 产物迁移**：旧版 `upseg_*/upthumb_*` 文件不再读取——重渲染该段时自动清理；旧 manifest 里的 upscale 记录因 hash 口径未变仍可比对，但 `files` 指向的旧文件名校验不过 → 视为待做，自动按新方案重渲染；
- **manifest 前向兼容**：`upscale` 键缺失按空处理；`truncate` 只在键存在且超界时截断；
- **缓存失效**：二采参数存于「导演台状态」widget 值内，改参数自动触发节点重执行（ComfyUI widget 变化即失效缓存）；且二采仅在自动存档开启时运行（彼时 `IS_CHANGED` 恒 nan 强制真执行），无需额外判定。

## 验证

- 全量测试（带 torch venv）：**18 个测试文件全绿**，其中 `test_upscale.py` 覆盖：`parse_state` 关闭/归一化/NaN 兜底/include 解析/**v1→v2 schema 迁移**（显式折算保行为、缺字段落新默认）；**`tail_refine_args` 换算契约**（默认/迁移档、σ≥0.95 全量重采、部分精化档永不撞 denoise=1、100 格反向钳、常用区不变量：步数精确 + σ₀ 贴请求 ≤0.07 网格量化）；`params_hash` 稳定性与参数敏感（mode/include 不进指纹）；`base_hash`；`record_stale` 重做判定（hash/base_hash/文件缺失含 thumb/手动范围）；`target_hw` 偶数对齐；`resize_latent_bilinear` 形状与恒等；2D/3D 网络前向形状（含时序块开/关、scale 路径、同尺寸直通）；`upscale_video` 恒等克隆与 T 不变；`load_model` 缓存命中 / fp16 精度 / 架构不匹配明确报错 / 文件不存在 / 目录扫描；`_detect_arch` 权重结构推断；**高清分段同名存档联动**（`upscale_files` 命名 / `truncate` 清扫 / `try_final` 记录门控 / `purge_legacy` 旧文件清理 / `projects.upscale_reset` 自愈重建）；
- 扩展 `test_routes.py`：`upscale_models` / `upscale_reset` 端点行为与 400 错误映射；路由注册表断言补入两条新路由；
- 扩展 `_js_harness.js`：`defaultUpscale` 默认态；`getDs` 二采配置归一化与旧 JSON 注入；`setUpscaleField` 切模式清/留勾选；`toggleUpscaleInclude` 累积排序/重复取消；`upTargetCanvas` 目标画布（2x/1.5x/2.6x 取偶/缺控件）；
- `node --check` 全部 JS（含 harness）语法全过；修改的 Python 模块 `py_compile` 全过；
- 部署：本地跑「同步到ComfyUI.bat」→ **重启 ComfyUI**（控制台应见 11 条路由含 `/h3chain/upscale_models`、`/h3chain/upscale_reset` 的注册日志）→ 浏览器 Ctrl+F5 强刷。

## 上手清单（建议实测顺序）

1. 从 HuggingFace `LBH-123-AI/Minimax_h3_latent_Upscaler` 下载权重放入 `ComfyUI/models/latent_upscale_models/`，重启后开导演台右栏「✦ 潜空间放大二采」→ 模型下拉应列出该文件；
2. 模式选「跟随生成」→ 运行 → 报告第一屏出现「潜空间放大二采：跟随生成 · …」行，每段落盘行附带「段N 二采：W×H · 3D 2× · 精化 n 步 @ σ≈…」；项目文件夹里 `seg_*.mp4` 直接就是高清分辨率（右键看属性），段卡片出现「二采」徽章；
3. 全链跑完后报告出现「二采成片：N 段高清流式拼接 → final_*.mp4」——成片就是高清结果（不再额外产出基础分辨率副本）；
4. 改二采强度再运行 → 只重做二采（已完成段按记录沿用，失效段重渲染），基础链段不重跑；改某段提示词再运行 → 基础链从该段重做，二采记录自动清，链完成后补渲染；
5. 段卡片点「↺ 重置二采」→ 该段记录与 seg mp4/缩略图/尾帧锚清掉，下次运行自愈重建（二采开=重渲染高清，二采关=回放段重编码基础分辨率）；
6. 插入视频段/序章在手动模式下勾选 → 同样产出高清版（空提示词轻精修）。
