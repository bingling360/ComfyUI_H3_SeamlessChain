# 二采优化路线：本轮已实施 + 后续规划

> 依据《二采放大环节对比分析》与 [T8mars/comfyui-minimax-h3-audio-T8](https://github.com/T8mars/comfyui-minimax-h3-audio-T8)（GPL-3.0）源码实证整理。
> 机制出处、参数语义（σ 起点 × 精化步数）、schema v2 迁移等背景见[更新说明_潜空间放大二采](更新说明_潜空间放大二采.md)，本文只记路线：**本轮做了什么、为什么低风险、接下来做什么**。

---

## 一、本轮已实施（2026-08-22）

### ① 细节增益度量（先能度量，再谈增强）

| | 内容 |
|---|---|
| 机制 | latent 域高频能量：逐帧 3×3 均值池化残差的 RMS（≈空域拉普拉斯能量）。对比「纯放大 latent（无精化）」vs「精化后 latent」→ 细节增益百分比 |
| 实现 | `upscale.latent_hf_energy`（纯函数：均匀抽样 ≤12 帧 + **CPU 计算**，避开二采峰值期显存）/ `hf_gain_ratio`；`render_latent` 在放大后取基线、精化后取终值，作为第 6 个返回值 |
| 输出 | 段报告行 `细节 +12%`（`{:+.0%}` 带符号——负值=精化抹平了高频，即调参信号）；manifest 记录 `hf_gain`（仅复盘用，不参与重做判定） |
| 成本 | 每段约 40MB CPU 张量 + 两次小卷积，<0.1s；零解码、零模型前向 |

**判读口径**：`细节 +15%~+40%` 正常；接近 0% → 精化太浅（σ 起点过低/步数过少）；大幅负值 → 过平滑（步数过多）或偏置过强。这是后续所有增强项（③-⑦）的效果标尺。

### ② time-bias 移植（Detail-Daemon 类，T8 机制）

| | 内容 |
|---|---|
| 机制 | 精化尾窗（进度 0.70→1.0，smoothstep 渐入）内，**只把喂给共享 AV Transformer 的 timestep（=σ×1000）向更干净方向偏置**——模型「看到稍干净的时间」从而多抠细节。不加噪声、不改积分 σ、**NFE 不变（零额外前向）** |
| 实现 | `upscale.time_bias_sigma`（纯数学核心）+ `time_bias_guard`（patch `dit.forward`，与 `cond_audio_rows_guard` 挂不同钩子无冲突）；`render_latent` 仅在 `time_bias>0` 时安装，try/finally 恢复（OOM 抛 `UpscaleAbortError` 时同样恢复） |
| 参数 | UI「时间偏置」0–0.2 默认 **0=关**；0.025–0.05 常用（对齐 T8 默认 -0.025~-0.05 的绝对值）。窗口 0.70→1.0 为代码常量（与 T8 默认一致） |
| 指纹 | **条件化**：`time_bias>0` 才进 `params_hash`——默认关闭不改变哈希，既有二采记录不失效重做（`_hash_params` 同口径供 `write_record.params`） |
| 音频 | 偏置会同时作用于音频 token 的时间嵌入——但二采输出的音频 latent 本就被丢弃（`unbind` 后弃用），对产物零影响 |

### 本轮风险排查记录（实现前逐项核查）

1. **patch 冲突面**：`cond_audio_rows_guard` 挂 `_cond_audio_rows` 属性、time-bias 挂 `forward`——不同钩子可叠加；主链的 `step_cond_noise_guard` 只存在于一采期间，时序上与二采不重叠；
2. **timestep 语义**：σ×1000、批内一致、tensor——由 `step_cond_noise_guard`（nodes.py:107）既有用法实证，守卫完全镜像该模式（读 `flatten()[0]`、`full_like` 保 dtype/shape）；
3. **clamp 边界**：精化网格最小评估 σ≈1/N（N≤100 → ≥0.01）> 偏置量 0.03~0.05 的常见档？——否，17 格网格最小评估 σ≈0.059，0.05 偏置可越界 → `time_bias_sigma` clamp ≥0 兜底（单测覆盖越界→0 用例）；
4. **显存峰值**：HF 度量若在 GPU 全量卷积需 ~0.9GB 额外峰值——改为抽样 ≤12 帧搬 CPU；
5. **`del up_v` 时序**：HF 基线必须在 NestedTensor 构造前取（之后本地名删除）；
6. **返回元组扩展**：`render_latent` 5→6 元素，唯一调用方 `render_segment` 同步；单测不触达运行期函数；
7. **manifest 前向兼容**：`rec["hf_gain"]` 新增键，`_record_valid` 不校验它，旧记录无此键照常有效；
8. **JS 深比较**：`defaultUpscale`/`getDs` 同步加 `time_bias: 0`，挂具的 deep-eq（旧 JSON 注入 == defaultUpscale）自动约束两侧一致。

**测试**：新增 6 个用例（HF 能量零/噪声/维度/确定性、增益比、time-bias 数学全边界、守卫 patch/恢复/clamp、指纹条件化）；全量回归除 `test_metrics.py`（存量环境缺可选嵌入依赖，与改动无关）全绿。

---

## 二、已实施（2026-08-23 轮，分支 `feature/upscale-freq-detail-mix`）

### ③ 频域细节混合（原创）

| | 内容 |
|---|---|
| 机制 | `out = 精化输出 + r·(低频(放大 latent) − 低频(精化输出))`，低频 = 逐帧 3×3 均值池化 + **replicate 边界填充**（零填充会把边界低频拉向 0，r=1 时角点偏差达 4/9 → 画面边缘晕影；replicate 下常量场严格守恒）。r=1 即设计端点：**低频全走放大 latent**（结构/段间接缝零漂移——放大 latent 与基础段逐像素同构）**+ 高频全走精化输出**（只取细节增益） |
| 实现 | `upscale.freq_mix_latents`（纯函数：CPU float32、按 T 分块 8 帧计算，池化逐帧独立故分块无边界效应）；`render_latent` 仅在 mix>0 时保留纯放大 latent 的 CPU 副本（关闭时零开销不复制、采样期显存零新增），精化 unbind 后混合，**细节增益在混合后的交付 latent 上度量**（报告行如实反映） |
| 参数 | UI「细节混合」0–1 默认 **0=关**（原样返回精化输出本身，不克隆）；0.3–0.6 常用（精化带花/内容漂移时加大）；1=结构全锁放大 latent |
| 指纹 | **条件化**：`mix>0` 才进 `params_hash`（同 time_bias 口径）——默认关闭不改变哈希，既有二采记录不失效重做 |
| 反漂移性质 | 精化若只做低频内容漂移（零高频），全混合后输出=纯放大 latent——漂移被完全丢弃、细节一点不少拿；这正是对冲「精化带花/改写画面」的数学保证 |

**本轮风险排查记录**：
1. **显存**：混合全程 CPU（副本仅一次 GPU→CPU 传输，采样期显存零新增；峰值 RAM ≈ 2× latent + 单块临时量，500MB 级）；
2. **边界填充**：零填充 vs replicate 的角点偏差用常量场单测钉死（守恒性）；
3. **分块一致性**：chunk=1 与 chunk=8 逐位一致（单测覆盖）；
4. **退化兜底**：None/形状不一致/空间<3 → 原样返回精化输出（保交付产物，不阻断）；
5. **度量口径**：增益在混合后取值——低频锚回会略降增益读数属正常（高频未变、总量口径变化），判读仍按 ① 的区间。

**测试**：新增 2 个用例（混合数学全边界：0=关不克隆/1=分层恒等/线性/钳制/反漂移/纯函数不改入参/float32 统一/分块一致/三类退化兜底；指纹条件化+解析归一+与 time_bias 独立叠加）；`test_parse_state_normalize` 补 mix 默认/钳制断言；全量 19 个测试文件全绿（本机 Python313 补装 einops 后 `test_metrics.py` 亦通过）。

---

## 三、已实施（2026-08-23 第二轮，直接在 main）

### ④ 段自适应 σ 起点

| | 内容 |
|---|---|
| 机制 | 基础段 **latent 域相对运动量**：均匀抽 ≤12 个相邻帧对，motion = mean(‖x[t+1]−x[t]‖_rms / ‖x[t]‖_rms)（尺度不变）。比原计划的 metrics.py 像素域 Farneback 光流（需解码帧）**零成本**：直接在基础 latent 上 CPU 计算，prompt/insert/prologue 段全适用；同 latent 同值（决定论，重放不串档） |
| 实现 | `latent_motion` / `motion_tier` / `adaptive_sigma` / `resolve_refine_sigma`（纯函数可单测）；render_latent（实际采样）与 render_segment（报告行）**同用 resolve_refine_sigma**，两处永远同口径 |
| 档位 | 静态（<0.34）σ+0.05 抠脸 / 中 ±0 / 高运动（>0.75）σ−0.075 防拖影鬼影，钳 [0.05, 0.95]。默认 σ=0.35 → 三档 ≈ **0.40 / 0.35 / 0.275**（对齐路线「0.4 抠脸 / 0.25-0.3 打斗」）。阈值 **v2 已按真实项目校准**（首个真实项目 8 段运动量 0.47–0.60 全饱和误判 → ×17/5 重标定，详见「三之二 修复轮」；报告行照打「自适应σ档位(运动量)」、manifest 记 `motion`，后续数据可继续校准） |
| 指纹 | 仅策略位（`adaptive: true`）进 params_hash——档内生效 σ 由该段基础 latent（经 base_hash 指纹）**决定论派生**，无需逐段进指纹也不会串档（路线原文「按档位量化进指纹」的更优解） |

### ⑤ 二采 shift 独立化（dual clock 结论）

| | 内容 |
|---|---|
| 机制 | **镜像官方 `MiniMaxH3SigmaShift` 节点**（comfy_extras/nodes_minimax_h3.py 源码实证）：`ModelSamplingAV+CONST` 子类 `set_parameters(shift, audio_shift)` + `add_object_patch("model_sampling")`，并**同步写 `transformer_options["minimax_h3_sigma_shift_video/audio"]`**——DiT 用这两键反演共享基网格，只改 model_sampling 不写键会静默不一致（官方契约） |
| 实现 | `_shifted_model`（ComfyUI 运行期）：`model.clone()` 补丁后**只喂给二采的 common_ksampler**——主链模型对象零改动、无需恢复；比路线原文「add_object_patch 包住调用后恢复」等价但更稳（无内部状态回滚风险）。audio_shift 原样透传主链现值（H3 默认 3.0） |
| 参数 | UI「调度偏移」0–16 默认 **0=关**（沿用主链 H3 默认 shift=12）；**6=T8 二采实证档**（高分辨率下调度更线性、细节合成更充分）；>0 进二采指纹 |

**本轮风险排查记录**：
1. **克隆共享**：clone 共享 DiT 权重与 `cond_audio_rows_guard`/`time_bias_guard` 挂的同一 diffusion_model 对象——守卫对克隆模型照常生效；
2. **DiT 双键契约**：model_sampling 与 transformer_options 必须同步（见上），官方节点源码逐行镜像；
3. **决定论**：运动量纯 CPU 基础 latent 计算，重渲染同段同值；adaptive 关=完全不算运动量（零开销）；
4. **布尔归一**：仅 JSON `true` 视为开（`"true"`/`1` 防御性关闭，默认关侧安全）；
5. **JS**：`upscaleSig` 补 time_bias/mix/adaptive/shift 四键（外部数据变化触发面板重绘）。

**测试**：新增 4 用例（latent_motion 静态=0/交替大幅>0.5/尺度不变/退化兜底/决定论/档位联动；adaptive_sigma 三档+阈值边界+clamp 双侧；resolve_refine_sigma 开/关两径与决定论派生；adaptive/shift 条件化指纹+互相独立+解析归一）；`test_parse_state_normalize` 补 adaptive/shift。全量 19 个测试文件全绿。

---

## 三之二、修复轮（2026-08-23 第三轮，main）

> 依据：真实项目 602（8 段 · 3D 1.5× · 四增强全开）运行报告 + 接缝体检复盘。三项修复，编号用「修复A/B/C」不占用后续规划的 ⑥⑦。

### 修复A：tw/th 宽高对调（历史 bug，全链路口径修正）

| | 内容 |
|---|---|
| 现象 | 二采报告/manifest 记「960×1728」（真实画幅 1728×960）；Linux 高清成片拼接 `ArgumentError … returned 22` 失败回退基础分辨率成片 |
| 根因 | `render_latent`/`preflight` 把 `target_hw(...)[0]`（latent **高**）命名 tw 当宽用——与官方 `_empty_av_latent(width, height)` 语义对调。非方画布下：cond 空画布与段 0 首帧 keyframe 被转置编码（DiT 布局取实际 latent 形状故其余段幸免）、manifest `size`、拼接目标宽高全部同错 |
| 修复 | 新增纯函数 `upscale.target_pixels(h,w,scale) -> (宽,高)`（宽在前，与官方同向），`render_latent`/`preflight` 统一走它 |
| 兼容 | 尺寸不入指纹（由 scale+基础画布派生）：既有高清分段**不重做**；段卡片徽章读旧记录 `size` 会显示转置数直到该段重渲染（仅显示层——分段 mp4 本身一直是正确横构图） |

### 修复B：成片拼接画幅改实测（try_final）

| | 内容 |
|---|---|
| 根因 | `try_final` 把记录 `size` 直接喂 `concat_av_mp4(width=…, height=…)`——旧记录存的是转置值，目标画幅 960×1728 与源 1728×960 相撞 |
| 修复 | 新增 `media.probe_video_size(path)`（探测 mp4 实测 宽×高，缺 av/无视频轨返回 None）；`try_final` 按首个分段**实测**画幅拼接与报告（记录 `size` 只用于一致性门控——旧记录整批同向转置，互比不受影响）；`concat_av_mp4` 失败时打印完整 traceback（下次环境类错误可直接定位抛出点） |
| 附带 | 拼接 EINVAL(22) 的确切抛出点仍需 Linux 控制台完整栈确认——修复A/B 去掉转置输入后大概率消失，traceback 是兜底证据链 |

### 修复C：段自适应 σ 阈值重标定（v2）

| | 内容 |
|---|---|
| 现象 | 真实项目 8 段运动量全部 0.47–0.60，旧阈值（静态<0.10/高运动>0.22）**全饱和**——统一误判高运动压 σ 0.35→0.275，细节增益 +6~11% 低于 +15~40% 健康区间（精化过浅，放大残留伪影清不干净） |
| 根因 | latent 相邻 token 隔约 17/5≈3.4 个像素帧（H3 的 17k+5↔5k+2 时间压缩），运动量按 token 间距计；合成张量标定的逐帧口径阈值在真实 latent 上整体偏低约 3.4 倍 |
| 修复 | 阈值 ×17/5 重标定：静态 <0.34 / 高运动 >0.75（该真实样本全落中档=σ 不动，真静态/真高运动仍可分）；`_ADAPTIVE_VERSION="v2"` 进 params_hash——adaptive 开启的旧记录按新档**自动重做**（下次运行触发高清重渲染，符合「策略变即重做」既有行为） |
| 验证锚点 | 重跑同项目：各段报告行应显示「自适应σ中(0.4x-0.6x)」、σ≈0.35、细节增益回到 +15% 以上量级 |

**测试**：新增 `test_target_pixels`（40×72 latent ×1.5 → 1728×960 真实案例 + 竖画布/取偶/恒等）、`test_probe_video_size`（真实编码 48×80 帧 → (80,48) 宽在前、坏路径 None；av 缺失 SKIP）；`test_adaptive_sigma` 更新至 v2 阈值 + 真实样本断言（0.47/0.60 落中档）；`test_params_hash_adaptive_shift` 改盯 `_ADAPTIVE_VERSION`。全量 169 用例：本机 167 passed + 2 skipped（`test_media` 需 av，ComfyUI 环境自带）。

---

## 四、后续规划（按收益/成本排序，均暂缓实施）

> ③ 频域细节混合、④ 段自适应 σ 起点、⑤ 二采 shift 独立化均已实施（见上），编号保留不复用。

### ⑥ 尾段细分（两次 common_ksampler 串联）

T8 对最后一段 sigma 区间再细分（75/50/25% 递减）。不手搓 sigma 的近似：第一次跑 [σ₀→σ₁] 主精化，第二次以更小 denoise 只跑尾格——保住「只用 common_ksampler 公开 API」原则。收益中等，等①的度量数据说话。

### ⑦ STG / EAV（实验档，默认关）

- STG（skip-layer 差分引导）：每步 +1 次扰动前向（跳 double block），T8 默认也关；CFG=1.0 下有效（自差分式，不依赖 CFG 分支）；
- EAV（Enhance-A-Video）：时序注意力增强，T8 已移植 H3，对二采时段序连贯有帮助；
- 两者都按 time_bias 的模式接入：默认关 + 条件化指纹 + try/finally patch。

### 长期项（专项工程）

- **face refine 局部重精化**：检测人脸区域 → latent mask 局部加噪重精化（T8 有 multiface_refine/face_refine_parity 可参考，GPL）——「小脸不崩」的正解，工程量大；
- **Tiled 二采**：分块精化 + 时序窗口重叠混合，突破 12MP 画布预检上限；
- **细节增益像素域校准**：latent 域 HF 与人眼感知的映射做一次离线校准（ab_report 抽样解码对比），让「+30%」有绝对含义。

### 明确不做

- **RF Restart**（真随机重启）：与「种子+1 重放一致」哲学冲突；
- **绕开 LoRA 做二采**：低噪声段会把细节风格回拉到基础模型，角色/脸部 LoRA 下是负优化（一采/二采同模型是对称设计）。

---

## 五、部署与验证提醒

1. 跑「同步到ComfyUI.bat」→ 重启 ComfyUI → Ctrl+F5；
2. 旧项目默认行为不变（time_bias/mix/shift=0、adaptive=false 不进指纹，既有二采记录**不**失效）；任一开关启用后该段按新指纹重做；
3. 验证顺序（逐项开、每步对比报告行与肉眼观感）：
   - 默认参数跑一段看「细节 +x%」基线；
   - 开时间偏置 0.03（0.025–0.05 扫档）；
   - 开细节混合 0.5（0.3–0.6 扫档——增益读数略降属正常，看接缝/结构）；
   - 开段自适应σ：看各段报告行「自适应σ档位(运动量)」，对话段应落静态档、打斗段落高运动档；分档与观感不符时用 manifest 的 `motion` 值校准阈值（`_MOTION_STATIC_MAX/_MOTION_ACTION_MIN`）；
   - 开调度偏移 6：对比 0/6 两档的细节读数与清晰度；
   - 全开时以肉眼观感为主、读数为辅（四项增强相互正交：偏置改模型时间观感、混合改交付 latent、自适应改 σ 起点、shift 改调度网格）。
