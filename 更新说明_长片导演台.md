# 长片导演台（web/h3_director.js）

参照「H3 一体化总控导演台」视觉风格制作的全屏一体化控制台，是原「长片审片」侧栏面板
（feature 分支 `h3chain_panel.js`）的全面升级。**零 Python 内核改动**：只新增本 JS 文件，
经 `__init__.py` 的 `WEB_DIRECTORY = "./web"` 自动加载，不修改任何节点 / 路由 / 现有前端。

## 打开方式

- **新前端**（ComfyUI v1.x）：左侧侧栏 → 「长片导演台」Tab → 迷你状态卡 → 点「打开长片导演台」进入全屏工作台
- **旧前端**：右下角出现 🎬 悬浮按钮，点击直接打开全屏工作台
- 全屏内按 **Esc** 或右上角 ✕ 关闭

## 界面结构

```
┌ 顶栏：H3·SEAMLESS CHAIN │ 长片一体化导演台 │ LED状态灯 + 当前项目 │ ✕ ┐
├────────────┬───────────────────────────┬──────────────┤
│ 左：项目与链 │ 中：段落流水线               │ 右：链参数      │
│ 项目列表     │ 状态条 + 段落进度轨           │ 宽高/帧数/引导帧  │
│ ＋新建项目   │ 段落卡片（常驻textarea 🎲重摇  │ 步数/CFG/种子🎲  │
│ 链参数概览   │  📎插视频）                  │ 采样器/调度器/审片 │
│ 素材与参考   │ ＋添加一段 / 尾部插入视频      ├──────────────┤
│ ◐模式:文生/ │ 上次运行报告（折叠）          │ 右：成片历史    │
│  首帧/多参   │                            │ 当前成片大播放器 │
│  首帧图片     │                            │ 历史（播放/下载/删）│
│  参考图片≤9   │                            │                │
│  (按模式接线) │                            │                │
├────────────┴───────────────────────────┴──────────────┤
│ 页脚：当前链 / 审片 / 引擎兼容提示 │ ▶ 开始生成 / ▶ 继续下一段 │
└─────────────────────────────────────────────────────┘
```

输入（模式选择 + 提示词 + 首帧图片/参考图片）、参数、运行、输出（成片历史）全部在导演台内完成，
无需在画布上手工连线——选模式即互斥，上传图片即自动创建 LoadImage 节点并接线，提示词常驻可编辑。

侧栏迷你卡（不开全屏也能看）：LED 状态灯、段落进度轨、状态文案、done/total 计数、
参数摘要（如 `864×480 · 124f/段 · 引导22帧`）。

## 操作与底层机制对照

| 界面操作 | 底层机制 |
|---|---|
| ◐ 生成模式（文生/首帧/多参） | 写回节点「生成模式」combo 控件；切换即自动断开不相容图片连线（文生清空首帧+参考、首帧清空参考、多参清空首帧），UI 层互斥；后端复验不一致直接报错 |
| ▶ 开始生成 / 继续下一段 | `app.queuePrompt()`（存档引擎自动续接，审片模式每次只跑下一段） |
| 点项目列表条目切换项目 | 把节点「存档目录」（或「断点目录」）控件值改为该项目目录名 |
| ＋ 新建项目 | 模态框命名 → 换存档目录名（= 开新链，提示词沿用为底稿，可选勾选清空） |
| ＋ 添加一段 | 节点动态加 `提示词_N` 输入，创建并连接 PrimitiveStringMultiline；台内常驻 textarea 直接编辑 |
| ✏ 提示词（常驻 textarea） | 段落卡片内常驻文本框，输入即防抖写回画布输入（已连 primitive 走上游 widget）；失焦立即落盘；编辑中锁定重渲防丢焦；提示词哈希变化自动从该段重做 |
| 🎲 重摇此段 | 设「重跑起始段」=该段号 + 随机种子 → 排队；运行成功后自动复位为 0 |
| 📎 插视频到段 N 前 / 段后 / 尾部 | 选文件上传 `/upload/image` 到 input 目录 → 「插入视频」控件追加 `位置\|文件名` |
| ✕ 移除插入 | 「插入视频」控件删掉对应行（其后段落自动重做，这是锚定机制决定的） |
| 上传首帧图片 / 参考图片 | 选模式后选文件上传 `/upload/image` 到 input 目录 → 自动创建 LoadImage 节点（摆在采样器左侧）并连到「首帧图片」/「参考图片_N」输入；缩略图实时回显（`/view?type=input`，加载失败显 ⚠）；提示词用 `<Picture 1..9>` 引用（编号按连接顺序，后端压实） |
| ✕ 移除参考图/首帧 | 断开该输入连线并删除对应 LoadImage 节点 |
| 链参数面板（右栏） | 宽度/高度/每段帧数/引导帧数/步数/CFG/种子(🎲)/采样器/调度器/审片模式/自动保存 —— 直接写画布节点控件值，随工作流保存 |
| 成片 🗑 删除 | `POST /h3chain/delete`（二次确认） |
| ↻ / 自动刷新 | 监听 `execution_success` / `executing` 事件 + 900ms 防抖拉取状态文件 |

数据全部经 ComfyUI 自带端点读取（`GET /api/view?type=output`）：
`checkpoints/h3chain_state.json`、`checkpoints/h3chain_index.json`、
`checkpoints/<链>/manifest.json`、`output/<前缀>/h3saver_history.json`
（前缀自动读画布 H3ChainSaver 节点的「输出前缀」，无节点默认 `h3_chain`）。

## 双分支兼容（合并技术改进前后都能用）

| 能力 | main 分支 | feature 分支 / 合并插入改进后 |
|---|---|---|
| 存档目录控件 | 「存档目录」 | 「断点目录」——UI 自动按两名探测 |
| 插入视频 | 控件不存在 → 插入按钮自动隐藏，页脚提示「未合并插入能力」 | 全部插入功能可用 |
| 项目列表 | 无 `h3chain_index.json` → 用 localStorage 记忆 + 当前链兜底 | 引擎全量索引（含进度/时间） |
| 成片删除路由 | 有 `/h3chain/delete` | 无 → 首次 404 后自动隐藏删除按钮 |

即：UI 与引擎改进可按任意顺序合并，互不依赖。

## 节点适配（nodes.py 已改）

导演台一体化控制依赖节点侧的「生成模式」控件，已对 `H3SeamlessChainSampler` 做如下适配：

- 新增 combo 控件 `生成模式`（文生视频 / 首帧视频 / 多参视频，默认文生视频），位于「重跑起始段」之后、「首帧图片」之前。
- `execute` 增 `生成模式` 形参；在原有「首帧图片↔参考素材互斥」之上，新增模式一致性复验：
  - 文生视频 +（首帧图片 或 参考素材）→ 报错
  - 首帧视频 + 参考素材 → 报错
  - 多参视频 + 首帧图片 → 报错
  报错文案均提示去导演台切换模式，避免跑废。
- 链路报告头追加 `模式 X` 字段，便于排查。
- 实际 UNET（fl2va / ref2va）仍由「模型」输入决定，本控件只做模式声明 + 素材互斥，不改采样路径。
- **旧工作流兼容**：已有工作流无该控件时，加载后用默认值「文生视频」，不阻断；导演台检测到节点无该控件会提示「未合并模式适配，互斥仅本会话生效」。

## 实机验证指引

1. 双击 `同步到ComfyUI.bat` 同步插件到本机 ComfyUI → 重启 ComfyUI
2. 侧栏打开「长片导演台」→ 迷你卡应显示进度轨与状态
3. 打开全屏 → 逐项验证：
   - 模式：切到「首帧」→ 上传首帧图 → 缩略图回显；切到「多参」→ 首帧自动断开、可加参考图；切「文生」→ 图片全清空
   - 跑前：卡片空态、＋添加一段、卡片内常驻 textarea 填提示词（失焦回写画布节点）
   - 跑中：LED 变青闪烁、顶栏/页脚状态更新
   - 跑后：段落卡片出现视频（poster 缩略图）、种子/接缝/桥分指标
   - 改词重跑（直接改 textarea → 运行，哈希变化自动从该段重做）/ 重摇（成功后「重跑起始段」自动复位）
   - 插入视频上传（需引擎含「插入视频」控件）、移除插入
   - 项目切换、新建项目（沿用底稿 / 勾选清空）
   - 成片历史播放 / 下载 / 删除；报告折叠区
   - 删掉画布节点 → 只读降级，卡片仍可从 manifest 渲染

## 已知边界

- 分镜节点 `H3StoryboardChain` 暂未纳入导演台（聚焦续拍链；v2 候选做「续拍/分镜」模式条）
- 接缝体检报告文件无法枚举（零自建路由约束），沿用 state.json 的 `report` 字段展示
- 提示词草稿不落 localStorage——事实源始终是画布节点，避免双写不一致
