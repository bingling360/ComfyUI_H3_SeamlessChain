# 长片导演台（web/h3_director.js + nodes.py 状态驱动）

参照「H3 一体化总控导演台」状态驱动架构制作的全屏一体化控制台。
所有输入（模式 / 提示词 / 首帧图片 / 标签素材池）存入节点「导演台状态」JSON widget，
节点 `execute` 读取 JSON 加载素材，**不在画布上动态创建 / 连接任何辅助节点**；
素材节点走「配套默认工作流」预连线、不用时隐藏（连线常驻）。

## 架构：状态驱动（JSON widget）+ 分段处理中心

```
导演台前端 (h3_director.js)
    │
    ▼  读写 JSON 状态（v2 schema）
节点「导演台状态」widget (multiline string, socketless, advanced)
    │  { mode, prompts[], first_frame,
    │    ref_assets:[{file,label}],          // v2 标签素材池（真源）
    │    ref_images:[],                       // 兼容保留 = ref_assets 的文件序列
    │    segments:[{ scene_prompt, character_prompt,
    │                seconds,                 // 本段时长（秒），空 = 跟随全局
    │                refs:[标签,...] }] }      // 本段引用的素材标签，空 = 引用全部
    ▼  execute 读取
H3SeamlessChainSampler.execute()
    ├─ 有 JSON 状态 → 从 input 目录加载图片，提示词从 JSON 读
    │  ├─ segments 场景/角色提示词 → 组合到各段主提示词前（场景 → 角色 → 主提示词）
    │  ├─ segments[i].seconds → 每段独立时长（17k+5 帧网格吸附）
    │  ├─ segments[i].refs → 每段素材子集，[[标签]] 按段压实重编号为 <Picture k>
    │  └─ 宽高比+百万像素 → 32 倍数对齐画布（短边≤768、长边≤1344）
    └─ 无 JSON 状态 → 回退原有画布接线（向后兼容旧工作流）
```

- **模式互斥在 JSON 层完成**：切模式即清空不相容字段（文生清空首帧+参考图，首帧清空参考图，多参清空首帧）
- **后端复验**：模式与素材不匹配直接报错（避免跑废）
- **提示词常驻可编辑**：段落卡片内 textarea，输入即防抖写回 JSON 状态（350ms），失焦立即落盘 + 刷新
- **图片上传后文件名存 JSON**：缩略图直接从 `/api/view?type=input` 生成 URL，同时点亮配套工作流对应 LoadImage 节点

## 官方参数化（本次更新）

### 1. 分辨率：宽高比 + 百万像素（官方 Resolution Selector 同款）

- **宽高比**：`自定义 / 21:9 / 16:9 / 9:16 / 4:3 / 3:4 / 1:1`（默认 16:9）
- **百万像素**：`0.25 / 0.5 / 0.75 / 1.0`（默认 0.5；0.25 草稿、0.5 快速预览、0.75 均衡、1.0 官方原生）
- 换算规则（`_resolve_canvas`，nodes.py 与前端同源实现）：
  - 按 宽高比×百万像素 反解宽高 → **32 倍数向上对齐** → **短边≤768、长边≤1344**（H3 原生画布上限，超出自动收到上限）
  - 例：16:9 + 1.0 → **1344×768**；9:16 + 1.0 → 768×1344
- 「宽高比=自定义」时宽/高裸值直接生效（原宽度/高度控件收进高级设置，仅此模式有意义）
- 参数面板实时显示**换算徽章**：`16:9 · 1.0MP → 1344×768`

### 2. 时长：每段帧数 → 每段时长（秒），且每段独立

- 全局默认「每段时长」Float（0.5–15.0s，步进 0.1，默认 5.0s）
- 每段卡片可单独填秒数（`segments[i].seconds`，留空 = 跟随全局默认）——**不再强制全链等长**
- 内部吸附 H3 帧网格（`_snap_seconds`）：`17k+5` 帧 @24fps，最小 5 帧
  - 例：5.0s → 124 帧；10.0s → 245 帧；0.5s → 5 帧
- 段卡输入框旁实时提示 `≈124帧`；报告逐段标注 `段N=X.Xs(帧数)`
- 哈希兼容：时长变更自动触发受影响段重做，与提示词哈希机制一致

### 3. 参数分组：常规五项 + 高级设置收纳其余全部

- **常规**（右栏直改）：宽高比 / 百万像素 / 每段时长 / 种子(🎲) / 步数
- **高级设置**（折叠收纳，默认收起）：引导帧数 / CFG / 采样器 / 调度器 / 自动存档 / 存档目录 / 审片模式 / 自动保存 / 重跑起始段 / 桥帧门控 / 清晰度阈值 / 回退上限 / 接缝混合 / 混合帧数 / 锚定加噪 / 宽度 / 高度
- 「生成模式」不在参数面板——由左侧模式条控制
- 高级参数同样直写画布控件，随工作流保存

## 素材标签与段级引用（本次更新）

### 标签素材池（v2）

- 每张参考图一个标签（`ref_assets:[{file,label}]`，标签≤24 字符）
- 上传时默认标签 `图片N`，可点击缩略图下方标签直接改名（**回车/失焦提交**，重名自动加后缀）
- 快捷标签按钮：`角色1 / 角色2 / 场景1 / 场景2 / 风格 / 道具`
- **重命名同步**：改名自动更新所有段级引用 chips 与提示词无需手改（引用按标签匹配）
- 标签去重：后端加载时自动加后缀（`角色1` → `角色12`…），报告不炸
- v1 状态（只有 `ref_images`）自动迁移默认标签 `图片N`

### 段级引用（chips 勾选 + [[标签]] 语法）

- 每段卡片一行 chips 勾选本段引用哪些素材（`segments[i].refs`）；**全部不勾 = 引用全部**（与旧行为一致，避免 ref2va 链出现空参考段）
- 提示词写 `[[角色1]]`，后端按**本段勾选顺序**压实重编号为 `<Picture k>`（每段独立编号，互不挤占 9 槽上限）
- 原生 `<Picture N>` 写法继续兼容（直接按素材池全序引用）
- 段内无任何 `<Picture>` 时，后端自动在段首补参考映射行（`_reference_header`），保证 ref2va 模式参考图生效
- 引用未知标签直接报错并列出可用标签（防静默丢图）
- 引用语模板（点击插入光标处）：`插入[[标签]] / 主角出场 / 配角出场 / 场景还原 / 风格参考 / 镜头参考`

## 配套默认工作流（web/h3_default_workflow.js，本次新增）

思路参照 [ComfyUI-JZL-MiniMax-H3](https://github.com/wjluoxiao/ComfyUI-JZL-MiniMax-H3) 的预连线工作流：**连线常驻、不用时隐藏**，导演台只做点亮/熄灭，不再动态建线。

- 画布上没有 H3SeamlessChainSampler 时，导演台左栏出现「⚡ 一键载入配套工作流」按钮
- 模板内容：UNETLoader / CLIPLoader / 视频VAE / 音频VAE + 主节点（预声明全部素材输入槽）+ H3ChainSaver 成片保存
- 「素材池 · 自动管理」节点组：首帧图 + 参考图×9 + 参考视频×3 + 参考音频×3 **全部预连线**到主节点对应槽
- 素材节点状态：**有素材 = mode 0 + 展开（点亮）；无素材 = mode 2(Never) + 折叠（隐藏）**——连线与配置始终保留
- 导演台上传/删除素材 → 镜像点亮/隐藏对应节点（`syncMaterialNodes`），画布即真实管线
- 提示词不走画布（状态驱动：导演台 JSON 优先），模板不预置提示词节点

## 打开方式

- **新前端**（ComfyUI v1.x）：左侧侧栏 → 「长片导演台」Tab → 迷你状态卡 → 点「打开长片导演台」进入全屏工作台
- **旧前端**：右下角出现悬浮按钮，点击直接打开全屏工作台
- 全屏内按 **Esc** 或右上角 ✕ 关闭

## 界面结构

```
┌ 顶栏：H3·SEAMLESS CHAIN │ 长片一体化导演台 │ LED状态灯 + 当前项目 │ ✕ ┐
├────────────┬───────────────────────────┬──────────────┤
│ 左：项目与链 │ 中：段落流水线               │ 右：链参数      │
│ 项目列表     │ 状态条 + 段落进度轨           │ 宽高比+百万像素  │
│ ＋新建项目   │ 段落卡片（常驻textarea 🎲重摇 │ 每段时长/种子🎲/步数│
│ ⚡一键载入   │  本段秒数(≈帧数) 📎插视频 ✕删除│ 换算徽章        │
│ 配套工作流   │  分段处理(场景/角色/引用chips) ├──────────────┤
│ 素材池·     │  引用语模板快捷插入            │ ⚙高级设置(折叠)  │
│ 分段处理中心 │ ＋添加一段 / 尾部插入视频       │  其余全部参数    │
│ ◐模式:文生/ │ 上次运行报告（折叠）          ├──────────────┤
│  首帧/多参   │                             │ 右：成片历史    │
│  首帧图片     │                             │ 当前成片大播放器 │
│  参考图片≤9   │                             │ 历史（播放/下载/删）│
│  (带标签编辑) │                             │                │
├────────────┴───────────────────────────┴──────────────┤
│ 页脚：当前链 / 审片 / 引擎兼容提示 │ ▶ 开始生成 / ▶ 继续下一段 │
└───────────────────────────────────────────────────────┘
```

## 操作与底层机制对照

| 界面操作 | 底层机制 |
|---|---|
| ◐ 生成模式（文生/首帧/多参） | 写回 JSON 状态 `mode` 字段 + 同步节点「生成模式」combo 控件；切换即清空不相容字段；后端复验不一致直接报错 |
| ▶ 开始生成 / 继续下一段 | 先把 JSON 状态写回 widget + 同步模式控件 → `app.queuePrompt()`（存档引擎自动续接，审片模式每次只跑下一段） |
| 宽高比 / 百万像素 | 写画布 combo 控件 + 换算徽章实时显示 `AR · MP → W×H`；自定义模式才动宽/高 |
| 每段时长（全局） | 写画布「每段时长」Float 控件（0.5–15s） |
| 段卡秒数输入 | 写 `segments[idx].seconds`（空 = 跟随全局），旁标 `≈N帧`；变更触发该段起重做 |
| ⚙ 高级设置 | 折叠面板内直写画布控件（引导/采样/审片/存档/接缝/画布微调全部收纳），随工作流保存 |
| 素材标签编辑 | 缩略图下方标签输入，回车/失焦写 `ref_assets[idx].label`；重名自动加后缀；重命名同步所有段级引用 |
| 段级引用 chips | 勾选写 `segments[idx].refs`；全不勾 = 引用全部 |
| 引用语模板 | 点击在提示词光标处插入 `[[标签]]` 模板句 |
| 🎲 重摇此段 | 设「重跑起始段」=该段号 + 随机种子 → 排队；运行成功后自动复位为 0 |
| 📎 插视频到段 N 前 / 段后 / 尾部 | 选文件上传 `/upload/image` 到 input 目录 → 「插入视频」控件追加 `位置|文件名` |
| 上传首帧 / 参考图片 | 选文件上传 → `first_frame` / `ref_assets[]` 追加 → 缩略图回显 + **点亮配套工作流对应 LoadImage** |
| ✕ 移除首帧 / 参考图 | JSON 状态清对应字段 + **隐藏配套工作流对应节点**（连线保留） |
| ⚡ 一键载入配套工作流 | 确认后 `app.loadGraphData(H3_DEFAULT_WORKFLOW)` 载入模板（未保存画布会提示） |
| 成片 🗑 删除 | `POST /h3chain/delete`（二次确认） |
| ↻ / 自动刷新 | 监听 `execution_success` / `executing` 事件 + 防抖拉取状态文件 |

## 节点适配（nodes.py）

### 1. schema 官方化

- 新增 combo：「宽高比」（自定义/21:9/16:9/9:16/4:3/3:4/1:1）、「百万像素」（0.25/0.5/0.75/1.0）
- 宽度/高度转 `advanced=True`（仅自定义模式生效，换算模式自动覆盖）
- 「每段帧数」→「每段时长」Float（0.5–15.0s，step 0.1），`_snap_seconds` 吸附 17k+5 @24fps
- `execute` 开头：非自定义模式 → `宽度, 高度 = _resolve_canvas(宽高比, 百万像素)`；报告标注换算行

### 2. 导演台状态 v2

- `ref_assets` 为标签素材池真源；`ref_images` 兼容字段 = 文件序列（旧版仍可读）
- `segments[i]` 扩展：`seconds`（每段时长）、`refs`（段级引用标签列表）
- 提示词处理管线（每段）：场景+角色组合 → `[[标签]]` 按段压实为 `<Picture k>`（`_apply_label_tokens`）→ 无 `<Picture>` 自动补参考映射行（`_reference_header`）
- 引用未知标签报错并列出可用标签；标签重复自动加后缀
- 每段长度：`segments[i].seconds` 吸附网格，None 跟随全局 `每段时长`

### 3. 旧工作流迁移

- 旧工作流（宽高裸值 + 每段帧数）载入时前端自动重排 `widgets_values`（widget 增删导致按位错位）
- 宽高完全命中某 AR×MP 组合 → 迁移为该组合；未命中 → 「自定义」保持原宽高
- 已有 JSON 状态 v1（只有 `ref_images`）→ 自动补默认标签 `图片N`，段级 refs 空 = 全部引用

## 双分支兼容（合并技术改进前后都能用）

| 能力 | main 分支 | feature 分支 / 合并插入改进后 |
|---|---|---|
| 存档目录控件 | 「存档目录」 | 「断点目录」——UI 自动按两名探测 |
| 插入视频 | 控件不存在 → 插入按钮自动隐藏，页脚提示「未合并插入能力」 | 全部插入功能可用 |
| 项目列表 | 无 `h3chain_index.json` → 用 localStorage 记忆 + 当前链兜底 | 引擎全量索引（含进度/时间） |
| 成片删除路由 | 有 `/h3chain/delete` | 无 → 首次 404 后自动隐藏删除按钮 |

## 实机验证指引

1. 双击 `同步到ComfyUI.bat` 同步插件到本机 ComfyUI → 重启 ComfyUI
2. 侧栏打开「长片导演台」→ 迷你卡应显示进度轨与状态
3. **配套工作流**：空画布打开导演台 → 左栏出现「⚡ 一键载入配套工作流」→ 载入后画布应为 模型×4 + 主节点 + 保存 + 素材节点组（全隐藏折叠、连线常驻）
4. **分辨率**：16:9+1.0 徽章显示 `1344×768`；切自定义 → 宽/高输入生效；改宽高比 → 已生成段落不受影响，新段按新画布（存档内画布不一致会报错提示开新链）
5. **每段时长**：段卡秒数输入旁实时 `≈帧数`；设 10.0s → `≈245帧`；不同段不同秒数 → 报告逐段标注实际秒/帧
6. **素材标签**：多参模式上传两图 → 标签分别改 `角色1`、`场景1` → 段1 只勾 `角色1` → 段2 全勾 → 运行后报告显示两段不同的参考映射行
7. **引用语法**：段1 提示词写 `[[角色1]] 全程出镜` → 报告显示压实为 `<Picture 1>`；点「主角出场」模板应插入完整模板句
8. 其余逐项验证：提示词写回 / 模式互斥 / 缩略图回显 / 改词重跑 / 重摇复位 / 插入视频 / 项目切换新建 / 成片历史播放下载删除 / 删画布节点只读降级

## 自动化测试

- `tests/test_official_params.py`（7 项）：`_resolve_canvas` 换算、`_snap_seconds` 网格、`_apply_label_tokens` 压实、`_reference_header` 映射行、v1 素材池迁移、段哈希兼容、标签去重
- `tests/_js_harness.js`：前端 `resolveCanvas` / `snapFrames` / 旧工作流迁移重排与后端同源校验（`node tests/_js_harness.js`）
- `tests/test_node_structure.py`：schema 顺序 / 能力探测 / 桥回退 / 锚定加噪 / is_changed / 尾帧切片 / 校验（回归）

## 已知边界

- 分镜节点 `H3StoryboardChain` 暂未纳入导演台（聚焦续拍链；v2 候选做「续拍/分镜」模式条）
- 接缝体检报告文件无法枚举（零自建路由约束），沿用 state.json 的 `report` 字段展示
- 提示词草稿不落 localStorage——事实源始终是 JSON widget，避免双写不一致
- 参考视频 / 参考音频仍走画布接线（配套工作流已预置节点槽，上传/点亮 UI 待后续接入 JSON 状态）
- 生成中途换宽高比/百万像素：新段画布与存档内旧段不一致时，段间 latent 切片会形状错位而运行失败——请开新链，不要改在跑链路的画布

## 设计参考

- 分段处理中心参照 [ComfyUI-JZL-MiniMax-H3](https://github.com/wjluoxiao/ComfyUI-JZL-MiniMax-H3)：JZL 用「分段处理中心 → 场景/视频/音频调度」分发素材；本实现化为「JSON 状态 segments[] + 标签素材池 + 段级引用」，不新增调度节点，不改核心采样逻辑
- 配套默认工作流同样参照其预连线思路：素材节点连线常驻、mode=2+折叠隐藏，导演台只点亮/熄灭，不做动态建线
- 分辨率/时长参数对齐官方 Resolution Selector 与帧网格（17k+5 @24fps），保证与原生工作流产物规格一致
