# 更新说明：段级独立镜头 / 插入视频段 / 合并导出

针对长片实拍节奏的三个细粒度功能补全——不是所有镜头都要续拍衔接、不是所有素材都要重新生成、不是所有拼接都要重跑链。全部落在导演台交互层 + 既有存档引擎上，不新增依赖、不动官方协议。

---

## 一、段级独立镜头（与上段断链）

**痛点**：链式续拍默认每段都吃上段尾帧引导桥——但转场到新场景、闪回、平行叙事的镜头和上一段毫无关联，引导桥反而把新场景的起步构图拖拽变形，接缝处理也只是在硬切缝上做无用的混合。

**用法**：导演台段落卡片 → 展开「分段处理」→ 勾选 **「🔗 独立镜头（与上段断链）」**。开启后一键全断（作用深度按用户选定「一键全断」方案）：

| 机制 | 独立镜头行为 |
|---|---|
| 引导桥注入 | 不注入（video + audio 双路都断），本段从纯噪声起步 |
| 头部裁剪 | 不裁头——普通续段要裁掉 `引导帧数` 重叠桥，本段整段输出全保留 |
| 接缝精修 / 像素混合 | 跳过（缝两侧本来就是两镜硬切，混合只会重影） |
| 段首响度对齐 | 跳过 |
| 接缝指标（帧差/响度/桥帧分） | 记 N/A，不参与链路总结统计 |
| 上段尾帧桥构造 | 上段不再为本段构造尾帧桥（`_next_wants_bridge` 判定），上段本身生成不受影响 |

**可见性**：卡片「独立镜头」徽章 + 顶部段落导轨橙色条纹标记；报告开头汇总 `独立镜头：N 段与上段断链（无桥接/硬切，段 2、5）`，逐段行尾注 `· 独立镜头（断链）`。

**重做语义**：断链标记进段哈希（`unlink|` 前缀）→ 勾选/取消勾选自动从该段起重做（其后段级联）。开关只管「本段与上段」的衔接，本段与下段的衔接由下段的开关决定。

## 二、插入视频段（外部素材进链）

**痛点**：手头有实拍/别处生成的视频，想让它出现在成片里、并让模型从它的结尾续拍下一段——此前唯一的入口「序章」只能放链首且只能一个。

**用法**：段卡片「分段处理」区点 **「尾部插入视频」**（或各段位的「上传插入视频」按钮）→ 选本地 mp4 → 上传进 input 目录、按链位插入。一条链可插多段。

- **链位语义**：`pos` = 提示词段 + 插入段混排后的 1-based 序号（不含序章）。链位 2 = 第 1 个提示词段之后；其后提示词段自动顺延——段落导轨与卡片顺序即最终成片顺序；
- **进成片**：画面 VAE 重编码（center-cover 到项目画幅，与全链一致），音轨保留原声（无音轨按静音补）；超「每段帧数」只取前段，帧数向下对齐 17k+5 网格，最少 5 帧（约 0.2 秒 @24fps）；
- **指导下一段**：插入段结尾按官方 keyframe 协议构造尾帧桥（与生成段同款，含音频窗），下一段从它的结尾无缝续拍；
- **与上段硬切**：外部素材画面固定，精修/混合只会重影，该缝不处理（指标记 N/A）——这是设计决定，不是缺陷；
- **存档**：插入段占全局段号（序章占 0 时其后 +1），latent 落盘 `seg_NNN.pt`、分段 mp4 / 缩略图照常，续跑秒级回放；manifest 记 `inserts: [{slot, file}]`，提示词清单对应行为 `[插入视频] 文件名` 占位（回写提示词时占位行原样保留、槽位不错位）；
- **重做**：插入段哈希 = **文件指纹（mtime + size）**——换文件 / 同名覆盖上传 = 从该段起重跑；删除插入 = 其后段落重做（`removeInsert` 触发哈希变化）。

**与序章的区别**：序章只能在整个链最前面（第 0 段，走节点接线 `LoadVideo`）；插入视频任意链位、可多段（导演台状态 `ds.inserts`，不占节点输入口）。

## 三、合并导出（任意素材拼一部片子）

**痛点**：出预告片 / 抽精选 / 补剪某几段，此前只能整链重跑或手动外部剪辑。

**用法**：导演台顶部点 **「⧉ 合并模式」**——

1. 段落导轨上勾选要拼接的段（**已完成段才可勾**，含序章/插入段；按链位顺序自动排列，无需手动排序）；
2. 可选「＋ 上传外部视频」（别的项目成片、实拍素材）追加到清单末尾；
3. 点「合并导出」→ 后端 `POST /h3chain/merge` 流式拼接，产物落项目目录 `merged_时间戳.mp4`；
4. manifest.merges 追加记录（含清单明细），成片区新增「合并片段」区块逐条可看可播可删（最新在前，展示最近 10 条）。

**工程细节**：

- **流式编码**（`media.concat_av_mp4`）：PyAV 逐源解码→reformat→编码，不整链载入内存；H.264 + AAC、24fps 统一，crf 20；异画幅源 reformat 到目标画幅（按项目参数 width/height，参数缺失回退首个源实际尺寸不缩放）；**无音轨的源按视频时长补静音**——输出始终有音轨，参数统一；
- **源解析**（`projects._merge_sources`）：`{"seg": n}` 按 1-based 全局槽位映射 `manifest.videos`（前端段落卡片链位即此编号）；`{"file": f}` 先查项目目录（final_* / merged_* / 任意 mp4）、再查 input 目录，路径过 `safe_name` 防穿越（至多两级子目录）+ realpath 复核；
- **路由**：`POST /h3chain/merge`，编码是 CPU 密集长任务，放线程池跑（不阻塞 aiohttp 事件循环拖死界面）；`ValueError`（清单/缺文件）→ 400，`RuntimeError`（编码失败）→ 500；`.part` 临时文件失败自清理；
- **写回竞态**：编码耗时分钟级，写回前重读 manifest 只追加 merges，避免覆盖期间生成主循环落盘的新段进度；
- **不动链**：勾选态纯内存（不落 ds / 不触发任何重做）；合并模式进行中生成按钮暂停，防误触跑链。

## 兼容性

- **旧存档**：`unlink` 默认 false、`inserts` 默认空——未用新功能的旧项目行为与之前逐帧一致；默认段（无时长/引用/断链定制）的段哈希公式不变，旧档续跑不受影响；
- **manifest 前向兼容**：`unlink` 状态存于导演台状态（`ds.segments[i].unlink`）并入段哈希；`inserts` 存 manifest（读档时按 `slot → pos` 还原导演台状态）；`merges` 键缺失时按空处理；
- **提示词回写**（`save_prompts`）：序章占位头、插入段占位行按槽位自动补回，纯提示词回写不让槽位错位；
- **节点控件零改动**：三个功能全走导演台状态与 HTTP 接口，不动节点 widgets 顺序（旧工作流 JSON 直接兼容）。

## 验证

- 全量测试（带 torch venv）：**17 个测试文件全绿**，其中新增与扩展——
  - 新增 `test_merge.py`：流式拼接帧数守恒 / 异画幅 reformat / 静音补齐（含全静音源）/ 失败清理（无产物无 .part 残留）/ `merge_project` 清单解析与防穿越（项目目录优先、越界/穿越不触磁盘）/ manifest.merges 追加记录与列表摘要；
  - 扩展 `test_routes.py`：merge 端点错误映射（清单/项目/段号校验 → 400，编码失败 → 500，失败不落 merges）；路由注册表断言补入 `/h3chain/merge`；
  - 扩展 `test_projects.py`：`save_prompts` 插入段占位行按槽位回写（序章 + 插入混排不错位）；
  - 扩展 `_js_harness.js`：unlink 布尔归一 / 回写回环 / 缺省 false；inserts 去重过滤排序；提示词段 + 插入段混排序列（头插 / 尾插 / 中插）；合并模式导出清单组装；
- `node --check` 三份前端 JS 语法全过；
- 部署：本地跑「同步到ComfyUI.bat」→ 重启 ComfyUI（控制台应见 9 条路由含 `/h3chain/merge` 的注册日志）→ 浏览器 Ctrl+F5 强刷。

## 上手清单（建议实测顺序）

1. 新建项目写 3 段提示词，第 2 段勾「🔗 独立镜头」→ 跑完看报告「独立镜头：1 段与上段断链」，成片该处应为干净硬切；
2. 段 2 与段 3 之间插入一段本地视频 → 运行 → 成片顺序含插入段原声，段 3 应从插入段结尾续拍；
3. 跑几段后进「⧉ 合并模式」勾 2 段 + 上传 1 个外部视频 → 合并导出 → 项目文件夹出现 `merged_*.mp4`，成片区「合并片段」可播放；
4. 换掉插入的视频再运行 → 报告应提示「检测到该段提示词已修改，从段 N 起重新生成」。
