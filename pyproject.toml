[project]
name = "ComfyUI_H3_SeamlessChain"
description = "MiniMax H3 多段视频段间引导（无缝续拍）单节点：上段尾帧 latent 直切钉入下段，官方原生协议，零 monkey-patch，零额外依赖。"
version = "0.1.0"
license = { text = "Apache-2.0" }
dependencies = []

[project.urls]
Repository = "https://github.com/local/ComfyUI_H3_SeamlessChain"

[tool.comfy]
PublisherId = "local"
DisplayName = "ComfyUI_H3_SeamlessChain"
