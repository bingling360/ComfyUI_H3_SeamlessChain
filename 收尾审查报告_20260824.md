# 二采修改收尾审查报告

- 日期：2026-08-24
- 分支：`main`
- 基线：`28a40e9`（`origin/main`）
- 范围：接手时全部未提交修改、相关二采调用链、前端状态迁移、媒体编码、测试与说明文档
- 结论：代码级收尾完成，自动化回归全绿；发布前仍建议在真实 MiniMax H3 模型与目标显卡上跑一段 GPU 冒烟测试

## 一、接手时的修改范围

接手时有 9 个已跟踪文件被修改：`checkpoint.py`、`media.py`、`metrics.py`、`nodes.py`、`tests/test_media.py`、`tests/test_metrics.py`、`tests/test_upscale.py`、`upscale.py`、`web/h3_director.js`；另有未跟踪 `.zcode/`，内容为 T8 参考源码和 AI 规划草稿。

本轮重点审查了以下行为：

1. 新增二采参数的前后端默认值、钳制、旧 JSON 迁移和条件化指纹；
2. 多轮递降精化、STG、latent / 像素锐化、增益重试和独立采样器 / 调度器；
3. 二采分段编码、最终高清成片拼接、音频和帧数守恒；
4. manifest 记录、失效判定、同名覆盖、重置 / truncate 和最终成片门控；
5. 当前 ComfyUI 的 `calc_cond_batch`、post-CFG hook、DiT replacement、`ModelSamplingAV` 和官方 `MiniMaxH3SigmaShift` 契约。

## 二、发现并完成的收尾

### 1. 高清成片开启抖动后可能整片发白

`concat_av_mp4` 把 `vf.to_ndarray(format="rgb24")` 得到的 `uint8 0..255` 直接传给只接受 `float 0..1` 的抖动量化函数，像素再次乘 255 后几乎全部截到 255。修复后：

- Bayer 抖动只在原始浮点帧第一次量化为 8bit 时执行；
- 最终拼接不对已编码 8bit 分段重复抖动，保留分段已有纹理；
- `dither_quantize` 对整数输入按 dtype 满量程归一化，未来误用也不会截白；
- 新增真实 PyAV 亮度阶梯回归，验证 HQ 拼接后非全白且亮度层次守恒。

### 2. 最终高清成片没有完整沿用编码档

原新增编码档只作用于二采分段，最终高清成片仍可能以默认 `CRF 20 / veryfast` 重编码。现已将所选档位的 CRF、preset、AQ 透传到最终拼接，并补单测钉住「高清 = CRF 16 / medium / AQ 3」。抖动在分段首次量化时完成，最终拼接不重复量化。

### 3. STG 会覆盖已有模型补丁

原 `_stg_model` 未检查模型已有 `sampler_post_cfg_function` 或目标 `double_block` replacement，可能与其他引导节点 / 模型补丁静默叠加或覆盖。现已：

- 安装前拒绝已有 post-CFG hook；
- 安装前拒绝目标 double block 已有 replacement；
- 弱分支执行前再次检查运行时 replacement 冲突；
- 缺少正向 conditioning 时给出明确错误；
- `scale=0` 严格原对象直通；
- 用 ComfyUI 最小 API 桩验证正常弱分支数学和所有 fail-closed 分支。

### 4. shift 的 ComfyUI 兼容错误不清晰

对照当前官方 `MiniMaxH3SigmaShift` 后确认 clone、`ModelSamplingAV + CONST`、对象补丁和 transformer options 双键口径一致。补充了：

- 缺少 `ModelSamplingAV` / `CONST` 时的升级提示；
- 非原生 H3 模型缺 `model_config` 时的明确错误；
- 主模型 `audio_shift=None` 时按 H3 默认 3.0 回落；
- noise scale 保留和主模型零改动单测。

### 5. 既有测试与指标问题

- 修复 `tests/test_media.py` 的多余右括号，使测试集可收集；
- 修复 OpenCV API 拼写：`calcOpticalFlowFarneBack` → `calcOpticalFlowFarneback`；
- `tests/test_metrics.py` 改用固定独立 `torch.Generator`，避免全套运行时其他测试消耗全局 RNG 导致阈值偶发波动；
- 修复 dither 测试的 NumPy 标量弃用警告。

### 6. 文档和发布边界未收尾

- 新增 `更新说明_二采抗糊抗条纹.md`，说明默认值、建议档、成本、冲突保护、编码行为与显存风险；
- README 同步新增参数，并明确最终成片沿用编码档；
- 修正旧说明中已过时的 `tail_refine_args` 换算公式和 LOW_VRAM / OOM 降级描述；
- 更新导演台 JS 版本标记，便于识别浏览器旧缓存；
- `.zcode/` 加入 `.gitignore`：保留本地参考内容，但不把 AI 草稿和外部参考源码纳入发布。

## 三、审查确认的实现语义

- 新增增强默认关闭或保持标准档；默认值不进入二采指纹，旧二采记录不会无故失效。
- STG、轮数 / 衰减、两种锐化、非标准编码、独立采样器 / 调度器、重试目标均按生效条件进入指纹。
- 多轮精化每轮使用递降 σ 和派生种子；重试从原始放大 latent 重跑整链并按细节增益取优。
- 二采音频仍使用原音轨；重采样产生的音频 latent 被丢弃。
- `write_record` 新增 `sharp` 只用于观测，不参与失效判定。
- 最终高清成片只有在全链记录有效、尺寸一致时生成；否则按原逻辑回退基础成片。
- 画布 / 显存预检和采样 OOM 属致命条件，会停止整链，避免高清 / 基础混排；其他单段异常仍可降级该段。

## 四、验证结果

最终验证命令与结果：

```text
python -m pytest -q
187 passed in 15.16s

python -m compileall -q .
通过

git diff --check
通过

node --check web/h3_director.js
通过

node tests/_js_harness.js
all js harness checks passed
```

专项二采 / 媒体 / 拼接回归为 `54 passed`。PyAV 测试使用真实 `libx264 + AAC` 编码和解码，覆盖帧数、画幅、音频时长、HQ 参数和亮度保持，不是纯 mock。

测试依赖安装在系统临时目录 `%TEMP%/codex_h3_testdeps_py313`，未修改项目依赖声明或仓库环境。

## 五、剩余风险与发布建议

自动化环境没有加载真实 ComfyUI MiniMax H3 权重，也没有执行高清 latent 的 GPU 采样，因此以下属于外部运行验证，不是当前测试能消除的风险：

1. 目标 GPU 上 1.5× / 2× 高清 latent 的实际峰值显存；
2. STG 块 25 在具体模型权重上的观感、耗时和伪影边界；
3. 多轮 + STG + 重试组合下的总耗时；
4. 真实渐变素材经分段编码和最终再编码后的 banding 改善程度。

发布前建议只做一段冒烟：先用「高清编码、其余增强关闭」验证画幅 / 颜色 / 成片，再单独开启 `STG 0.5、块 25` 或第二轮精化。不要第一次就全开。同步到 ComfyUI 后需重启后端并强刷浏览器；控制台版本应显示 `20260824+upscale-antiblur`。

本轮未执行同步脚本、未修改外部 ComfyUI 安装、未创建 git 提交。
