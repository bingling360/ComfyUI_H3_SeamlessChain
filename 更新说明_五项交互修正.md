# 更新说明：五项交互修正（百万像素 / 尾帧上传 / 0?段 / 多段提示词 / 分段存储）

针对导演台上手实测反馈的五个小问题逐一修正，全部为交互与模板层改动，生成链路逻辑不变。

---

## 一、百万像素：0.1–2.0 共 20 档，官方箭头微调同款

- 后端 `nodes.py`：「百万像素」由 4 档下拉改为 **Float 浮点控件**（min 0.1 / max 2.0 / step 0.1），与官方 Resolution Selector 的 megapixels 一致，画布上用上下箭头逐档微调；
- 前端导演台右栏「链参数」同步适配：数字输入框带 min/max/step，箭头同样 0.1 步进，右侧带 `MP` 单位与档位说明 tooltip；
- `_resolve_canvas` 补了一个边界修复：0.1 步进后 2.0MP 正方形 ceil 对齐会顶破短边上限（800>768），现按长短边各自回落到 768/1344（均为 32 倍数，对齐不破坏）；前端 `resolveCanvas` 镜像同步；
- 旧工作流兼容：原下拉字符串值（"0.5" 等）加载后照常换算；旧 0.25/0.75 档画布反推不中组合时回落「自定义」宽高，画布尺寸不变。

## 二、首尾帧模式补上「尾帧上传」

- 导演台素材区（左下）在**所有模式**下都显示「尾帧锚定」卡片：上传 / 替换 / 移除，与首帧卡片同款交互；
- 尾帧锚定 = 身份锚点（last_frame keyframe），任意模式可用，不随模式切换清空；不上传即不锚定，行为与之前一致；
- 后端已支持导演台状态 `last_frame` 字段：有值时优先于画布接线，从 input 目录加载；
- 配套工作流模板新增隐藏「尾帧图」LoadImage 节点（预连线到主节点「尾帧锚定」槽，上传时自动点亮）；**注意：主节点输入槽顺序因此修正**——「尾帧锚定」插在「首帧图片」之后（槽位 5），其后 起始视频/起始音轨/提示词组/各参考组 全部 +1，与后端 schema 完全对齐（旧模板此槽缺失，属模板 bug）。

## 三、「0/? 段」显示修正

「?」出现于 manifest 没有段落数的场合（新建项目 0 段草稿 / 尚未配置提示词），旧文案直接显示 `0/? 段` 造成困惑。现改为按语义显示：

- 侧栏迷你卡：有段落 → `done/total 段`；无段落 → 「0 段 · 待添加」；
- 项目列表：无段落项目显示「未配置段落」+「草稿」徽章，不再出现 `0/?`；
- 读档提示：`已读档「项目名」（草稿，未配置段落）`；
- 计量条（存档目录下方）：无段落时显示「未配置段落（提示词每行一段）」（原此处仍残留 `0/? 段`）。

## 四、多段提示词输入方式说明（1–64 段）

提示词从导演台中栏「段落流水线」卡片输入，**不限于 3 段**：

- 每张卡片 = 一段，点底部「＋ 添加一段」即可追加（上限 64 段）；
- 画布上的「提示词·1..3」隐藏节点只是镜像兼兜底（没有导演台状态时按画布内容直接跑），超过 3 段的部分全在导演台管理；
- 中栏标题、添加按钮 tooltip、配套工作流说明均已写明此规则。

## 五、分段视频存储位置统一到项目文件夹

- **修正前**：配套工作流里有一条「分段打包（每段一个视频）→ SaveVideo → output/h3_segments/」链，与项目系统并存，每段视频存两份、位置分散，导演台段卡片却不显示这条链的结果；
- **修正后**：模板移除该冗余链（主节点默认「自动保存=分段+成片」），每段视频统一存进项目文件夹 `output/h3_projects/<项目名>/seg_NNN.mp4`，manifest 记录文件名；
- **段卡片显示**：已完成段的卡片缩略区自动显示该段视频（可直接播放）/缩略图——读取的就是项目文件夹里的分段文件；需要另行编码导出时，手动连主节点「分段图像 / 分段音频」输出到 CreateVideo 即可；
- 旧存档缺 `videos` 键时无需重跑：下次运行触发纯回放兜底回写自愈。

## 验证

- 全量测试（带 torch venv）：16 个测试文件全绿；`test_official_params` 新增 20 档 × 6 种宽高比的 32 倍数与上限断言，`test_node_structure` 断言浮点控件 min/max/step；
- JS harness：新增 尾帧图节点/槽位5 连线/无遗留分段打包链/浮点默认值 断言，864×480 反推命中 16:9+0.4（20 档细分后的正确迁移）；
- 部署：本地跑「同步到ComfyUI.bat」→ 重启 ComfyUI → 浏览器 Ctrl+F5；旧配套工作流建议用「⚡ 一键载入配套工作流」重新载入（槽位顺序已修正）。
