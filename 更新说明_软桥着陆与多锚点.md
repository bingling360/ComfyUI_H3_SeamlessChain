# 更新说明：桥区软着陆 / 音频接缝软过渡 / 段中锚点

依据 ComfyUI **v0.34.0（2026-08-26）** 对 MiniMax H3 桥接行为的两处上游改进，给长视频接续加了一条新路径。三项能力全部以**实验开关**交付，默认关闭，不满足版本要求自动回退，节点控件与输出契约零改动。

---

## 一、上游改了什么（已从主干源码逐条核实）

### 1. 官方 `MiniMaxH3AddGuide`（`comfy_extras/nodes_minimax_h3.py`）

新增节点，可在**任意帧**锚定图像 / 音频引导，并**串联多个节点形成多锚点**：

- `frame_idx` 默认 0、范围 ±9999，**负值自尾部计数**（`frame_count + frame_idx`）
- 锚定片段帧数：`<5` 帧只取首帧（1 帧锚），否则**向下对齐 17k+5**
- 越界校验：`resolved_frame_index + guide_frames > frame_count` 即报错
- 音频按剩余时长裁剪：`max_rt = floor(audio_t - FRAME_RESCALE * resolved_frame_index)`
- 多锚点：`keyframes = list(positive[0][1].get("minimax_keyframes", [])); keyframes.append(...)`

### 2. 模型层逐 token 软噪声掩码（`comfy/ldm/minimax/model.py`）

`MiniMaxH3Model.forward(..., denoise_mask=None, audio_denoise_mask=None)`，`_forward` 里：

```python
m = mask_row_values(denoise_mask[0, 0], latent_t, lat_h, lat_w)   # 每 2×2 patch 行一个值
rows_t = (1.0 - m * sigma).clamp(max=t_pin_v)                     # t_pin_v = max(t_v, 0.999)
# 音频侧：m = audio_denoise_mask[0,0].reshape(-1)，长度 audio_t*2，clamp 到 1.0
```

即掩码值 m：**0 = 完全保留**（该行 timestep 钉在 0.999 / 1.0），**1 = 完全生成**，可为 0~1 软值。

### 3. 掩码入口是 `latent["noise_mask"]`，不是 conditioning

这一点与常见直觉相反，务必记牢。`nodes.py::common_ksampler` 里是：

```python
noise_mask = None
if "noise_mask" in latent:
    noise_mask = latent["noise_mask"]
```

也就是官方 `SetLatentNoiseMask` 的同一入口。`CFGGuider.sample` 随后支持 **NestedTensor 双掩码**：

```python
if denoise_mask.is_nested:
    denoise_masks = denoise_mask.unbind()[:len(latent_shapes)]
for i in range(len(denoise_masks)):
    denoise_masks[i] = comfy.sampler_helpers.prepare_mask(denoise_masks[i], latent_shapes[i], device)
```

配合 `KSamplerX0Inpaint.__call__`，每步把掩码为 0 的行拉回 `latent_image`：

```python
latent_mask = 1. - denoise_mask
x = x * denoise_mask + scale_latent_inpaint(...) * latent_mask
out = self.inner_model(x, sigma, ...)
out = out * denoise_mask + self.latent_image * latent_mask
```

另外 `process_conds` 会把 `denoise_mask` 与 `latent_shapes` 一起交给 `extra_conds`——这正是模型层能同时拿到 `denoise_mask` / `audio_denoise_mask` 的路径。

---

## 二、软桥：从"重画一遍再裁掉"到"接着上段尾画"

### 现状（钉桥 + 裁头）

每段采样 `段长 + ctx` 帧（ctx 默认 22），上段尾 latent 只作为 keyframe 进 conditioning，采样后把头 ctx 帧**整个裁掉**。两个代价：

1. 白算 ctx 帧（22/146 ≈ 15%）；
2. 接缝第一帧是模型自由生成的，与条件里的上段尾只有**软关联**——缝差全靠重摇兜。

### 软桥

1. 把上段尾 latent 写进本段初始 latent 头部（`bridge.build_initial_latent`）；
2. 用逐 token 掩码把这段标成"已有内容"（`bridge.video_hold_mask` / `audio_hold_mask`）；
3. 采样帧数改为 `段长 + 钉住帧数`，裁掉的量也从 ctx 降到钉住帧数。

于是模型看到的**即时上文就是上一段的真实最后几帧**（干净行、时间轴位置正确），本段第一帧是真正的续画，而不是重画。默认钉住 9 帧（3 token），每段少算 ctx−9 帧。

### 反直觉结论：默认不做"斜坡"，而是整窗钉死

计划里原本设想的是"桥区 0→1 的软斜坡"。读源码后该设想被否掉：

- 钉住区**之外** `latent_image` 是空的。掩码 m<1 时 `out = out*m + latent_image*(1-m)` 会把行拉向**全零 latent**——黑帧 / 静音。所以"钉住区外做斜坡"是错的。
- 钉住区**内部**若提前释放，模型看到的结尾就不是上段真实最后一帧，衔接反而变差。

故默认 `释放曲线 = hold`：整窗 m=0（严格钉住），窗外一律 m=1。`linear / smoothstep / ease_in` 三档保留，仅作 A/B 对照（让钉住窗尾部提前放开），并可在报告里直接看到用的是哪条曲线。

### 音频

音频头部按 `audio_tokens_for_frames(钉住帧数)` 同帧钉住并同样裁掉 → 接缝**逐帧连续**。`qc.loudness_align_head` 新增 `strength` 参数（1.0 = 现状、0.0 = 不干预）；软桥下 `音频接缝软过渡` 开关默认给 0，需要时可回加。

---

## 三、多锚点与任意帧引导（对齐官方 AddGuide 语义）

新增 `guides.py`（纯函数、无 torch/ComfyUI 依赖），把所有锚点构造收敛到同一套语义与守卫：

| 函数 | 语义 |
|---|---|
| `resolve_frame_index` | 负索引自尾部计数 |
| `clip_guide_frames` | `<5` → 1 帧锚，否则向下对齐 17k+5 |
| `validate_anchor` | 越界判定（不抛异常） |
| `crop_audio_for_anchor` | `max_rt = floor(audio_t - FRAME_RESCALE * idx)` |
| `build_keyframe` | 只放非 None 的分支（模型层按键预留行） |
| `prepare_anchor` | 一站式：解析 → 校验 → 裁音频 → 组装；越界返回 `(None, 原因)` 软降级 |
| `audit_keyframes` | 只读体检，输出越界告警 |
| `mid_anchor_index` | 段中锚点帧位 |

- **段中锚点**（新实验项）：在段中部再钉一个锚，素材复用本段已有的**头锚 latent**（有首帧图引用）或**记忆锚 latent**（E2 开启），不引入新素材源；位置比例可调（默认 0.5）。构造全程走 `guides.prepare_anchor`，越界自动跳过并写报告。
- **体检只报不拦**：`audit_keyframes` 对已有锚点（段首桥 / 尾锚 / 记忆锚）只输出告警。历史上已有锚点若被静默丢弃，旧存档续跑就不再逐帧一致了——这是刻意保留的保守行为。

---

## 四、能力探测与降级

`bridge.probe_soft_bridge()` 在进程内探测一次（只读、不加载权重），返回 `(等级, 原因)`：

| 等级 | 含义 | 触发条件 |
|---|---|---|
| 2 | 采样器 + 模型逐行 timestep（完整软桥） | `CFGGuider.sample` 支持 `is_nested` 掩码 + `common_ksampler` 读 `latent["noise_mask"]` + 掩码整形探针通过 + `MiniMaxH3.extra_conds` 透传 `audio_denoise_mask` |
| 1 | 仅采样器噪声掩码（保留生效，模型不知情） | 上面前三项满足，模型层参数/透传缺一 |
| 0 | 不可用 → 走现状「钉桥 + 裁头」 | 任一项前置不满足 |

降级路径有三层，都不会让段长与裁剪量对不上：

1. **整链**：等级 0 → 每段预检不通过，`seg_len` 与 `skip_f` 完全按现状算，报告首部注明。
2. **整段**：预检不通过（钉住帧数不足 1 token / 上段无桥 / 重摇模式为「仅锚下段·无锚」）→ 本段走现状段长。
3. **装配异常**：仍按钉住帧数裁头（形状一致），只是没有钉住效果，报告注明原因。

新增 `tools/probe_bridge_caps.py`，AutoDL / 本机都能直接跑（只读、秒级），输出版本号、AddGuide 存在性、模型签名、extra_conds 透传情况、掩码整形探针与最终等级，可整段贴回 issue。

---

## 五、改动清单

| 文件 | 改动 |
|---|---|
| `bridge.py` | **新增**。能力探测、钉住计划、video/audio 逐 token 掩码、初始 latent 装配、`apply_soft_bridge` 一站式入口 |
| `guides.py` | **新增**。官方 AddGuide 锚点语义与守卫（纯函数） |
| `grid.py` | 补 `token_start_frames` / `token_center_frames` / `snap_frames_to_tokens`（纯数学，无新依赖） |
| `nodes.py` | 主循环接入软桥（预检 → `seg_len` → 装配 → 采样 → 报告）；`_apply_guide` 增 `mid_kfs`；段中锚点与锚点越界体检；响度对齐强度 |
| `experiments.py` | 新增三项实验定义：`soft_bridge` / `audio_seam` / `mid_anchor` |
| `qc.py` | `loudness_align_head` 增 `strength`（1.0 逐字节等价于现状） |
| `tools/probe_bridge_caps.py` | **新增**。能力自检脚本 |
| `tests/test_bridge.py`、`tests/test_guides.py` | **新增**。共 18 例 |
| `tests/test_grid.py`、`tests/test_routes.py`、`tests/test_node_structure.py` | 补网格助手、实验端点数量、节点契约断言 |

**未改动**：节点控件顺序与六个输出（旧工作流 JSON 零改动）；`cond_video_rows_guard` / `cond_audio_rows_guard` / `step_cond_noise_guard` / `full_bridge_supported()` 等兼容兜底全部保留；`checkpoint.py` 未改——桥模式与开关状态已由**实验指纹双向比对**覆盖（关→开、开→关、换参数都判不一致并要求新目录），比新增普通键更严格。

---

## 六、验证

- 新增单测全绿：`tests/test_guides.py`（10 例）、`tests/test_bridge.py`（11 例）、`tests/test_grid.py` 网格助手（2 例）。
- 全量：`pytest tests/` → **243 passed, 3 skipped, 1 failed**；失败项 `tests/test_upscale.py::test_try_final_gating` 是**改动前就存在**的失败（`upscale.py` 与该文件本次均未改动，单独跑也失败），未在本轮范围内处理。
- 节点契约断言：widgets 顺序与六个输出未变，且新增断言确认三项新能力只走实验开关、不落地为控件。

### 建议的实测顺序（AutoDL）

1. `python tools/probe_bridge_caps.py` → 记下软桥等级（贴回报告）。
2. 3 段短链，先关软桥跑一条基线，记 `接缝平均帧差` / `光流 z`。
3. 开「桥区软着陆」（默认 9 帧 / hold）同参数重跑 → 比对两项指标与单段采样耗时（每段应少算 13 帧）。
4. 开「音频接缝软过渡」（强度保持 0）→ 听接缝是否有台阶；有残留再把强度调到 0.3~0.5。
5. 开「段中锚点」跑长段（≥56 帧/段）→ 看段内后段是否更稳。
6. A/B 汇总可用 `tools/ab_report.py`（读 `manifest.seam_metrics`）。

---

## 七、安全审查（改动引入 bug 的可能性）

本轮改动最危险的不是"崩"，而是**静默产出坏片**。按这个标准逐条过了一遍：

### 已修：回放分支裁剪量不同步（会导致成片凭空变短）

软桥段存下的是「段长 + hold」帧的 latent，但回放分支当时仍按 `ctx` 裁 → **多裁 (ctx − hold) 帧**，成片每段变短、时间线错乱，**且不报任何错**。这是本轮唯一的真 bug。

修法是把裁剪决策收敛成纯函数 `bridge.resolve_crop(soft_hold, ctx, has_source)`，**回放分支与生成分支强制共用同一个 `skip_f`**，并用单测锁住"现状路径取值与改动前完全一致""软桥路径 skip_f == 采样额外帧数 == 钉住帧数"两个不变量。

### 已加固的四处

| 隐患 | 处理 |
|---|---|
| 桥 latent 比钉住计划短 → 多出的行被钉到**全零 latent**（黑帧/静音） | `plan_for_guide` 按桥的实际长度收窄钉住区 |
| 钉住计划超过**本段长度** → 整段被钉死 = 整段是上段尾的复制 | `apply_soft_bridge` 再按 `video_t.shape[2]` 收窄一层 |
| E4 用「缝区独占纯噪声」重采过渡窗，会把刚钉住的上段尾衔接**冲掉** | 软桥段跳过 E4（两者目标相反，不叠加） |
| 装配异常被 `except` 静默吞掉 | OOM 不降级、直接抛；其余降级但**写进报告**，不留无声失败 |

### 已核实安全的点

- **存档不会混用不同桥模式的段**：桥模式与参数全在实验指纹里，而 `assert_match` 对 `experiments` 是**双向严格比对**——关→开、开→关、换钉住帧数、甚至 `H3_EXPERIMENTS=0` 强制全关，都会判不一致并要求换目录。比新增普通键（缺键按相等处理，自定义目录会静默复用）更严。
- **二采安全**：`upscale.py` 用 `frames[skip_f:skip_f+vis_len]`，与主循环 `_decode_crop` 同口径，`skip_f` 变小只是裁得少，不会错位。
- **重摇安全**：软桥装配在 `while` 循环外，各次 attempt 只换种子、钉住内容不变（正确语义）；`reroll_max` 依赖 `skip_f` 非零，软桥下仍成立。
- **现状路径逐字节一致**：`resolve_crop(0, ctx, ...)` 与原 `skip_f` / `seg_len` 公式完全等价；`loudness_align_head(strength=1.0)` 是 `db * 1.0`（IEEE754 恒等）；三项实验默认关闭，全关时连能力探测都不执行、报告不多一行。
- **形状/设备/dtype**：掩码在 CPU float32 构造，由官方 `prepare_mask` 统一搬设备；初始 latent 用 `torch.zeros_like(官方 latent)`，天然同设备同 dtype。
- **显存净下降**：多一份 latent 副本的代价，远小于每段少采样 13 帧的收益。

### 残余风险（无法靠代码消除，只能靠实测）

1. **未实测**：本机与 AutoDL 侧都没有能出片的 H3 环境，本轮只到单测 + 源码级核对。**在真机 A/B 之前，软桥应视为未经验证的能力**。
2. **音频缩放**：`KSamplerX0Inpaint` 调的是 `BaseModel.scale_latent_inpaint`，未确认 H3 为音频流做了专门处理；若有偏差表现为接缝极小电平台阶，用「响度对齐强度」回加即可兜住。
3. **锚点越界体检会多打报告行**：`guides.audit_keyframes` 在实验全开全关时都执行。正常链不会触发（无越界锚点 → 零输出），但一旦存在历史越界锚点，报告会多出告警行。报告不进指纹、不影响出片，属可接受的信息增益。
4. **段中锚点用静态素材的风险**：已改为优先用 E2 记忆锚（动态视频 latent）；退化为静态首帧图时报告会明确提示"可能使构图回落"。

## 八、已知风险与待办

- **未实测**：本机与 AutoDL 侧都没有可用于出片的 H3 环境，本轮验证只到单测 + 源码级核对。软桥的实际画质 / 缝差收益与采样耗时节省需要在真机 A/B 上确认后再考虑转正（提升为控件或改默认值）。
- **音频缩放**：`KSamplerX0Inpaint` 调的是 `BaseModel.scale_latent_inpaint`，未确认 H3 是否为音频流做了专门处理；若音频钉住区有轻微电平偏差，表现为接缝处极小台阶，调「响度对齐强度」可兜。
- **二采（高清重制）**：`_up_hi` 接收的是 `skip_f`，软桥下数值变小，二采的裁剪口径随之变化——已在报告里体现，但高清路径尚未在软桥下实测。
- **E4 过渡重生成**：软桥下缝差通常已达标，E4 触发概率下降；两者可共存（E4 作为兜底），未做互斥。
- **后续可选项**：软桥稳定后可考虑把段首桥 keyframe 降级为"仅身份锚（少量 token）"甚至关闭，进一步缩短注意力序列。
- 安全审查的完整记录见上一节；其中「回放分支裁剪量不同步」是本轮修掉的唯一真 bug，相关不变量已由 `test_resolve_crop_*` 两例锁住，改动该函数会直接测挂。
