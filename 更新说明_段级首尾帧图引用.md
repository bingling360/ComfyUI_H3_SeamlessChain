# 更新说明：段级首尾帧图引用（每段可勾选是否参考首帧/尾帧图）

> 2026-08-25。此前首帧图片只注入第 1 段（i2v 起手）、尾帧图片只注入最后一段
> （剧情终点锚），中段无从参考。现在像多参模式的段级素材勾选一样，每个段卡片
> 上可以直接勾选本段是否参考首帧图/尾帧图。

## 背景

首帧视频模式下两张图的注入范围是写死的：

- **首帧图片**：仅第 1 段（`first_frame=首帧图片 if i == 0 else None`）；
- **尾帧图片**：仅最后一个提示词段的末帧 keyframe（FL2VA 剧情终点）。

长链中段只能靠段间尾帧桥逐段接力，首帧图的身份信息无法沿链持续生效——
这正是用户遇到的"首帧参考图只在第一个视频参考"的问题。

## 新增：段卡片「首尾帧图」勾选行

- **入口**：导演台「首帧视频」模式且设了首帧图/尾帧图时，每个段卡片在
  「引用素材」行下方显示「首尾帧图」行——两个 chip（带缩略图）：
  「首帧图」「尾帧图」，样式与多参素材 chips 一致。
- **JSON 契约**：`ds.segments[i].frame_refs`：
  - 缺省（`null`/无键）＝旧行为：首段参考首帧图、末段参考尾帧图，其余段不参考；
  - 数组＝显式勾选（可含 `"首帧图"`/`"尾帧图"`）；空数组＝两张图都不参考；
  - 首次点选把缺省值物化为显式数组再切换，行尾提示「未设置=默认」。

## 勾选语义

| 勾选 | 首段 | 中段 | 末段 |
|---|---|---|---|
| 首帧图 | i2v 起手帧（=现状） | **新增**：首帧图 latent 注入段头 keyframe（身份锚，与段首桥同位叠加，同 E2 记忆锚段首注入模式） | 头部身份锚 |
| 尾帧图 | 该段末帧锚（剧情终点锚提前） | 该段末帧锚（链向终点收拢） | 末帧 keyframe（=现状） |

- **同位置唯一锚**：勾了尾帧图的段，末帧锚＝尾帧图 latent，不再叠加每段尾帧锚定；
  未勾段回落到每段尾帧锚定（若设）。
- **独立镜头段**：首帧图头锚照常注入（用户主动设定的本段锚，与段间衔接无关，
  同每段尾帧锚定的断链规则）。
- **官方指令行**：首段首帧行/末段尾帧行随勾选状态注入；首段关掉首帧图时，
  末段尾帧行的 `<Picture>` 编号自动从 2 前移到 1。
- **锚定加噪**：头锚与桥/尾锚共用同一条 `minimax_keyframes` 通道，
  锚定加噪/递减锚定对它同样生效。

## 存档与重做（哈希规则）

- 显式勾选且**与默认值不同**的段，`fr:` 标记进逐段哈希 → 重跑自动从该段起重做；
- 显式但等于默认（点过又点回）→ 行为不变，不触发重做；
- 未设置段哈希完全不变 → **旧项目存档续跑零影响**。

## 二采（潜空间放大）同步

头锚 latent 与尾锚/桥一致走神经放大后注入高清重采样 cond
（`render_segment`/`render_latent` 新增 `head_kf_latent` 参数）；
首段关掉首帧图时二采也不传 first_frame——基础链与二采行为始终一致。

## 测试

`tests/test_frame_refs.py`（11 例）：默认值/解析（过滤未知、去重、保序、非数组视为
未设置）/哈希规则（等于默认不重做、中段加锚与末段关锚触发该段重做）/
`_apply_guide` 头锚注入位置（段头 index 0、与桥同位、尾锚钉段尾）。

## 报告行示例

```
首尾帧图段级引用：首帧图→段1、3（首段=i2v 起手，中段=头部身份锚） · 尾帧图→段4 ；未勾段尾部锚回落到每段尾帧锚定
段 3/4：… | guide=上段尾34帧 · 首帧图头锚
```

## Bugfix（2026-08-25 追加）：勾选不刷新 / 上传图片不出现 chips

首版上线后段卡片的首尾帧 chips 点了不动（勾不上、取消不了）、上传首帧/尾帧图后
chips 行不出现。根因：中栏段卡片区由 `cardsSignature` 签名守卫按需重建，而签名
**没含 `segments[i].frame_refs` 和 `first_frame`/`end_frame`**——数据其实已写入
JSON（节点状态是对的），但签名不变 → 卡片永不重建 → UI 停在旧状态。

修复：`cardsSignature` 的 segs 项并入 `frame_refs`（数组 join，null 区分未设置），
并新增 `frames: [first_frame, end_frame]` 项（换图/撤图也触发重建）。

