# 更新说明：总提示词——多段一次性分配 + skill 规范

> 2026-08-23。提示词现状回顾 + 新增「📋 总提示词」：按固定格式粘贴一次，
> 场景/角色/环境音/配乐/时长/主体自动分配到所有段；配套 ZCode skill
> `h3-video-prompts` 让 AI 直接生成合规全文。

## 提示词注入链路（现状，便于理解总提示词写在哪一层）

每段共五层字段，运行时由后端 `_compose_official()` 组装成官方三字段结构发给模型：

```
ds.prompts[i]                 段主体（时间线+运镜+对白）
segments[i].scene_prompt      场景（风格+环境+光照）   ┐
segments[i].character_prompt  角色（外观+服饰+位置）   │ 段卡片「🎬 分段处理」四框
segments[i].soundscape        环境音（overall_soundscape）│
segments[i].music             配乐（non_diegetic_music）┘
```

组装产物：`integrated_multimodal_description: [Shot N] 场景。角色。主体` + `overall_soundscape:` +
`non_diegetic_music:`；对白「」自动转 `<d>[中文]`；主体验到官方字段标签时原样直通。

## 新增：总提示词格式（六标签）

```
【段1】
场景：…
角色：…
环境音：…
配乐：…
时长：5          ← 可选，秒；缺省=沿用全局「每段时长」
提示词：…        ← 可多行（续行不写标签）
```

- 段头【段1】/【第2段】/【段落1】等价，序号可省略（按出现顺序）；整篇无段头=单段。
- **写了即生效（写空=清空），没写不动**：分配只覆盖出现过的标签，段内素材勾选、
  断链标记等未提及字段原样保留；插入视频段（inserts）不受影响。
- 只认 6 个行首标签，其余文本原样进主体（官方直通不受干扰）；CRLF/空行/游离行容错，
  异常进警告提示不阻断。

## 前端

- 段落流水线标题栏新增「📋 总提示词」按钮 → 模态框：
  - 大号等宽文本域，实时识别预览（段数 + 各字段命中数）与警告行；
  - 「从当前段落导出」把现有链导出为同格式文本（喂 AI 续改再贴回）；
  - 「解析并分配」重排段数并写入五层字段，同步项目 manifest 底稿。
- 解析/导出/应用为纯函数（`parseMasterPrompt` / `exportMasterPrompt` /
  `applyMasterPrompt`），harness 新增 20+ 断言（含导出→解析回环）。

## skill：h3-video-prompts

位置：`D:\h3长视频节点开发\.agents\skills\h3-video-prompts\SKILL.md`（工作区级，
ZCode 会话里说"生成多段提示词/分镜脚本"即可触发，也可 `/h3-video-prompts`）。

- 输出契约：只输出格式文本（备注只能放文末），格式语法与解析器逐条对齐；
- 内容规则：世界观字段逐字复用防接缝漂移、每段一个具体运镜、对白「」自动转换、
  场景/角色写常量而主体写本段事件；
- `tools/check_skill_prompt_format.js`：把 SKILL.md 的示例块抽出来跑真实解析器校验
  （段数/字段/零警告），skill 改动后可重跑防格式漂移。

## 验证

1. `node tests/_js_harness.js` / `python -m pytest tests/` 全过；
2. `node tools/check_skill_prompt_format.js` 全过；
3. 同步后：导演台 → 段落流水线 → 「📋 总提示词」→ 贴入 skill 生成的全文 →
   预览行显示正确段数与字段命中数 → 分配后段卡片数量与各段「分段处理」内容对位。

## 扩展（同日）：八标签 + 【完】标记 + skill 重构为「提示词翻译官」

针对本地部署弱文本编码器的提示词难题（官方 32B 编码器 → 本地 4B/8B/量化版退化：
长句丢尾、代词漂移、否定反转、抽象词无效），总提示词格式从六标签扩到**八标签**：

- `独立镜头：是/否` → seg.unlink（断链不锚定上段尾帧；跳转/闪回/蒙太奇段用）
- `参考：角色1，图片2` → seg.refs（逗号/顿号分隔；**应用时按「素材与参考」已有标签
  过滤**，未知标签剔除并提示，根除运行期「引用未知素材标签」中断）
- `【完】`结束标记：其后内容（AI 输出的参考素材建议/备注）不参与解析——
  skill 因此能在格式文本后附带建议块而不污染段落

skill `h3-video-prompts` 重构为**剧情→H3 语言的翻译官**（依据官方 h3-prompt-writing
skill 与社区对官方 45 条提示词的六块结构逆向工程）：

- 弱编码器语言规则：短句主谓宾、时间线 `[0s-3s]` 切片、正向措辞（否定最少化）、
  角色描述逐段逐字复用、每段一个运镜词、字面文本直角引号；
- 自动化决策：时长按节拍估算（5-10s）、独立镜头自动判定、参考标签只在用户已有时写入；
- 「参考素材建议」块：每条含名称/用途/**可直接粘贴 GPT 画图的提示词**（自包含
  画风契约+构图），建议 ≤4 条对齐系统 9图/3视频/3音频上限；
- 深度参考合并进 SKILL.md 的 PART B 附录（合同前置+自门控分区：常规任务只读
  PART A，附录不改写合同；单文件 262 行自包含，便于整体粘给任意 AI 助手）。

`tools/check_skill_prompt_format.js` 同步升级：完整示例（3 段）过真实解析器，
断言八标签对位与【完】截断。

## 严谨性修订（跨 agent）

- 解析器容忍 markdown 代码围栏（跨 agent 输出常被围栏包裹，围栏行不解析不报警）
- skill：时长改为每段必写且=时间线止点（修示例段3缺时长）；时间戳精确规范
  `[Ns-Ms]`（0s 起、连续、不重叠）；多角色分号规则；输出前自检清单；
  移植说明（全文可粘给任意 AI 助手作系统指令）；音频跨段连续性诚实说明

## 修复：markdown 软换行丢失导致整篇塌缩成 1 段（mpReflow）

> 2026-08-23。现象：把 AI 在聊天界面生成的总提示词粘贴进「📋 总提示词」，
> 预览只识别 1 段、八标签全部糊进主体，且无任何警告。

根因：聊天界面按 markdown 渲染输出时，段内**单个换行复制后变空格**（软换行丢失），
一段七行糊成一行——段头正则要求独占一行，于是全文被判为"无段头=单段主体"，
静默塌缩。实测 8 段故事原文（换行完好）经解析器 8 段全对位零警告，证明格式本身
无问题，坏在复制环节。

修复（两层）：

- **解析器**（`parseMasterPrompt` 前置 `mpReflow`）：行内出现段头（非独占行）是
  走样铁证，此时整篇重分行——段头与【完】前后插换行、行中八标签前提行，再按常规
  行解析，并进警告「已自动重分行解析」提示用户。未走样文本原样返回，主体里写
  「参考：」等标签字样不受影响（重分行仅在检测到走样后发生）。
- **skill**：输出规则从"不要代码围栏"反转为"markdown 渲染界面**必须**用围栏包裹"
  ——围栏保护换行且解析器忽略围栏行；纯文本终端可不加。

harness 新增 7 条断言（走样文本字段对位、【完】后截断、未走样文本行内标签不动）；
`tools/check_skill_prompt_format.js` 既有断言不变全过。
