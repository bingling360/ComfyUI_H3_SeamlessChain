# 二采抗糊、抗条纹增强说明

本轮在既有「神经放大 latent + 低强度重采样」二采链路上补齐了细节增强、编码质量和可观测性。所有新增增强默认关闭或保持标准档，旧工作流不改行为；启用项会进入二采指纹，只重做二采，不重跑基础链。

## 参数与作用

| 参数 | 默认 | 建议起点 | 作用与代价 |
|---|---:|---:|---|
| STG 引导 | 0（关） | 0.5–0.8，块 25 | 激活窗内增加一次跳块弱前向，再放大完整/弱分支差异；CFG=1 也有效。每个激活步约多一次前向，遇到已有 post-CFG hook 或同块 replacement 会明确拒绝覆盖。 |
| 精化轮数 / σ 衰减 | 1 / 0.5 | 2 轮 / 0.5 | 在上一轮输出上以更低 σ 再精化，适合先稳结构再补纹理；耗时近似按轮数增加。 |
| latent 锐化 | 0（关） | 0.2–0.3 | VAE 解码前放大空间高频；CPU 分块，不增加模型前向。过大会放大噪点或伪影。 |
| 像素锐化 | 0（关） | 0.15–0.3 | VAE 解码后、编码前补偿解码软化；与 latent 锐化可叠加，但建议一次只调一种。 |
| 编码档位 | 标准 | 高清 | 标准=`CRF 20 / veryfast`；高清=`CRF 16 / medium / AQ 3 / Bayer 抖动`；极致=`CRF 13 / slow / AQ 3 / Bayer 抖动`。同时用于二采分段和最终高清成片。 |
| 独立采样器 / 调度器 | 空 | 先沿用主链 | 非空时仅二采使用；名称不在当前 ComfyUI 注册表时自动回退主链，并写入运行报告。 |
| 增益重试 / 目标 | 关 / 0.15 | 仅问题段开启 | 细节增益未达目标时以更高 σ 重跑整条精化链，取增益更高者；最坏会让该段精化耗时再翻一倍。 |

段报告新增 `细节`、`清晰`、实际 σ 序列、STG、锐化、编码和重试结果；manifest 的二采段记录增加 `hf_gain`、`motion`、`sharp`，这些观测值不参与重做判定。

## 推荐调参顺序

1. 先保持单轮、STG 和两种锐化全关，只把编码档改为「高清」，确认模糊是否主要来自 H.264 二次压缩或 8bit 渐变色带。
2. 细节增益接近 0 时，先把精化起始 σ 或步数小幅上调；结构开始漂移则提高细节混合，不要先堆锐化。
3. 仍缺纹理时开第二轮精化；STG 放在最后尝试，从 0.5、块 25 起，不建议与未知的其他采样后引导同时使用。
4. VAE 解码后观感偏软再加像素锐化；噪点、描边或振铃出现即回退。
5. 增益重试只用于少量难段。多轮 + STG + 重试同时开启时，时间和显存压力会显著增加。

## 编码与条纹

Bayer 抖动只在原始浮点帧第一次量化到 RGB 8bit 时执行。最终成片由已编码分段解码后再拼接，已经没有高于 8bit 的信息，因此不会重复抖动；最终拼接仍沿用所选 CRF、preset 和 AQ，并保留分段中已有的抖动纹理。这样可避免把 `uint8` 帧误当 `0..1` 浮点再次量化造成高光截断或整片发白。

## 兼容性与风险

- shift 依赖当前 ComfyUI 的 `ModelSamplingAV + CONST`；缺少 API 时会给出升级提示，不会静默套用错误时钟。
- STG 机制参考 [T8mars/comfyui-minimax-h3-audio-T8](https://github.com/T8mars/comfyui-minimax-h3-audio-T8) 的 GPL-3.0 实现思路，本项目按二采局部进度和现有 conditioning 契约重新接入。
- STG 会在安装时检查已有 `sampler_post_cfg_function` 和目标 double block replacement，弱分支执行前再检查一次运行时冲突。
- 二采预检失败或采样 OOM 会停止整链，避免产生高清/基础分辨率混排；普通单段非显存异常仍会记录错误并回退该段。
- 自动化测试覆盖纯函数、状态迁移、指纹、真实 PyAV 编码/拼接和 ComfyUI API 桩。最终效果仍需在真实 H3 模型、目标显卡和实际素材上做一段 GPU 冒烟测试。
