# 更新说明：段落缩略图竖屏修复（9:16 段落恢复可播放/全屏/下载）

> 2026-08-26。修一个老毛病：段落卡片的缩略图小框，16:9 段落可以播放、
> 右下角三个点（⋮）里全屏浏览/下载，9:16 竖屏段落却完全没有控制栏——
> 点不了播放、没三个点，等于没法浏览。

## 现象与根因

`.h3d-thumb` 是 150px 宽、16:9（高 84.4px）的盒子，`overflow:hidden`，
内部 `display:grid`，视频作为 grid item 写的是 `width:100%;height:100%`。

问题在 **grid 自动行高的循环依赖**：`height:100%` 要等行高定下来才解析，
而自动行高又按 item 内容（intrinsic 尺寸）定 → 百分比失效回退成
intrinsic 高度。于是：

| 段落画幅 | video 元素实际高度 | 结果 |
|---|---|---|
| 16:9（640×360） | 150×84.4（intrinsic 高恰好=盒高） | 侥幸填满，控制栏可见 |
| 9:16（360×640） | 150×**266.7** | 底部溢出 182px 被 `overflow:hidden` 裁掉，**原生控制栏（含 ⋮ 菜单）整条消失** |

也就是说 16:9「正常」纯属巧合——横屏 intrinsic 高度正好等于盒高；
任何非 16:9 画幅（9:16、3:4、1:1）控制栏都会被裁。

## 修复（`web/h3_director.js`）

1. **视频/图片绝对定位填满盒子**（根因修复）：
   `.h3d-thumb{position:relative}` + `.h3d-thumb video,.h3d-thumb img{position:absolute;inset:0}`。
   视频元素恒等于盒子尺寸（150×84.4），任何画幅下原生控制栏都完整显示在盒底。
2. **竖屏段自动换竖版缩略盒**：视频元数据加载后若 `videoHeight > videoWidth`，
   盒子加 `portrait` 类 → `aspect-ratio:9/16; height:150px`（宽自适应约 84px），
   预览面积约 2.6 倍，`object-fit:contain` 不再只剩中间细条；横屏段保持原样。
3. **悬浮「⛶ 全屏 / ⬇ 下载」按钮**：缩略图右上角常驻两个小钮——
   全屏走 `video.requestFullscreen()`（失败兜底新标签页打开），下载为
   `<a download>` 直链。不再依赖原生 ⋮ 菜单（元素窄到 ~84px 时浏览器会隐藏它，
   悬浮钮保证任何尺寸下全屏/下载一键可达）。

## 验证

- 本地对照实验（150×84.4 盒 + h264 测试视频）：原版 CSS 下 9:16 video 元素
  150×266.7、底部溢出 -182.3px（复现用户 bug）；修复后 16:9 → 150×84.4、
  竖屏自动 84.4×150，两者溢出均为 0px，控制栏与悬浮钮均可见；
- 同 pattern 排查：`H3ChainSaver` 画廊（`.h3sv-seg video` 用 `aspect-ratio`
  直接约束元素）与右栏成片区（`.h3d-result video` 无裁剪容器）不受此 bug 影响，
  无需改动。

## 部署

1. 本地跑「同步到ComfyUI.bat」→ 重启 ComfyUI；
2. 浏览器 **Ctrl+F5 强刷**（旧 JS 有缓存）；
3. 控制台过滤 `[h3-director]` 应显示版本 `20260826+portrait-thumb`；
4. 找一个含 9:16 段落的项目：缩略图应为竖版盒，能播放、右上角有 ⛶/⬇，
   悬停视频底部有完整控制栏。
